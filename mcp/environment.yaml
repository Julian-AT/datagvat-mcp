name: austria-mcp
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - pip
  - pip:
      # Core dependencies
      - fastmcp>=2.3.0
      - httpx>=0.27.0
      - rdflib>=7.0.0
      - pydantic>=2.0.0
      - pydantic-settings>=2.0.0
      # Dev/Test dependencies
      - pytest>=8.0.0
      - pytest-asyncio>=0.23.0
      - pytest-cov>=4.1.0
      - pytest-mock>=3.12.0
      - ruff>=0.4.0

