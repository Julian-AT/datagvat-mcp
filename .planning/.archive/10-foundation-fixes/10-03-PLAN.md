---
phase: 10-foundation-fixes
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - docs/components/language-toggle.tsx
  - docs/app/[lang]/docs/layout.tsx
autonomous: false

must_haves:
  truths:
    - "User can click language toggle and switch between EN/DE"
    - "Language toggle preserves current page path when switching"
    - "Documentation displays correctly on mobile (375px), tablet (768px), and desktop (1024px+)"
    - "No layout shift or horizontal scroll on any screen size"
  artifacts:
    - path: "docs/components/language-toggle.tsx"
      provides: "Language switcher component"
      min_lines: 30
      exports: ["LanguageToggle"]
    - path: "docs/app/[lang]/docs/layout.tsx"
      provides: "Docs layout with responsive DocsLayout from Fumadocs"
      min_lines: 20
  key_links:
    - from: "docs/components/language-toggle.tsx"
      to: "next/navigation"
      via: "usePathname hook"
      pattern: "usePathname"
    - from: "docs/components/language-toggle.tsx"
      to: "docs/lib/i18n.ts"
      via: "import i18n"
      pattern: "from.*@/lib/i18n"
---

<objective>
Fix language switcher and verify responsive design across all screen sizes.

Purpose: Ensures language toggle works correctly with i18n routing and documentation displays properly on all devices.

Output: Working language switcher that preserves paths, fully responsive documentation layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-fixes/10-01-SUMMARY.md
@.planning/phases/10-foundation-fixes/10-02-SUMMARY.md
@docs/components/language-toggle.tsx
@docs/app/[lang]/docs/layout.tsx
@docs/lib/i18n.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix language toggle to work with middleware routing</name>
  <files>docs/components/language-toggle.tsx</files>
  <action>
Review and fix docs/components/language-toggle.tsx to ensure:

1. Uses usePathname() from next/navigation to get current path
2. Switches language by updating path segment: /en/docs/... → /de/docs/...
3. Uses Next.js Link component (not window.location) for client-side navigation
4. Preserves rest of path when switching (e.g., /en/docs/api/tools → /de/docs/api/tools)
5. Works with the middleware created in Task 10-01

Current implementation exists - verify it correctly:
- Imports i18n from @/lib/i18n
- Maps over i18n.languages to render options
- Uses getNewPath() function to construct locale-switched URL
- Uses Link from next/link for navigation

If issues found, fix the path construction logic to ensure it correctly replaces lang segment while preserving the rest.

Pattern to verify:
```typescript
const getNewPath = (newLang: string) => {
  const segments = pathname.split('/');
  segments[1] = newLang; // Replace locale segment
  return segments.join('/');
};
```

This should work with middleware that ensures all routes are /[lang]/... format.
  </action>
  <verify>
1. language-toggle.tsx uses usePathname()
2. getNewPath() correctly replaces lang segment
3. Uses Next.js Link for navigation
4. TypeScript compiles without errors
5. Component imports i18n.languages correctly
  </verify>
  <done>
Language toggle correctly switches locales while preserving paths. Requirement FOUND-05 partially satisfied (needs human verification).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify responsive layout configuration in DocsLayout</name>
  <files>docs/app/[lang]/docs/layout.tsx</files>
  <action>
Verify docs/app/[lang]/docs/layout.tsx uses Fumadocs DocsLayout component correctly for responsive design:

1. Imports DocsLayout from 'fumadocs-ui/layouts/docs'
2. Passes proper tree prop: source.pageTree[lang]
3. Includes nav configuration with logo and language toggle
4. Fumadocs DocsLayout is already responsive by default (mobile-first design)

Current layout exists - verify it has:
```typescript
<DocsLayout
  tree={source.pageTree[lang]}
  nav={{
    title: <Logo />,
    children: <LanguageToggle lang={lang} />,
  }}
>
  {children}
</DocsLayout>
```

The DocsLayout component from Fumadocs handles:
- Responsive sidebar (hamburger menu on mobile)
- Table of contents (TOC) positioning
- Mobile-optimized navigation
- No additional configuration needed

Just verify the component is used correctly with proper props. Fumadocs handles all responsive breakpoints internally.
  </action>
  <verify>
1. layout.tsx imports DocsLayout from fumadocs-ui
2. Passes tree={source.pageTree[lang]}
3. Includes nav prop with title and language toggle
4. TypeScript compiles without errors
5. No custom CSS overriding Fumadocs responsive behavior
  </verify>
  <done>
DocsLayout configured correctly for responsive design. Fumadocs handles breakpoints automatically. Requirement FOUND-06 partially satisfied (needs human verification).
  </done>
</task>

<task type="auto">
  <name>Task 3: Build and start development server for testing</name>
  <files>None (verification only)</files>
  <action>
Build the site and prepare for human verification:

1. Full production build: `cd docs && npm run build`
2. Verify build succeeds with all previous fixes
3. Note the dev server command for human testing: `cd docs && npm run dev`

Build should show:
- All routes compiled successfully
- Static pages for /en and /de
- No errors or warnings
- Optimized bundle generated

The development server will be needed for human verification of:
- Language toggle functionality
- Responsive design on different screen sizes
  </action>
  <verify>
1. cd docs && npm run build completes successfully
2. No build errors or warnings
3. .next/server/app/[lang] contains compiled routes
4. Build output shows successful optimization
  </verify>
  <done>
Build succeeds. Site ready for human verification of language toggle and responsive design.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Language toggle and responsive documentation layout</what-built>
  <how-to-verify>
1. Start development server: `cd docs && npm run dev`
2. Open browser to http://localhost:3000

**Test Language Toggle (FOUND-05):**
3. Navigate to /en/docs page
4. Click language toggle (Globe icon or language selector)
5. Verify:
   - Dropdown shows English and Deutsch options
   - Current language is indicated (checkmark or highlight)
   - Clicking Deutsch switches to /de/docs (same page in German)
   - Path is preserved (e.g., /en/docs/api/tools → /de/docs/api/tools)
6. Switch back to English - verify it works
7. Try on different documentation pages - verify toggle works everywhere

**Test Responsive Design (FOUND-06):**
8. Desktop (>1024px width):
   - Verify sidebar visible on left
   - Table of contents visible on right
   - Content area properly sized between them
   - No horizontal scroll
9. Tablet (768px-1024px):
   - Verify sidebar collapses to icon-only or hamburger
   - Content adjusts to available width
   - Navigation still accessible
10. Mobile (375px-768px):
    - Verify hamburger menu for navigation
    - Full-width content area
    - No horizontal scroll
    - Touch-friendly tap targets
11. Very small mobile (320px):
    - Verify still readable, no layout break
    - All content accessible

**Additional checks:**
12. Test on actual mobile device if possible (iOS/Android)
13. Check dark mode toggle works at all sizes (if implemented)
14. Verify no console errors in browser DevTools
  </how-to-verify>
  <resume-signal>Type "approved" if language toggle and responsive design work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/components/language-toggle.tsx correctly switches locales
- [ ] docs/app/[lang]/docs/layout.tsx uses DocsLayout properly
- [ ] cd docs && npm run build succeeds
- [ ] Human verified language toggle works
- [ ] Human verified responsive design on mobile/tablet/desktop
</verification>

<success_criteria>
- All tasks completed
- Language toggle switches between EN/DE while preserving paths
- Responsive design works on all screen sizes (mobile, tablet, desktop)
- No layout shift or horizontal scroll
- Human verification approved
- Requirements FOUND-05 and FOUND-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-fixes/10-03-SUMMARY.md`
</output>
