---
phase: 20-guides-and-workflows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guides/workflow-patterns.mdx
  - docs/guides/meta.json
autonomous: true

must_haves:
  truths:
    - "User finds workflow-oriented guide showing research and validation patterns"
    - "User navigates guides section with all 4 guides visible in navigation"
    - "User sees task-oriented guide titles in navigation (not tool names)"
    - "User accesses workflow patterns as reusable templates"
  artifacts:
    - path: "docs/guides/workflow-patterns.mdx"
      provides: "Reusable workflow patterns for research and validation tasks"
      min_lines: 150
    - path: "docs/guides/meta.json"
      provides: "Updated navigation with all 4 guides in learning progression order"
      contains: "[\"searching\", \"data-preview\", \"quality-metrics\", \"workflow-patterns\"]"
  key_links:
    - from: "docs/guides/meta.json"
      to: "all 4 guide files"
      via: "pages array"
      pattern: "\"pages\".*searching.*data-preview.*quality-metrics.*workflow-patterns"
    - from: "docs/guides/workflow-patterns.mdx"
      to: "other guides"
      via: "cross-references"
      pattern: "\\[.*\\]\\(\\/guides\\/"
---

<objective>
Create workflow patterns guide and update guides navigation to implement FOUND-04 and FOUND-05 requirements (complete navigation structure).

Purpose: Provide reusable workflow templates for research and validation tasks while implementing the navigation requirements from foundation phase. The meta.json update makes all guides visible in navigation with task-oriented structure (FOUND-04: "Complete navigation structure with sidebar and breadcrumbs" and FOUND-05: "Consistent naming conventions and patterns").

Output: workflow-patterns.mdx with research/validation workflow templates and updated meta.json implementing navigation requirements with all 4 guides in optimal learning progression order.
</objective>

<execution_context>
@C:\Users\travis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\travis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\STATE.md
@C:\GitHub\datagvat-mcp\.planning\REQUIREMENTS.md
@C:\GitHub\datagvat-mcp\.planning\phases\20-guides-and-workflows\20-RESEARCH.md
@C:\GitHub\datagvat-mcp\.planning\phases\18-documentation-foundation\18-01-SUMMARY.md
@C:\GitHub\datagvat-mcp\docs\guides\meta.json
</context>

<tasks>

<task type="auto">
  <name>Create workflow-patterns.mdx - Research and Validation Workflow Templates</name>
  <files>docs/guides/workflow-patterns.mdx</files>
  <action>
Create workflow patterns guide (GUIDE-04) with reusable templates for research and validation tasks.

**Structure:**
```markdown
---
title: Common Workflow Patterns
description: Reusable patterns for research and data validation tasks
---

# Common Workflow Patterns

Proven patterns for accomplishing common research and validation tasks with Austria MCP.

## When to Use This Guide

- You need a starting point for research workflows
- You want to validate data before use
- You need patterns for iterative dataset exploration
- You want to combine multiple tools effectively

## Research Workflow Pattern

**Goal:** Find, evaluate, and download datasets for research purposes

**When to use:** Academic research, literature reviews, citation-quality data needs

**Pattern:**

<Tabs items={['Overview', 'Detailed Steps']} groupId="workflow-style" persist>
  <Tab value="Overview">
    ### Quick Pattern

    1. Semantic search with quality boost
    2. Analyze quality scores (target 85+)
    3. Preview schema for required columns
    4. Verify license permits research use
    5. Download if all checks pass

    **Time:** 5-10 minutes per dataset

    **Success criteria:**
    - Quality score ≥85
    - All required columns present
    - License allows citation
    - Recent update (within 12 months)
  </Tab>

  <Tab value="Detailed Steps">
    ### Step-by-Step Research Pattern

    **Step 1: Targeted Search**

    Use semantic search with quality boost:

    ```python
    results = semantic_search_datasets(
        natural_query="[your research question]",
        boost_quality=True,
        limit=20
    )
    ```

    Filter for high-quality results (score >85).

    **Step 2: Quality Verification**

    Analyze top candidates:

    ```python
    for dataset in results['results'][:5]:
        if dataset.get('quality_score', 0) >= 85:
            quality = analyze_dataset_quality(dataset['id'])
            print(f"{dataset['title']}: {quality['metrics']}")
    ```

    Check for completeness, timeliness, compliance.

    **Step 3: Schema Validation**

    Verify required columns:

    ```python
    distributions = get_dataset_distributions(dataset_id)
    url = distributions[0]['downloadURL']
    schema = preview_schema(url=url)

    required = ["year", "region", "value"]
    actual = [c['name'] for c in schema['columns']]
    has_required = all(col in actual for col in required)
    ```

    **Step 4: License Check**

    ```python
    dataset = get_dataset(dataset_id)
    license = dataset.get('license')
    # Verify license allows research use
    # Common research-friendly: CC-BY, CC0, ODbL
    ```

    **Step 5: Download Decision**

    If all checks pass:
    - [ ] Quality score ≥85
    - [ ] Required columns present
    - [ ] Research-friendly license
    - [ ] Recent update

    Proceed with download using URL from distributions.

    **Cross-references:**
    - [Searching Guide](/guides/searching) - Semantic search details
    - [Quality Metrics Guide](/guides/quality-metrics) - Score interpretation
    - [Data Preview Guide](/guides/data-preview) - Schema validation
  </Tab>
</Tabs>

## Validation Workflow Pattern

**Goal:** Verify dataset meets requirements before production use

**When to use:** Application integration, production pipelines, data quality assurance

**Pattern:**

<Tabs items={['Overview', 'Detailed Steps']} persist groupId="workflow-style">
  <Tab value="Overview">
    ### Quick Pattern

    1. Get dataset metadata
    2. Check quality score (target 70+)
    3. Preview schema and sample data
    4. Validate data types match expectations
    5. Test download URL accessibility
    6. Approve for production use

    **Time:** 10-15 minutes per dataset

    **Success criteria:**
    - Quality score ≥70
    - Schema matches requirements
    - Data types correct
    - Download URL accessible
    - No truncated/corrupt data
  </Tab>

  <Tab value="Detailed Steps">
    ### Step-by-Step Validation Pattern

    **Step 1: Metadata Check**

    ```python
    dataset = get_dataset(dataset_id)
    required_fields = ['title', 'description', 'license', 'publisher']
    missing = [f for f in required_fields if not dataset.get(f)]
    if missing:
        print(f"Missing metadata: {missing}")
    ```

    **Step 2: Quality Assessment**

    ```python
    quality = analyze_dataset_quality(dataset_id)
    if quality['metrics']['overall_score'] < 70:
        print("Quality below production threshold")
        # Review quality breakdown
        print(quality['metadata'])
    ```

    **Step 3: Schema Validation**

    ```python
    distributions = get_dataset_distributions(dataset_id)
    url = next(d['downloadURL'] for d in distributions
               if d.get('format') == 'CSV')

    schema = preview_schema(url=url)

    # Validate columns
    required_columns = ["id", "timestamp", "value"]
    actual_columns = [c['name'] for c in schema['columns']]
    missing_cols = set(required_columns) - set(actual_columns)

    if missing_cols:
        print(f"Missing columns: {missing_cols}")
        # FAIL validation

    # Validate types
    type_requirements = {
        "id": "string",
        "timestamp": "string",  # ISO 8601
        "value": ["integer", "float"]  # Accept either
    }

    for col in schema['columns']:
        expected = type_requirements.get(col['name'])
        if expected and col['type'] not in (expected if isinstance(expected, list) else [expected]):
            print(f"Type mismatch: {col['name']} is {col['type']}, expected {expected}")
            # WARN or FAIL
    ```

    **Step 4: Data Sample Check**

    ```python
    data = preview_data(url=url, max_rows=20)

    # Check for null values in required fields
    for row in data['rows']:
        if not all(row.get(col) for col in required_columns):
            print("Null values in required columns")
            # FAIL validation

    # Check value ranges
    values = [row['value'] for row in data['rows']]
    if any(v < 0 for v in values):
        print("Negative values found (may be invalid)")
    ```

    **Step 5: Download Test**

    ```python
    import requests
    response = requests.head(url, timeout=10)
    if response.status_code != 200:
        print(f"Download URL inaccessible: {response.status_code}")
        # FAIL validation
    ```

    **Step 6: Validation Decision**

    Checklist for production approval:
    - [ ] All required metadata present
    - [ ] Quality score ≥70
    - [ ] Schema matches requirements
    - [ ] Data types correct
    - [ ] No null values in required fields
    - [ ] Value ranges valid
    - [ ] Download URL accessible

    **Cross-references:**
    - [Quality Metrics Guide](/guides/quality-metrics) - Quality thresholds
    - [Data Preview Guide](/guides/data-preview) - Schema/data validation
  </Tab>
</Tabs>

## Iterative Exploration Pattern

**Goal:** Discover datasets through progressive refinement

**When to use:** Unfamiliar domain, exploratory research, broad topic

**Pattern:**

1. **Broad Search:** Start with semantic search, no filters
2. **Facet Analysis:** Review facets (themes, formats, publishers) in results
3. **Refine:** Add relevant filters based on facets
4. **Related Datasets:** Use find_related_datasets on promising results
5. **Quality Filter:** Apply boost_quality once topic narrowed
6. **Deep Dive:** Analyze top 3-5 candidates with quality/preview tools

**Example progression:**

```
Search 1: "environmental data" → 500 results
  ↓ (Review facets: ENVI theme dominant, CSV/JSON formats)
Search 2: themes=["ENVI"], formats=["CSV"] → 150 results
  ↓ (Find related to top result)
Search 3: Related to dataset-123 → 25 similar datasets
  ↓ (Apply quality filter)
Search 4: Same criteria + boost_quality → 10 high-quality results
  ↓ (Analyze finalists)
```

**Cross-references:**
- [Searching Guide](/guides/searching) - Faceted search
- [Quality Metrics Guide](/guides/quality-metrics) - Related datasets

## Comparative Analysis Pattern

**Goal:** Compare multiple datasets for same topic

**When to use:** Choosing between alternatives, benchmarking, data fusion

**Pattern:**

1. Search for topic with multiple results
2. Analyze quality scores for all candidates
3. Preview schemas to compare structure
4. Find related datasets to expand options
5. Create comparison matrix
6. Select based on quality, recency, completeness

**Comparison matrix template:**

| Dataset | Quality | Columns | Last Update | Format | License |
|---------|---------|---------|-------------|--------|---------|
| A | 87 | 15 | 2024-01 | CSV | CC-BY |
| B | 72 | 12 | 2023-06 | JSON | ODbL |
| C | 65 | 18 | 2024-03 | CSV | CC0 |

**Decision criteria:**
- Research: Highest quality + recency
- Production: Quality + schema match + format
- Exploration: Most complete data (column count)

## Troubleshooting Patterns

### Pattern Fails at Search Step

**Symptom:** No relevant results from initial search

**Solutions:**
- Use semantic_search_datasets for query expansion
- Try German keywords if data is Austria-specific
- Search without filters first, then refine
- Check autocomplete_suggestions for theme/publisher IDs

### Pattern Fails at Quality Check

**Symptom:** All candidates below quality threshold

**Solutions:**
- Lower threshold (research 85→70, production 70→60)
- Review quality breakdown for acceptable gaps
- Prioritize completeness over timeliness if metadata sparse
- Consider manual verification for niche domains

### Pattern Fails at Schema Validation

**Symptom:** Required columns missing from all candidates

**Solutions:**
- Review column names in preview (may have different naming)
- Check if data structure differs from expectations
- Find related datasets with different schemas
- Contact publisher if critical data expected but missing

## Next Steps

- **[Dataset Discovery Workflow](/workflows/discovery)** - Complete end-to-end example
- **[Quality Assessment Workflow](/workflows/quality-assessment)** - Detailed quality evaluation
- **[Searching Guide](/guides/searching)** - Search techniques reference
- **[Quality Metrics Guide](/guides/quality-metrics)** - Quality scoring reference
```

**Target:** 150-200 lines covering research pattern, validation pattern, iterative exploration, comparative analysis with Overview/Detailed tabs for each pattern.
  </action>
  <verify>
```bash
cd C:/GitHub/datagvat-mcp/docs/guides && \
  grep -c "groupId=\"workflow-style\"" workflow-patterns.mdx && \
  grep -c "Pattern" workflow-patterns.mdx && \
  grep -c "\\[.*\\](/guides/" workflow-patterns.mdx && \
  wc -l workflow-patterns.mdx
```

Expected:
- 4+ instances of groupId="workflow-style"
- 10+ "Pattern" headings/mentions
- 6+ cross-references to other guides (verify count ≥6)
- 150-200 lines total
  </verify>
  <done>
workflow-patterns.mdx exists with 4 reusable patterns (research, validation, iterative, comparative), Overview/Detailed tabs for each, 6+ cross-references to other guides, 150+ lines
  </done>
</task>

<task type="auto">
  <name>Update guides meta.json implementing navigation requirements</name>
  <files>docs/guides/meta.json</files>
  <action>
Update guides section navigation to implement FOUND-04/FOUND-05 requirements (complete navigation structure with consistent naming).

**Current state (from context):**
```json
{
  "$schema": "../.source/json-schema/docs.meta.json",
  "title": "Guides",
  "description": "Comprehensive guides for using Austria MCP",
  "icon": "Book",
  "root": true,
  "pages": ["configuration", "searching", "quality-metrics", "data-preview"]
}
```

**Updated configuration (implements FOUND-04/FOUND-05):**
```json
{
  "$schema": "../.source/json-schema/docs.meta.json",
  "title": "Guides",
  "description": "Task-oriented guides for common Austria MCP workflows",
  "icon": "Book",
  "root": true,
  "pages": [
    "searching",
    "data-preview",
    "quality-metrics",
    "workflow-patterns"
  ]
}
```

**Changes implementing navigation requirements:**
1. Remove "configuration" (doesn't exist, incomplete from Phase 18)
2. Order by learning progression (FOUND-05: consistent patterns):
   - searching (start with finding datasets)
   - data-preview (inspect what you found)
   - quality-metrics (evaluate what you found)
   - workflow-patterns (combine techniques into patterns)
3. Update description to emphasize task-oriented approach (FOUND-05: naming conventions)
4. Complete navigation structure with all guides visible (FOUND-04)

**Navigation order rationale (FOUND-05):**
- searching: First action users take (find datasets)
- data-preview: Second action (inspect before downloading)
- quality-metrics: Evaluation step (assess quality)
- workflow-patterns: Advanced step (combine techniques)

This follows natural workflow progression from discovery → inspection → evaluation → integration, implementing consistent navigation patterns per FOUND-04/FOUND-05.
  </action>
  <verify>
```bash
cd C:/GitHub/datagvat-mcp/docs/guides && \
  cat meta.json | grep -c "\"searching\"" && \
  cat meta.json | grep -c "\"workflow-patterns\"" && \
  cat meta.json | grep -c "\"configuration\"" && \
  cat meta.json | jq '.pages | length'
```

Expected:
- 1 instance of "searching"
- 1 instance of "workflow-patterns"
- 0 instances of "configuration" (removed)
- pages array length: 4
  </verify>
  <done>
meta.json updated implementing FOUND-04/FOUND-05: all 4 guides in learning progression order (searching, data-preview, quality-metrics, workflow-patterns), configuration removed, description updated to emphasize task-oriented approach, complete navigation structure established
  </done>
</task>

</tasks>

<verification>
After completion:

1. **Navigation Completeness:**
```bash
cd C:/GitHub/datagvat-mcp/docs/guides && \
  cat meta.json | jq '.pages'
```
Expected: ["searching", "data-preview", "quality-metrics", "workflow-patterns"]

2. **File Creation:**
```bash
ls docs/guides/workflow-patterns.mdx
```
Expected: File exists

3. **Cross-References:**
```bash
grep -o "/guides/[a-z-]*" docs/guides/workflow-patterns.mdx | sort -u
```
Expected: Links to searching, data-preview, quality-metrics

4. **Build Validation:**
```bash
cd docs && npm run build 2>&1 | grep -i "error" | wc -l
```
Expected: 0 errors

5. **All Guides Present:**
```bash
cd docs/guides && ls -1 *.mdx | wc -l
```
Expected: 4 files (searching, data-preview, quality-metrics, workflow-patterns)
</verification>

<success_criteria>
1. workflow-patterns.mdx covers research and validation patterns with reusable templates (GUIDE-04 satisfied)
2. All 4 guides have task-oriented titles following GUIDE-06 pattern
3. meta.json includes all 4 guides in learning progression order (FOUND-04 satisfied)
4. Navigation description emphasizes task-oriented approach (FOUND-05 satisfied)
5. Cross-references connect workflow-patterns to other guides (6+ verified)
6. Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-guides-and-workflows/20-02-SUMMARY.md` following summary template.
</output>
