---
phase: 09-fumadocs-component-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/mdx-components.tsx, docs/app/[lang]/docs/[[...slug]]/page.tsx]
autonomous: true

must_haves:
  truths:
    - "Images in MDX files are zoomable when clicked"
    - "Relative MDX file links resolve correctly"
    - "All Fumadocs components are available in MDX files"
  artifacts:
    - path: "docs/mdx-components.tsx"
      provides: "ImageZoom integration and component exports"
      exports: ["useMDXComponents", "ImageZoom"]
    - path: "docs/app/[lang]/docs/[[...slug]]/page.tsx"
      provides: "Relative link support via createRelativeLink"
      contains: "createRelativeLink"
  key_links:
    - from: "mdx-components.tsx"
      to: "MDX files"
      via: "useMDXComponents export"
      pattern: "export function useMDXComponents"
    - from: "page.tsx"
      to: "createRelativeLink"
      via: "fumadocs-ui/mdx import"
      pattern: "createRelativeLink\\(source"
---

<objective>
Enhance MDX component infrastructure with ImageZoom and relative link support.

Purpose: Enable interactive image viewing and proper MDX file linking as foundation for component-rich documentation.
Output: Enhanced component configuration supporting all Fumadocs UI features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/mdx-components.tsx
@docs/app/[lang]/docs/[[...slug]]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ImageZoom to mdx-components.tsx</name>
  <files>docs/mdx-components.tsx</files>
  <action>
    Import ImageZoom from 'fumadocs-ui/components/image-zoom'.
    In useMDXComponents function, override the img element with ImageZoom component:
    ```tsx
    img: (props) => <ImageZoom {...(props as any)} />
    ```
    This wraps all MDX images with zoom functionality while preserving all image attributes.
    Do NOT modify existing component exports - only add ImageZoom integration.
  </action>
  <verify>npm run build succeeds, no TypeScript errors</verify>
  <done>ImageZoom imported and img tag overridden in useMDXComponents</done>
</task>

<task type="auto">
  <name>Task 2: Add relative link support to page.tsx</name>
  <files>docs/app/[lang]/docs/[[...slug]]/page.tsx</files>
  <action>
    Import createRelativeLink from 'fumadocs-ui/mdx' and getMDXComponents from '@/mdx-components'.
    Import source from '@/lib/source'.

    In the MDXContent rendering, wrap with getMDXComponents that includes createRelativeLink:
    ```tsx
    import { createRelativeLink } from 'fumadocs-ui/mdx';
    import { getMDXComponents } from '@/mdx-components';
    import { source } from '@/lib/source';

    const components = getMDXComponents({
      a: createRelativeLink(source, page),
    });

    <MDXContent components={components} />
    ```

    This enables relative file paths like `[link](./other-file.mdx)` to work correctly.
    Keep existing logic for page rendering, metadata, and table of contents.
  </action>
  <verify>npm run build succeeds, TypeScript validates</verify>
  <done>Relative link support added via createRelativeLink integration</done>
</task>

<task type="auto">
  <name>Task 3: Verify component availability</name>
  <files>docs/mdx-components.tsx</files>
  <action>
    Confirm all Fumadocs components are properly exported from useMDXComponents:
    - defaultMdxComponents (base components)
    - Accordion, Accordions
    - Tab, Tabs
    - Step, Steps
    - TypeTable
    - Callout
    - Card, Cards
    - File, Files, Folder
    - ImageZoom (newly added)

    No changes needed if all are present - this is verification only.
    Run `npm run build` to ensure no import/export errors.
  </action>
  <verify>npm run build succeeds, all components available in MDX</verify>
  <done>All Fumadocs UI components confirmed available for MDX usage</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] TypeScript compilation passes
- [ ] All Fumadocs components exported from mdx-components.tsx
- [ ] ImageZoom integrated for img tags
- [ ] createRelativeLink integrated in page.tsx
</verification>

<success_criteria>
- All tasks completed
- Build succeeds
- No TypeScript errors
- Component infrastructure ready for enhanced documentation
</success_criteria>

<output>
After completion, create `.planning/phases/09-fumadocs-component-integration/09-01-SUMMARY.md`
</output>
