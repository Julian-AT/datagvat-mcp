---
phase: 02-navigation-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/content/docs/test-meta-syntax/meta.json
  - docs/content/docs/test-meta-syntax/index.mdx
  - docs/content/docs/test-meta-syntax/(test-group)/item-root/meta.json
autonomous: true

must_haves:
  truths:
    - "Advanced meta.json syntax support is known (separators, external links, rest operators, folder groups)"
    - "Test documentation demonstrates which features work in current Fumadocs version"
    - "Architecture decisions for Phase 2 are based on verified capabilities"
  artifacts:
    - path: "docs/content/docs/test-meta-syntax/meta.json"
      provides: "Test file with all advanced syntax features"
      min_lines: 30
    - path: ".planning/phases/02-navigation-restructuring/syntax-support.md"
      provides: "Documentation of verified syntax support"
      min_lines: 50
  key_links:
    - from: "test-meta-syntax/meta.json"
      to: "Fumadocs loader"
      via: "next build process"
      pattern: "pages.*array"
---

<objective>
Verify advanced Fumadocs meta.json syntax support before implementing navigation restructure.

Purpose: Research identified several meta.json features (external links with `external:[Text](url)`, extract operator `...folder`, reversed rest `z...a`, exclude operator `!item`, icon separators `---[Icon]Label---`, folder groups with root:true) that are not documented in official sources. Testing these features before implementation prevents mid-execution architecture changes.

Output:
- Test section demonstrating all proposed syntax features
- Documented list of supported vs unsupported features
- Clear guidance for Plan 02-03 architecture decisions
</objective>

<execution_context>
@C:/GitHub/datagvat-mcp/.planning/get-shit-done/workflows/execute-plan.md
@C:/GitHub/datagvat-mcp/.planning/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/GitHub/datagvat-mcp/.planning/PROJECT.md
@C:/GitHub/datagvat-mcp/.planning/ROADMAP.md
@C:/GitHub/datagvat-mcp/.planning/STATE.md
@C:/GitHub/datagvat-mcp/.planning/phases/02-navigation-restructuring/02-CONTEXT.md
@C:/GitHub/datagvat-mcp/.planning/phases/02-navigation-restructuring/02-RESEARCH.md

# Phase 1 infrastructure
@C:/GitHub/datagvat-mcp/.planning/phases/01-infrastructure-modernization/01-01-SUMMARY.md

# Current navigation structure
@C:/GitHub/datagvat-mcp/docs/content/docs/meta.json
@C:/GitHub/datagvat-mcp/docs/content/docs/getting-started/meta.json
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Create test section with basic syntax features and folder groups</name>
  <files>
    docs/content/docs/test-meta-syntax/meta.json
    docs/content/docs/test-meta-syntax/index.mdx
    docs/content/docs/test-meta-syntax/item-a.mdx
    docs/content/docs/test-meta-syntax/item-b.mdx
    docs/content/docs/test-meta-syntax/item-c.mdx
    docs/content/docs/test-meta-syntax/(test-group)/item-root/meta.json
    docs/content/docs/test-meta-syntax/(test-group)/item-root/index.mdx
  </files>
  <action>
Create test documentation section to verify basic and folder group syntax:

**Create test-meta-syntax/meta.json:**
```json
{
  "$schema": "../.source/json-schema/docs.meta.json",
  "title": "Meta Syntax Test",
  "description": "Testing advanced meta.json features",
  "icon": "TestTube",
  "root": true,
  "pages": [
    "---Basic Separator---",
    "index",
    "item-a",
    "...",
    "!item-c",
    "---Folder Groups---",
    "(test-group)"
  ]
}
```

**Create test-meta-syntax/index.mdx:**
Minimal MDX file explaining this is a syntax test section.

**Create test-meta-syntax/item-*.mdx:**
Three minimal test pages (item-a.mdx, item-b.mdx, item-c.mdx) with basic content.

**Create test-meta-syntax/(test-group)/item-root/meta.json:**
```json
{
  "$schema": "../../../.source/json-schema/docs.meta.json",
  "title": "Item Root",
  "description": "Testing folder group with root:true",
  "icon": "Folder",
  "root": true
}
```

**Create test-meta-syntax/(test-group)/item-root/index.mdx:**
Minimal MDX file explaining this tests folder group behavior.

**Features tested (basic set):**
- Basic separator `---Label---` (verified, control group)
- Rest operator `...` (verified, control group)
- Exclude operator `!item-c` (needs verification)
- **Folder group `(test-group)` with root:true** (core organizational feature for NAV-02)

**Why this approach:**
- Tests foundational features needed for NAV-02
- Folder groups are critical for navigation restructure
- Basic separators and rest operators provide control group
- Exclude operator is simple boolean behavior
- DO NOT use Code2 icon (doesn't exist in lucide-react)
  </action>
  <verify>
1. Run `cd docs && bun run build`
2. Build succeeds without meta.json parsing errors
3. Navigate to http://localhost:3000/docs/test-meta-syntax in development
4. Inspect rendered navigation to see which features rendered correctly
5. **Verify folder group behavior:** item-root should appear as sibling nav item (same level as item-a, item-b), NOT nested under (test-group)
6. **Verify folder group URL:** Navigate to item-root and confirm URL is /docs/test-meta-syntax/item-root (NOT /docs/test-meta-syntax/(test-group)/item-root)
7. **Verify exclude operator:** item-c should NOT appear in navigation (excluded by `!item-c`)
  </verify>
  <done>
Test section exists with basic syntax features and folder groups, build completes successfully, navigation renders correctly, folder group behavior verified, exclude operator verified.
  </done>
</task>

<task type="auto">
  <name>Task 1b: Test advanced syntax features (icon separators and external links)</name>
  <files>
    docs/content/docs/test-meta-syntax/meta.json
  </files>
  <action>
Update test-meta-syntax/meta.json to add advanced features:

**Update meta.json pages array to include:**
```json
{
  "pages": [
    "---Basic Separator---",
    "index",
    "---[BookOpen]Icon Separator Test---",
    "item-a",
    "external:[Fumadocs Official](https://fumadocs.dev)",
    "...",
    "!item-c",
    "---Folder Groups---",
    "(test-group)"
  ]
}
```

**Features tested (advanced set):**
- Icon separator `---[BookOpen]Label---` (needs verification)
- External link `external:[Text](url)` (needs verification)

**Why separate from Task 1a:**
- Isolates failure diagnosis - if build breaks, we know it's advanced features
- Basic features (Task 1a) provide working baseline
- Can proceed with basic features if advanced features unsupported
  </action>
  <verify>
1. Run `cd docs && bun run build`
2. If build fails, note which feature caused the failure (icon separator or external link)
3. If build succeeds, navigate to http://localhost:3000/docs/test-meta-syntax
4. **Verify icon separator:** Check if BookOpen icon renders in separator between "Basic Separator" and "Icon Separator Test"
5. **Verify external link:** Check if "Fumadocs Official" link appears in navigation and opens https://fumadocs.dev externally
  </verify>
  <done>
Advanced syntax features tested, results documented (either working or unsupported), clear understanding of which features can be used in Plan 02-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document syntax support results including folder group behavior</name>
  <files>
    .planning/phases/02-navigation-restructuring/syntax-support.md
  </files>
  <action>
Analyze build output and rendered navigation to document which syntax features work, with special attention to folder group behavior:

**Test each feature:**

1. **Basic separator** `---Label---`: Expected to work (verified in research)
2. **Icon separator** `---[Icon]Label---`: Check if icon renders in separator
3. **External link** `external:[Text](url)`: Check if link appears in nav and opens externally
4. **Rest operator** `...`: Expected to work (verified in research)
5. **Exclude operator** `!item-c`: Check if item-c is omitted from navigation
6. **Folder group with root:true** `(test-group)` containing `item-root`:
   - Verify item-root appears as sibling in navigation (not nested under folder group)
   - Verify URL is /docs/test-meta-syntax/item-root (no parentheses folder in URL)
   - Verify (test-group) folder name does NOT appear in navigation or URLs

**Create syntax-support.md with structure:**

```markdown
# Fumadocs meta.json Syntax Support Analysis

**Tested:** 2026-01-21
**Fumadocs Version:** 16.4.7 (from package.json)

## Verified Working Features

| Feature | Syntax | Status | Evidence |
|---------|--------|--------|----------|
| Basic separator | `---Label---` | ✓ WORKS | (describe what you saw) |
| Folder groups | `(folder)` with root:true | ✓ WORKS/✗ UNSUPPORTED | (describe navigation behavior and URL structure) |
| ... | ... | ... | ... |

## Unsupported Features

| Feature | Syntax | Status | Workaround |
|---------|--------|--------|------------|
| ... | ... | ✗ UNSUPPORTED | (suggest alternative) |

## Folder Group Behavior Details

**Critical for NAV-02 implementation:**
- Navigation rendering: [Describe how item-root appears in nav tree]
- URL structure: [Confirm /docs/item-root vs /docs/(test-group)/item-root]
- Visual hierarchy: [Describe indentation/nesting behavior]
- Limitations: [Any edge cases or issues discovered]

## Recommendations for Phase 2 Architecture

Based on test results:
- Use [verified features] for navigation structure
- Avoid [unsupported features] - alternatives: [...]
- Folder groups: [RECOMMENDED/NOT RECOMMENDED based on test results]
- [Any other architectural guidance]
```

**Analysis approach:**
- If feature works as expected → "WORKS" with description
- If feature causes build error → "BREAKS BUILD" with error message
- If feature ignored silently → "UNSUPPORTED" with observed behavior
- If feature works differently than expected → "PARTIAL" with details

**Provide clear recommendations:**
For Plan 02-03, we need to know definitively which patterns to use. If folder groups work, confirm they're suitable for organizing guides/workflows/examples. If external links don't work, suggest alternative (separate external-links.mdx page). If icon separators don't work, confirm plain separators are sufficient.
  </action>
  <verify>
1. cat .planning/phases/02-navigation-restructuring/syntax-support.md shows complete analysis with clear WORKS/UNSUPPORTED status for each feature tested
2. **Verify "Folder Group Behavior Details" section exists:** grep -A 5 "Folder Group Behavior Details" .planning/phases/02-navigation-restructuring/syntax-support.md returns content
3. Clear recommendation on whether to use folder groups in Plan 02-03
  </verify>
  <done>
Syntax support document exists with definitive status for all tested features (including folder groups), clear folder group behavior documentation section, and clear recommendations for Plan 02-03 architecture.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove test section and restore clean state</name>
  <files>
    docs/content/docs/test-meta-syntax/
  </files>
  <action>
After documenting syntax support, remove the test section to restore documentation to clean state:

```bash
cd docs/content/docs
rm -rf test-meta-syntax/
```

**Why remove:**
- Test section served its purpose (syntax verification including folder groups)
- Should not appear in production documentation
- Knowledge captured in syntax-support.md for Plan 02-03
- Clean slate for implementing verified architecture

**DO NOT commit the test section** - only commit syntax-support.md documentation.
  </action>
  <verify>
1. ls docs/content/docs/test-meta-syntax should return "No such file or directory"
2. git status shows test-meta-syntax/ not staged
3. Only .planning/phases/02-navigation-restructuring/syntax-support.md is staged
  </verify>
  <done>
Test section removed, syntax-support.md committed, documentation restored to clean state before restructure.
  </done>
</task>

</tasks>

<verification>
- [ ] Test section created with basic syntax features and folder groups
- [ ] Advanced syntax features tested separately
- [ ] Build succeeds (even if some features unsupported)
- [ ] syntax-support.md documents WORKS/UNSUPPORTED status for each feature
- [ ] Folder group behavior explicitly tested and documented with dedicated section
- [ ] Clear recommendations provided for Plan 02-03 architecture
- [ ] Test section removed from codebase
- [ ] Only syntax-support.md committed to git
</verification>

<success_criteria>
Plan 02-03 can proceed with confidence knowing which meta.json features are supported in Fumadocs 16.4.7, including definitive knowledge of folder group behavior. Architecture decisions based on verified capabilities, not assumptions. Zero risk of mid-execution syntax failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-restructuring/02-01-SUMMARY.md` following the summary template with:
- Duration and performance metrics
- List of supported vs unsupported features discovered
- Folder group behavior details (critical for Plan 02-03)
- Architectural guidance for remaining plans
- Any deviations from expected syntax support
</output>
