# Phase 16 Plan 05: Fix Remaining Parameter Error

**Goal:** Fix the remaining parameter syntax error in search.mdx line 724

## Context

Verification identified one remaining parameter error that was missed during the Phase 16-04 accuracy audit:
- **Location:** `docs/content/docs/examples/search.mdx` line 724
- **Error:** Uses `sort="modified+desc"` instead of `sort_by="modified_desc"`
- **Status:** German version already correct, English version needs fix

This is the last remaining gap from verification - all other documentation is accurate and working.

## Tasks

### Task 1: Fix Parameter Syntax in English search.mdx

**What:** Replace incorrect sort parameter with correct syntax
**Where:** `docs/content/docs/examples/search.mdx` line 724
**How:**
1. Read the file to confirm current state
2. Replace `sort="modified+desc"` with `sort_by="modified_desc"`
3. Verify the fix matches implementation and German version

**Acceptance Criteria:**
- Line 724 uses `sort_by` parameter name (not `sort`)
- Value uses underscore format: `modified_desc` (not plus sign)
- Syntax matches German version and other corrected examples

### Task 2: Verify German Version Consistency

**What:** Confirm German version already has correct syntax
**Where:** `docs/content/docs/examples/search.de.mdx` line 725
**How:**
1. Read German file around line 725
2. Verify it uses `sort_by="modified_desc"`
3. Document that German version was already correct from previous fix

**Acceptance Criteria:**
- German version confirmed to use correct syntax
- No changes needed to German file
- Both language versions are now consistent

## Definition of Done

- [ ] English search.mdx line 724 uses `sort_by="modified_desc"`
- [ ] German search.de.mdx confirmed correct (no changes needed)
- [ ] Both versions use identical parameter syntax
- [ ] Change committed with clear message referencing gap closure

## Dependencies

**Requires:**
- Phase 16-04 completion (accuracy audit identified most errors)
- Verification results showing this specific gap

**Provides:**
- 100% accurate code examples across all documentation
- Complete gap closure for Phase 16

## Time Estimate

**2 minutes** - Single line fix with verification

## Technical Notes

**Root Cause:** This error was missed during the Phase 16-04 accuracy audit because:
1. Sed replacements were used for bulk changes
2. This specific occurrence was in a different section (Publisher Exploration)
3. Pattern matching didn't catch this exact variant

**Why German version is already correct:** The German translation was updated after the English sed replacements were refined, so it received the corrected syntax from the start.

**Implementation verification:** The `search_datasets` tool signature in `app/tools/discovery.py` confirms:
- Parameter name is `sort_by` (not `sort`)
- Accepted values use underscore format: `modified_desc`, `relevance_desc`, etc.
