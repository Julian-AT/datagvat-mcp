---
phase: 06-semantic-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/semantic.py, app/tools/discovery.py, tests/test_semantic.py]
autonomous: true

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "User can enter natural language queries and get semantically relevant results"
    - "System expands natural language into effective search terms"
    - "German queries work as well as English queries"
    - "Search gracefully falls back when LLM sampling unavailable"
    - "Semantic results are more relevant than pure keyword matching"
  artifacts:
    - path: "app/semantic.py"
      provides: "Natural language query expansion and semantic search logic"
      min_lines: 100
      exports: ["expand_natural_query", "semantic_search"]
    - path: "app/tools/discovery.py"
      provides: "semantic_search_datasets MCP tool registration"
      contains: "semantic_search_datasets"
    - path: "tests/test_semantic.py"
      provides: "Comprehensive test coverage for semantic search"
      min_lines: 200
  key_links:
    - from: "app/tools/discovery.py"
      to: "app/semantic.py"
      via: "imports and calls expand_natural_query"
      pattern: "from app.semantic import.*expand_natural_query"
    - from: "app/semantic.py"
      to: "FastMCP Context.sample"
      via: "LLM query expansion"
      pattern: "ctx\\.sample\\("
    - from: "semantic_search_datasets"
      to: "existing search_datasets"
      via: "fallback mechanism"
      pattern: "search_datasets\\("
---

<objective>
Implement semantic search using FastMCP Context.sample() for natural language query understanding and expansion.

Purpose: Enable users to search with natural language (e.g., "health data from Vienna") and get semantically relevant results through LLM-powered query expansion.
Output: Working semantic search with multilingual support and graceful fallback.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Technology context
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md

# Existing search implementation to understand patterns
@app/tools/discovery.py
@app/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create semantic query expansion service</name>
  <files>app/semantic.py</files>
  <action>
    Create semantic.py with:
    1. expand_natural_query(ctx: Context, query: str, language: str = "auto") -> dict
       - Use FastMCP Context.sample() to expand natural language to search terms
       - Support English and German with language-specific prompts
       - Auto-detect language using simple heuristics (German articles: der/die/das, von, für, etc.)
       - Return expanded query with themes, keywords, and structured filters
       - Handle sampling failures gracefully (return original query structure)
    2. semantic_search(ctx: Context, query: str, **kwargs) -> dict
       - Wrapper that calls expand_natural_query then search_datasets_advanced
       - Merge semantic expansion with any explicit filters provided
       - Fall back to direct search if expansion fails
    Use existing client patterns and error handling from discovery.py. Include comprehensive docstrings and type hints.
  </action>
  <verify>python -m pytest tests/test_semantic.py -k "test_expand" --verbose</verify>
  <done>Functions created, typed, documented, and passing expansion tests</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic_search_datasets MCP tool</name>
  <files>app/tools/discovery.py</files>
  <action>
    Add semantic_search_datasets tool function to discovery.py:
    1. Import semantic functions: from app.semantic import expand_natural_query, semantic_search
    2. Register new tool with @mcp.tool decorator
    3. Tool accepts natural_query parameter and optional filters (themes, formats, etc.)
    4. Description emphasizes natural language capability ("Ask in plain English or German")
    5. Tool calls semantic_search function, handles errors with ToolError
    6. Progress reporting during LLM expansion ("Expanding natural language query...")
    7. Return same structure as search_datasets but with expansion_info showing original vs expanded query
    Follow existing tool patterns from discovery.py for consistency.
  </action>
  <verify>python -c "from app.tools.discovery import mcp; print([t for t in mcp._tools if 'semantic' in t])"</verify>
  <done>semantic_search_datasets tool registered and callable via MCP</done>
</task>

<task type="auto">
  <name>Task 3: Create comprehensive test suite</name>
  <files>tests/test_semantic.py</files>
  <action>
    Create test_semantic.py with comprehensive coverage:
    1. Test expand_natural_query:
       - English queries: "health datasets from Vienna" -> themes, keywords
       - German queries: "Gesundheitsdaten aus Wien" -> themes, keywords
       - Language auto-detection accuracy
       - LLM sampling failure handling (mock Context.sample to raise exception)
       - Empty/invalid input handling
    2. Test semantic_search integration:
       - End-to-end with mocked client responses
       - Fallback to direct search when expansion fails
       - Filter merging (explicit + semantic expansion)
    3. Test semantic_search_datasets tool:
       - Mock FastMCP tool execution
       - Progress reporting verification
       - Error handling paths
    Use pytest-asyncio, pytest-mock patterns from existing tests. Achieve >90% coverage.
  </action>
  <verify>python -m pytest tests/test_semantic.py --verbose --cov=app.semantic --cov-report=term-missing</verify>
  <done>Test suite comprehensive, all passing, >90% coverage achieved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m pytest tests/test_semantic.py passes completely
- [ ] python -c "from app.semantic import expand_natural_query, semantic_search" succeeds
- [ ] python -c "from app.tools.discovery import mcp; print('semantic_search_datasets' in [t.name for t in mcp._tools.values()])" returns True
- [ ] No import errors or type checking issues with existing code
</verification>

<success_criteria>
- All tasks completed successfully
- Semantic search functions work with natural language input
- FastMCP Context.sample() integration functional
- Multilingual support (English/German) operational
- Graceful fallback when LLM sampling unavailable
- MCP tool registered and accessible
- Comprehensive test coverage (>90%) achieved
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-semantic-search/06-01-SUMMARY.md`
</output>