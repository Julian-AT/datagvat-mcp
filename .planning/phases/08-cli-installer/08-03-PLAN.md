---
phase: 08-cli-installer
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - packages/cli/src/commands/init.ts
  - packages/cli/src/messages.ts
  - packages/cli/README.md
autonomous: false

must_haves:
  truths:
    - "User sees clear post-install instructions after configuration"
    - "Instructions are tool-specific (restart Claude Desktop vs reload VS Code)"
    - "User knows what example queries to try"
    - "CLI can be installed via npx @datagvat/mcp-installer"
  artifacts:
    - path: "packages/cli/src/messages.ts"
      provides: "Post-install guidance templates"
      exports: ["getRestartInstructions", "getExampleQueries"]
    - path: "packages/cli/README.md"
      provides: "Installation and usage documentation"
      min_lines: 50
  key_links:
    - from: "packages/cli/src/commands/init.ts"
      to: "packages/cli/src/messages.ts"
      via: "post-install message display"
      pattern: "getRestartInstructions|getExampleQueries"
---

<objective>
Add post-install guidance and test the complete installation flow end-to-end before publishing to npm.

Purpose: Ensure users know how to restart tools and verify successful installation
Output: Production-ready CLI package with clear post-install messages and tested npx execution
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cli-installer/08-CONTEXT.md

# Prior plans - complete CLI implementation
@.planning/phases/08-cli-installer/08-01-PLAN.md
@.planning/phases/08-cli-installer/08-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Add post-install messages and example queries</name>
  <files>
    packages/cli/src/messages.ts
    packages/cli/src/commands/init.ts
  </files>
  <action>
    Create post-install guidance system:

    **src/messages.ts:**
    - Import chalk for formatting
    - Import ToolName type from './types.ts'

    - Export function getRestartInstructions(tool: ToolName): string
      - Claude Desktop: "Quit and restart Claude Desktop app (Cmd+Q on macOS, Alt+F4 on Windows)"
      - Continue: "Reload VS Code window (Cmd+Shift+P -> 'Developer: Reload Window')"
      - Cline: "Reload VS Code window (Cmd+Shift+P -> 'Developer: Reload Window')"
      - Include tool-specific details (Claude Desktop needs full restart, VS Code extensions need window reload)

    - Export function getExampleQueries(): string[]
      - Return array of example queries:
        - "Find datasets about Vienna population"
        - "Show me datasets with quality score above 80"
        - "What health-related datasets are available?"
        - "Search for datasets about air quality in Austria"

    - Export function displayPostInstall(tools: ToolName[]): void
      - Display formatted success message
      - For each tool: Show restart instructions
      - Show "Try these queries:" section with examples
      - Show documentation link: https://datagvat-mcp-docs.vercel.app (or actual docs URL)
      - Use chalk.bold for headers, chalk.cyan for tool names, chalk.gray for examples

    **src/commands/init.ts (update):**
    - Import displayPostInstall from '../messages.ts'
    - After configureTools() completes successfully:
      - Call displayPostInstall(configuredTools)
      - Display tool-specific restart instructions
      - Show example queries
      - Show documentation link

    Professional formatting with clear visual hierarchy, no emoji (Phase 5 decision)
  </action>
  <verify>
    bun src/index.ts init --yes (check post-install messages appear)
    Verify restart instructions are tool-specific
    Verify example queries are displayed
    Verify documentation link is shown
  </verify>
  <done>
    Post-install messages display after successful configuration
    Restart instructions are tool-specific and actionable
    Example queries guide users on first steps
    Documentation link provides further guidance
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete CLI installer package with tool detection, interactive prompts, configuration writers, and post-install guidance
  </what-built>
  <how-to-verify>
    Test the complete installation flow:

    1. **Local testing with bun link:**
       ```bash
       cd packages/cli
       bun link
       datagvat-mcp init
       ```
       Expected: CLI runs, detects tools, shows checkbox, configures selected tools

    2. **Test npx execution (local):**
       ```bash
       cd packages/cli
       bun run build
       npx . init
       ```
       Expected: Works via npx without global install

    3. **Verify configuration:**
       - Check Claude Desktop config: Open config file and verify 'datagvat' entry exists
       - macOS: ~/Library/Application Support/Claude/claude_desktop_config.json
       - Windows: %APPDATA%\Claude\claude_desktop_config.json
       - Linux: ~/.config/Claude/claude_desktop_config.json

    4. **Test flags:**
       - `datagvat-mcp init --yes` (skips prompts, configures all)
       - `datagvat-mcp init --tool claude-desktop` (specific tool)
       - `datagvat-mcp add continue` (alias command)

    5. **Test error cases:**
       - No tools detected (rename config temporarily)
       - Already configured (run init twice)
       - Invalid JSON in config (manually corrupt config)

    6. **Verify post-install messages:**
       - Restart instructions shown
       - Example queries displayed
       - Documentation link present

    **Success criteria:**
    - All commands execute without errors
    - Config files updated correctly
    - Existing configs preserved (merge, not replace)
    - Post-install messages clear and actionable
    - Error cases handled gracefully
  </how-to-verify>
  <resume-signal>
    Type "approved" to proceed with npm publishing preparation, or describe any issues found
  </resume-signal>
</task>

<task type="auto">
  <name>Create README and prepare for npm publishing</name>
  <files>
    packages/cli/README.md
    packages/cli/.npmignore
    packages/cli/package.json
  </files>
  <action>
    Document the CLI and prepare for npm publishing:

    **README.md:**
    - Title: "data.gv.at MCP Installer"
    - Description: "shadcn-like one-command installer for data.gv.at MCP Server"
    - Installation: `npx @datagvat/mcp-installer init`
    - Features: Tool detection, interactive prompts, automatic configuration
    - Supported tools: Claude Desktop, Continue, Cline
    - Commands:
      - `init` - Interactive installation
      - `init --yes` - Skip prompts, configure all detected tools
      - `init --tool <name>` - Configure specific tool
      - `add <tool>` - Alias for `init --tool`
    - Platform support: macOS, Windows, Linux
    - Example output (screenshot or code block)
    - Troubleshooting section:
      - No tools detected: Install Claude Desktop/Continue/Cline first
      - Already configured: Skip warning is expected
      - Permission denied: Run with appropriate permissions
    - Links to main documentation
    - License: MIT

    **.npmignore:**
    - src/**/*.test.ts
    - src/**/*.spec.ts
    - tsconfig.json
    - bunfig.toml
    - *.log

    **package.json (update):**
    - files: ["dist/**/*", "src/**/*", "README.md", "LICENSE"]
    - main: "dist/index.js"
    - types: "dist/index.d.ts"
    - repository: { type: "git", url: "https://github.com/username/datagvat-mcp.git", directory: "packages/cli" }
    - publishConfig: { access: "public" }
    - Add LICENSE file reference

    Follow npm publishing best practices (files array, publishConfig, repository)
  </action>
  <verify>
    cat packages/cli/README.md (check documentation complete)
    bun run build && ls dist/ (verify compiled output)
    npm pack (dry-run, check package contents)
  </verify>
  <done>
    README.md provides complete installation and usage documentation
    .npmignore excludes test files and dev configs
    package.json configured for public npm publishing
    Package can be published to npm (ready for `npm publish`)
  </done>
</task>

</tasks>

<verification>
1. Post-install messages display tool-specific restart instructions
2. Example queries guide users on first steps
3. Complete installation flow tested end-to-end with bun link
4. Config files updated correctly (verified manually)
5. Error cases handled gracefully
6. README.md provides comprehensive documentation
7. Package ready for npm publishing (npm pack succeeds)
</verification>

<success_criteria>
- Post-install guidance shown after successful configuration
- Complete CLI tested end-to-end via bun link and npx
- Config files verified to contain correct 'datagvat' entry
- Error cases tested and handled gracefully
- README.md provides clear installation and troubleshooting guidance
- Package ready for npm publishing with proper metadata
- Phase 8 complete: shadcn-like one-command installer working
</success_criteria>

<output>
After completion, create `.planning/phases/08-cli-installer/08-03-SUMMARY.md`

**Note:** Actual npm publishing (`npm publish`) is deferred to user - CLI is ready but publishing requires npm account credentials and registry configuration.
</output>
