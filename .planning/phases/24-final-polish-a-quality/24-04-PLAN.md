---
phase: 24-final-polish-a-quality
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/scripts/verify-requirements.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TypeScript compilation passes with zero errors"
    - "verify-requirements.ts script executes successfully"
    - "Production build completes without TypeScript errors"
  artifacts:
    - path: "docs/scripts/verify-requirements.ts"
      provides: "Requirements verification script"
      min_lines: 250
      exports: []
  key_links:
    - from: "docs/scripts/verify-requirements.ts"
      to: "Phase 23 artifacts"
      via: "artifact.pattern property check"
      pattern: "artifact\\.pattern"
---

<objective>
Fix TypeScript compilation errors in verify-requirements.ts to enable production builds.

**Purpose:** Phase 24 verification failed because verify-requirements.ts has type errors at lines 247-250, preventing production builds from completing.

**Output:** Clean TypeScript compilation and successful script execution.
</objective>

<execution_context>
@C:\GitHub\datagvat-mcp\.planning\get-shit-done\workflows\execute-plan.md
@C:\GitHub\datagvat-mcp\.planning\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\STATE.md
@C:\GitHub\datagvat-mcp\.planning\phases\24-final-polish-a-quality\24-VERIFICATION.md
@C:\GitHub\datagvat-mcp\docs\scripts\verify-requirements.ts
</context>

<tasks>

<task type="auto">
  <name>Fix TypeScript type errors in verify-requirements.ts</name>
  <files>docs/scripts/verify-requirements.ts</files>
  <action>
Fix the TypeScript type errors at lines 247-250 caused by accessing optional `pattern` property.

**Root cause:** The `phase23Artifacts` object has mixed types - some artifacts have `pattern` property, others don't. TypeScript correctly flags `artifact.pattern` access as unsafe.

**Fix approach:**
1. Read verify-requirements.ts lines 240-260
2. Add type guard or optional chaining for pattern property access
3. Replace `artifact.pattern` with `'pattern' in artifact ? artifact.pattern : undefined`
4. Replace `artifact.pattern` usage in line 249-250 with safe access

**Example fix:**
```typescript
// Line 247: Check if artifact has pattern property
if (id === 'BEST-01' && exists && 'pattern' in artifact && artifact.pattern) {
  const content = readFileSync(join(__dirname, '..', artifact.path), 'utf-8');
  const hasPattern = content.toLowerCase().includes(artifact.pattern);
  evidence += ` (${hasPattern ? 'contains' : 'missing'} ${artifact.pattern} content)`;
}
```

This uses TypeScript's type narrowing - the `'pattern' in artifact` check narrows the type so `artifact.pattern` is safe to access.

**Why this approach:** Minimal change, leverages TypeScript's type system, preserves original logic.
  </action>
  <verify>
Run TypeScript compilation:
```bash
cd docs && npx tsc --noEmit scripts/verify-requirements.ts
```

Expected output: No errors (only @types/mdx warnings which are not blockers).

Also verify script executes:
```bash
cd docs && npx tsx scripts/verify-requirements.ts
```

Expected: Script runs and produces requirements-verification.md without errors.
  </verify>
  <done>
- TypeScript compilation passes for verify-requirements.ts
- Script executes successfully and produces output
- Lines 247-250 use safe property access with type guards
  </done>
</task>

<task type="auto">
  <name>Verify production build completes</name>
  <files>N/A</files>
  <action>
Verify that the TypeScript fix allows production build to complete successfully.

Run full production build:
```bash
cd docs && npm run build
```

Expected outcomes:
- Build completes without TypeScript errors
- Static pages generated successfully (481 pages expected)
- Only expected warnings (CRLF, peer dependencies) allowed
- No errors in verify-requirements.ts

**If build fails:** Investigate additional TypeScript errors and fix them using same pattern (type guards, optional chaining).
  </action>
  <verify>
```bash
cd docs && npm run build 2>&1 | tee build-output.txt
echo "Exit code: $?"
```

Success criteria:
- Exit code 0
- "Compiled successfully" or similar success message
- No TypeScript errors in output
- Static pages generated
  </verify>
  <done>
- Production build completes with exit code 0
- No TypeScript compilation errors
- 481+ static pages generated successfully
- verify-requirements.ts no longer blocks build
  </done>
</task>

</tasks>

<verification>
**Overall phase checks:**

1. TypeScript compilation clean:
```bash
cd docs && npx tsc --noEmit scripts/verify-requirements.ts
```
Expected: Zero errors (excluding @types/mdx namespace warnings)

2. Script executes successfully:
```bash
cd docs && npx tsx scripts/verify-requirements.ts
```
Expected: Generates requirements-verification.md with artifact checks

3. Production build succeeds:
```bash
cd docs && npm run build
```
Expected: Completes with 481+ pages, zero TypeScript errors

**Gap closure verification:**
- Gap truth "Production build completes with zero errors" now achievable
- verify-requirements.ts no longer blocking production deployment
</verification>

<success_criteria>
**This gap closure plan succeeds when:**

1. TypeScript compilation of verify-requirements.ts passes with zero errors
2. verify-requirements.ts script executes without runtime errors
3. Production build (npm run build) completes successfully
4. All 3 type errors (lines 247, 249, 250) resolved
5. No new TypeScript errors introduced

**Measurable outcomes:**
- `npx tsc --noEmit scripts/verify-requirements.ts` exits with code 0
- `npm run build` exits with code 0
- Git diff shows only targeted changes to lines 247-251
</success_criteria>

<output>
After completion, create `.planning/phases/24-final-polish-a-quality/24-04-SUMMARY.md` with:
- TypeScript errors fixed (line-level changes)
- Build verification results
- Gap closure confirmation
</output>
