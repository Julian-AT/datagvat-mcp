---
phase: 01-infrastructure-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/bunfig.toml
  - docs/biome.json
  - docs/package.json
autonomous: true

must_haves:
  truths:
    - Developer can install dependencies using "bun install" command
    - Code quality checks run via Biome with user-specified rules
    - Bun configuration enables proper install caching and test coverage
    - Biome respects .gitignore via VCS integration
  artifacts:
    - path: "docs/bunfig.toml"
      provides: "Bun runtime configuration"
      min_lines: 8
    - path: "docs/biome.json"
      provides: "Biome linting and formatting rules"
      contains: "formatWithErrors"
    - path: "docs/package.json"
      provides: "Updated scripts using Bun commands"
      contains: "bun run"
  key_links:
    - from: "docs/biome.json"
      to: ".gitignore"
      via: "vcs.useIgnoreFile setting"
      pattern: "useIgnoreFile.*true"
    - from: "docs/package.json"
      to: "docs/bunfig.toml"
      via: "Scripts execute with Bun runtime"
      pattern: "bun run"
---

<objective>
Establish Bun as the primary runtime and package manager, replacing Node.js/npm. Configure Biome with strict linting rules and VCS integration for consistent code quality enforcement.

Purpose: Modernize build tooling foundation with faster runtime (3-5x install speed) and unified linting/formatting (100x faster than ESLint+Prettier).

Output: Complete Bun and Biome configuration with updated package.json scripts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v2.0-ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-modernization/01-CONTEXT.md
@.planning/phases/01-infrastructure-modernization/01-RESEARCH.md
@C:/GitHub/datagvat-mcp/docs/package.json
@C:/GitHub/datagvat-mcp/docs/biome.json
</context>

<tasks>

<task type="auto">
  <name>Create Bun Configuration</name>
  <files>docs/bunfig.toml</files>
  <action>
Create bunfig.toml in docs/ directory with configuration from CONTEXT.md:

```toml
[install]
cache = "~/.bun/install/cache"

[test]
coverage = true

[run]
shell = "bash"
```

This configures:
- Install cache directory for faster subsequent installs
- Test coverage enabled for future test execution
- Bash shell for cross-platform script compatibility

Update docs/package.json scripts to use Bun commands:
- Replace any "npm run" with "bun run"
- Replace "node scripts/" with "bun scripts/"
- Replace "npx tsx" with "bun" (Bun runs TypeScript natively)
- Keep Next.js commands as-is ("next dev", "next build" - Bun executes these)

DO NOT change dependency versions. DO NOT run "bun install" yet (no bun.lockb exists).
  </action>
  <verify>
1. Confirm bunfig.toml exists in docs/ directory
2. Check package.json scripts no longer reference npm or npx tsx
3. Verify all custom scripts use "bun run" or "bun scripts/"
  </verify>
  <done>
- bunfig.toml created with install cache, test coverage, bash shell settings
- package.json scripts migrated to Bun commands (no npm/npx references)
- Next.js commands remain unchanged (next dev, next build, next start)
  </done>
</task>

<task type="auto">
  <name>Update Biome Configuration</name>
  <files>docs/biome.json</files>
  <action>
Update docs/biome.json to match user specifications from CONTEXT.md.

Current config has:
- indentStyle: "tab"
- quoteStyle: "double"
- Basic recommended rules only

Update to:
```json
{
  "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "ignoreUnknown": false
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100,
    "formatWithErrors": false
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true,
      "style": {
        "noNegationElse": "error",
        "useBlockStatements": "warn",
        "useCollapsedElseIf": "error",
        "useShorthandArrayType": "error"
      },
      "complexity": {
        "noExtraBooleanCast": "error",
        "noUselessFragments": "error",
        "useFlatMap": "error"
      },
      "suspicious": {
        "noConsoleLog": "warn",
        "noDebugger": "error"
      }
    }
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "semicolons": "always"
    }
  },
  "assist": {
    "enabled": true,
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  }
}
```

Key changes:
- VCS integration: useIgnoreFile respects .gitignore (prevents linting node_modules, .next)
- Formatter: space indent (2 width), 100 char line width, formatWithErrors: false (fail fast)
- Linter: specific style/complexity/suspicious rules per user specs
- JavaScript: single quotes, always semicolons (user spec)
- Schema version 1.9.4 (matches Biome 2.3.11)

After updating, format existing config with new rules:
```bash
bun biome format --write docs/biome.json
```
  </action>
  <verify>
1. Run `bun biome check docs/biome.json` to validate configuration syntax
2. Verify VCS settings present: enabled: true, clientKind: "git", useIgnoreFile: true
3. Confirm formatWithErrors is false (fail-fast on errors)
4. Check style rules include noNegationElse, useBlockStatements, useCollapsedElseIf, useShorthandArrayType
5. Check suspicious rules include noConsoleLog (warn), noDebugger (error)
  </verify>
  <done>
- biome.json updated with VCS integration, 100-char line width, formatWithErrors: false
- Style/complexity/suspicious rules configured per user specifications
- JavaScript formatter uses single quotes and always semicolons
- Configuration validates successfully with `biome check`
  </done>
</task>

</tasks>

<verification>
**Configuration files exist and are valid:**
- bunfig.toml in docs/ with install/test/run sections
- biome.json with $schema, vcs, formatter, linter, javascript sections
- package.json scripts use "bun run" commands

**Biome configuration matches specs:**
- VCS useIgnoreFile: true
- formatWithErrors: false
- lineWidth: 100
- All user-specified rules present (noNegationElse, noConsoleLog, etc.)
- Single quotes, always semicolons

**Scripts migrated to Bun:**
- No "npm run" commands in package.json
- No "npx tsx" commands in package.json
- "bun" or "bun run" used for TypeScript scripts
</verification>

<success_criteria>
1. Developer can run `bun install` in docs/ directory (after plan completes)
2. Developer can run `bun biome check .` to validate code quality
3. Biome respects .gitignore (no errors about node_modules or .next)
4. Package.json scripts ready for Bun execution (no Node.js dependencies)
5. Configuration files committed and ready for Wave 2 script creation
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-modernization/01-01-SUMMARY.md`
</output>
