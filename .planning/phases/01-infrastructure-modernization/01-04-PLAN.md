---
phase: 01-infrastructure-modernization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [docs/scripts/validate-links.ts]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "validate-links.ts validates MDX component href attributes (Card, Callout, Tabs)"
    - "Link validation catches broken links in MDX component attributes"
    - "CI pipeline validates all internal links including component attributes"
  artifacts:
    - path: "docs/scripts/validate-links.ts"
      provides: "MDX component attribute validation"
      min_lines: 35
      contains: "markdown:"
  key_links:
    - from: "docs/scripts/validate-links.ts"
      to: "validateFiles"
      via: "markdown config option"
      pattern: "markdown:\\s*\\{"
---

<objective>
Close UAT gap: Add MDX component attribute validation to validate-links.ts

Purpose: Ensure link validation covers custom MDX component href attributes (Card, Callout, Tabs), not just standard HTML/markdown links
Output: Updated validate-links.ts with markdown config option enabling component attribute validation
</objective>

<execution_context>
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\workflows\execute-plan.md
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\milestones\v2.0-ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\STATE.md
@C:\GitHub\datagvat-mcp\.planning\phases\01-infrastructure-modernization\01-02-SUMMARY.md
@C:\GitHub\datagvat-mcp\.planning\phases\01-infrastructure-modernization\01-UAT.md

## Gap Source

From 01-UAT.md test 4:

**Truth:** validate-links.ts validates MDX component href attributes (Card, Callout, Tabs)
**Status:** failed
**Reason:** User reported: Missing component validation for Card, Callout, Tabs in markdown config
**Root Cause:** validate-links.ts uses next-validate-link with default config, only validates standard HTML/markdown links, missing markdown option to specify custom MDX component attributes
**Fix:** Add markdown config to validateFiles() with component attribute mappings

## Current Implementation

File: docs/scripts/validate-links.ts

```typescript
const results = await validateFiles(files, {
  scanned,
  checkRelativePaths: 'as-url',
});
```

**Missing:** markdown config option with component attribute mappings
</context>

<tasks>

<task type="auto">
  <name>Add MDX Component Attribute Validation</name>
  <files>docs/scripts/validate-links.ts</files>
  <action>
Add `markdown` config option to validateFiles() call in docs/scripts/validate-links.ts:

**Current code (lines 16-19):**
```typescript
const results = await validateFiles(files, {
  scanned,
  checkRelativePaths: 'as-url',
});
```

**Update to:**
```typescript
const results = await validateFiles(files, {
  scanned,
  checkRelativePaths: 'as-url',
  markdown: {
    Card: ['href'],
    Callout: ['href'],
    'Tabs.Tab': ['href'],
  },
});
```

**Why this works:**
- next-validate-link's `markdown` option specifies custom MDX component attributes to validate
- Maps component names to array of attribute names containing links
- Card, Callout, and Tabs.Tab are Fumadocs components used in documentation
- Each component has an optional `href` attribute that should be validated as an internal link

**Testing:**
After updating, the script will validate href attributes in:
- `<Card href="/guides/search">...</Card>`
- `<Callout href="/api/reference">...</Callout>`
- `<Tabs.Tab href="/workflows/basic">...</Tabs.Tab>`

Previous behavior: Only validated standard markdown links and HTML `<a>` tags
New behavior: Also validates MDX component href attributes
  </action>
  <verify>
1. Read updated file to confirm markdown config added
2. Run link validation: `cd docs && bun run lint:links`
3. Verify output shows "All links validated successfully" with component attribute checking enabled
4. Check that validation now catches broken links in MDX component href attributes
  </verify>
  <done>
- docs/scripts/validate-links.ts contains markdown config with Card, Callout, Tabs.Tab mappings
- bun run lint:links successfully validates component attributes
- UAT truth "validate-links.ts validates MDX component href attributes" is achievable
  </done>
</task>

</tasks>

<verification>
## Manual Verification

After implementation, verify:

1. **Script executes without errors:**
   ```bash
   cd docs
   bun run lint:links
   ```
   Expected: "✓ All links validated successfully"

2. **Component attributes validated:**
   Test by temporarily adding a broken link in an MDX component:
   ```bash
   # In any .mdx file, add:
   <Card href="/broken-link">Test</Card>

   # Run validation:
   bun run lint:links

   # Expected: Error reported for /broken-link
   # Revert test change
   ```

3. **CI integration works:**
   - Validation runs in prebuild script
   - CI catches broken component links
</verification>

<success_criteria>
- [x] UAT Test 4 status changes from "issue" to "pass"
- [x] validate-links.ts contains markdown config option
- [x] Component attributes (Card, Callout, Tabs.Tab href) are validated
- [x] bun run lint:links validates all internal links including MDX components
- [x] CI pipeline validates component attributes via prebuild → lint:links
- [x] No regression in existing link validation functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-modernization/01-04-SUMMARY.md`

Summary should include:
- Gap closed (UAT Test 4)
- markdown config added to validateFiles()
- Component attribute mappings (Card, Callout, Tabs.Tab)
- Verification results
- Duration and metrics
</output>
