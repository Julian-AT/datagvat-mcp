---
phase: 01-infrastructure-modernization
plan: 05
type: execute
wave: 5
depends_on: ["01", "02", "03", "04"]
files_modified: [".git/hooks/pre-commit"]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Bun runtime is installed and functional"
    - "Pre-commit hooks are installed and prevent broken commits"
    - "All Bun-based scripts execute successfully"
  artifacts:
    - path: ".git/hooks/pre-commit"
      provides: "Git pre-commit hook that runs validation"
      min_lines: 1
      contains: "simple-git-hooks"
  key_links:
    - from: ".git/hooks/pre-commit"
      to: "docs/package.json"
      via: "simple-git-hooks config"
      pattern: "cd docs && bun run validate"
---

<objective>
Close operational gaps: Install pre-commit hooks and verify Bun runtime functionality

Purpose: Complete Phase 1 by ensuring the development environment is fully operational with hooks installed and Bun commands working
Output: Functional pre-commit hooks, verified Bun installation, all validation scripts executing correctly
</objective>

<execution_context>
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\workflows\execute-plan.md
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\milestones\v2.0-ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\STATE.md
@C:\GitHub\datagvat-mcp\.planning\phases\01-infrastructure-modernization\01-VERIFICATION.md

## Gap Source

From 01-VERIFICATION.md:

**Gap 1: Pre-commit Hooks Not Installed**
- Status: Configuration correct but .git/hooks/pre-commit missing
- Fix: Execute `cd docs && bun run prepare`
- Impact: Local validation not enforced

**Gap 2: Bun Runtime Verification**
- Status: Configuration correct but need to verify Bun commands work
- Fix: Verify `bun install`, `bun run validate`, `bun run build`
- Impact: Cannot confirm runtime behavior

## Current State

All configuration files exist and are correct:
- docs/package.json has prepare script
- docs/package.json has simple-git-hooks config
- docs/bunfig.toml exists
- All scripts use Bun runtime

Missing: Execution of prepare script, verification of Bun functionality
</context>

<tasks>

<task type="auto">
  <name>Verify Bun Installation</name>
  <files>None (verification only)</files>
  <action>
Verify Bun is installed and accessible:

```bash
bun --version
```

Expected output: Version number (e.g., "1.1.38" or similar)

If Bun not found, user must install it first:
- macOS/Linux: `curl -fsSL https://bun.sh/install | bash`
- Windows: Download from bun.sh

**Why this matters:**
- All package.json scripts require Bun runtime
- prebuild/postbuild scripts import from 'bun' module
- CI uses Bun for validation and build

If Bun is installed, proceed to next task.
If not installed, stop and inform user.
  </action>
  <verify>
1. Run `bun --version` and confirm version displays
2. Check that bun is in PATH
  </verify>
  <done>
- Bun --version command succeeds
- Bun runtime is functional
  </done>
</task>

<task type="auto">
  <name>Install Pre-commit Hooks</name>
  <files>.git/hooks/pre-commit</files>
  <action>
Execute the prepare script to install pre-commit hooks:

```bash
cd docs && bun run prepare
```

This runs simple-git-hooks which:
1. Reads pre-commit config from package.json
2. Creates .git/hooks/pre-commit file
3. Makes it executable
4. Hook will run `cd docs && bun run validate` before each commit

**Verification:**
```bash
ls -la .git/hooks/pre-commit
cat .git/hooks/pre-commit
```

Expected:
- File exists at .git/hooks/pre-commit
- File is executable (-rwxr-xr-x)
- Contains reference to `cd docs && bun run validate`

**Why this matters:**
- Prevents committing code that fails validation
- Enforces Biome linting before commit
- Catches broken links before commit
- Ensures type safety before commit
  </action>
  <verify>
1. Confirm .git/hooks/pre-commit exists
2. Verify hook contains validation command
3. Check hook is executable
  </verify>
  <done>
- .git/hooks/pre-commit file exists
- Hook contains `cd docs && bun run validate`
- Hook is executable
  </done>
</task>

<task type="auto">
  <name>Verify Bun Scripts Execute</name>
  <files>None (verification only)</files>
  <action>
Verify all key Bun scripts execute successfully:

**1. Install dependencies:**
```bash
cd docs && bun install
```

Expected: Dependencies install without errors

**2. Run validation:**
```bash
cd docs && bun run validate
```

This runs the validation pipeline:
- Biome linting and formatting checks
- Link validation (including MDX components)
- TypeScript type checking

Expected: All checks pass (or show specific errors if code has issues)

**3. Run build:**
```bash
cd docs && bun run build
```

This runs:
- prebuild (validation)
- Next.js build
- postbuild (verification)

Expected: Build completes successfully

**Why this matters:**
- Confirms Bun runtime is fully functional
- Validates all scripts work in the environment
- Proves the development workflow is operational
  </action>
  <verify>
1. bun install completes without errors
2. bun run validate executes all checks
3. bun run build completes full pipeline
4. All commands show expected output
  </verify>
  <done>
- All three Bun commands execute successfully
- No runtime errors or missing dependencies
- Development workflow is fully operational
  </done>
</task>

</tasks>

<verification>
## Manual Verification

After implementation, verify:

1. **Pre-commit hook works:**
   ```bash
   # Create intentional error
   echo "console.log('test')" >> docs/scripts/prebuild.ts

   # Try to commit
   git add docs/scripts/prebuild.ts
   git commit -m "test"

   # Expected: Commit blocked with Biome error

   # Revert
   git checkout docs/scripts/prebuild.ts
   ```

2. **All validation scripts work:**
   ```bash
   cd docs
   bun run validate
   # Expected: Biome, links, types all checked
   ```

3. **Build pipeline works:**
   ```bash
   cd docs
   bun run build
   # Expected: Full build completes
   ```

All three verifications should pass without errors.
</verification>

<success_criteria>
- [x] Bun runtime verified (bun --version works)
- [x] Pre-commit hooks installed (.git/hooks/pre-commit exists)
- [x] Hook is executable and contains validation command
- [x] bun install works in docs/ directory
- [x] bun run validate executes all checks
- [x] bun run build completes full pipeline
- [x] Phase 1 operational gaps closed
- [x] Development environment fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-modernization/01-05-SUMMARY.md`

Summary should include:
- Bun installation verification
- Pre-commit hook installation results
- Script execution verification
- Any issues encountered
- Duration and metrics
</output>
