---
phase: 07-openapi-integration
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - docs/lib/openapi.ts
  - docs/lib/source.tsx
  - docs/content/docs/api-reference/meta.json
  - docs/content/docs/meta.json
autonomous: true

must_haves:
  truths:
    - "OpenAPI configuration points to data.gv.at schema"
    - "API reference pages appear in navigation"
    - "API documentation renders without errors"
    - "Build succeeds with auto-generated API docs"
  artifacts:
    - path: "docs/lib/openapi.ts"
      provides: "OpenAPI server instance pointing to data.gv.at schema"
      contains: "data.gv.at-openapi.yaml"
    - path: "docs/lib/source.tsx"
      provides: "Loader with OpenAPI virtual pages"
      contains: "openapiSource"
    - path: "docs/content/docs/api-reference/meta.json"
      provides: "Navigation metadata for API reference section"
      min_lines: 5
  key_links:
    - from: "docs/lib/openapi.ts"
      to: "docs/data.gv.at-openapi.yaml"
      via: "createOpenAPI input"
      pattern: "data\\.gv\\.at-openapi\\.yaml"
    - from: "docs/lib/source.tsx"
      to: "docs/lib/openapi.ts"
      via: "import"
      pattern: "import.*openapi.*from.*lib/openapi"
    - from: "docs/lib/source.tsx"
      to: "openapiSource"
      via: "loader multiple"
      pattern: "openapiSource\\(openapi"
---

<objective>
Configure Fumadocs to generate API reference documentation from data.gv.at OpenAPI schema.

Purpose: Replace placeholder scalar.yaml with real data.gv.at schema, configure navigation structure, and verify that auto-generated API documentation renders correctly in the documentation site.

Output: Working API reference section with auto-generated pages from OpenAPI schema, integrated into main navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase context
@.planning/phases/07-openapi-integration/07-RESEARCH.md
@.planning/phases/07-openapi-integration/07-01-SUMMARY.md

# Existing OpenAPI setup
@docs/lib/openapi.ts
@docs/lib/source.tsx
@docs/components/api-page.tsx

# Navigation structure (for API reference placement)
@docs/content/docs/meta.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OpenAPI configuration to use data.gv.at schema</name>
  <files>docs/lib/openapi.ts</files>
  <action>
Update docs/lib/openapi.ts to reference the downloaded schema:

1. **Change input path:**
   - Replace: `path.resolve('./scalar.yaml')`
   - With: `path.resolve('./data.gv.at-openapi.yaml')`
   - Keep path.resolve() for absolute path resolution (RESEARCH.md pattern)

2. **Add schema existence validation (optional but recommended):**
   - Check if schema file exists before createOpenAPI
   - If missing, throw error with helpful message: "Run: bun run scripts/download-openapi.ts"
   - Use: `await Bun.file(SCHEMA_PATH).exists()`
   - This provides fail-fast error vs cryptic build errors

3. **Review proxyUrl setting:**
   - Current: `proxyUrl: '/api/proxy'`
   - Keep as-is (enables API playground if needed later)
   - Not required for basic API docs (RESEARCH.md open question #4)

4. **Follow RESEARCH.md Pattern 2:**
   - Reference: Code Examples > "OpenAPI Server with Error Handling"
   - Match error handling style and path construction

**Why validate existence:** Prevents cryptic fumadocs-openapi errors during build (RESEARCH.md Pitfall 1).

**Why keep proxyUrl:** Future-proofing for interactive API playground without breaking changes.
  </action>
  <verify>
1. Read updated openapi.ts: Check path points to data.gv.at-openapi.yaml
2. Verify import statements unchanged (path from 'node:path', createOpenAPI)
3. Confirm export const openapi maintains same name (used by source.tsx)
  </verify>
  <done>
- [x] openapi.ts references data.gv.at-openapi.yaml
- [x] Schema existence validation added (fail-fast on missing file)
- [x] proxyUrl preserved for future playground support
- [x] Export name unchanged (maintains compatibility)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update loader baseDir for appropriate navigation placement</name>
  <files>docs/lib/source.tsx</files>
  <action>
Update docs/lib/source.tsx to place API docs in appropriate navigation section:

1. **Review current baseDir:**
   - Current: `baseDir: 'openapi/(generated)'`
   - This creates navigation under /docs/openapi/(generated)

2. **Update to clearer path:**
   - Change to: `baseDir: 'api-reference'`
   - Creates navigation under /docs/api-reference (clearer for users)
   - RESEARCH.md recommendation: "Use baseDir: 'api-reference' for clear separation"

3. **Verify openapiSource configuration:**
   - Pattern: `await openapiSource(openapi, { baseDir: 'api-reference' })`
   - Ensure await is present (required for async operation, RESEARCH.md Pitfall 4)
   - Keep existing plugins array: `[pageTreeCodeTitles(), lucideIconsPlugin(), openapiPlugin()]`

4. **No other changes needed:**
   - Import of openapi from '@/lib/openapi' already correct
   - multiple() configuration with docs + openapi sources correct
   - i18n configuration already in place

**Why api-reference:** Clear, standard naming convention for API documentation sections. Users expect /api-reference path.

**Why await matters:** Without await, loader cache issues can cause stale docs (RESEARCH.md Pitfall 4).
  </action>
  <verify>
1. Read updated source.tsx: Check baseDir is 'api-reference'
2. Verify await openapiSource still present
3. Confirm plugins array includes openapiPlugin()
4. Check import of openapi unchanged
  </verify>
  <done>
- [x] baseDir updated to 'api-reference'
- [x] openapiSource called with await
- [x] Plugins configuration maintained
- [x] Virtual pages will appear under /docs/api-reference
  </done>
</task>

<task type="auto">
  <name>Task 3: Create navigation metadata and verify build</name>
  <files>
    docs/content/docs/api-reference/meta.json
    docs/content/docs/meta.json
  </files>
  <action>
Set up navigation metadata for API reference section:

1. **Create api-reference section metadata:**
   - File: `docs/content/docs/api-reference/meta.json`
   - Content:
     ```json
     {
       "title": "API Reference",
       "description": "Auto-generated API documentation from data.gv.at OpenAPI schema",
       "root": true
     }
     ```
   - This provides navigation title and description for the section

2. **Update root navigation (if needed):**
   - File: `docs/content/docs/meta.json`
   - Check if api-reference already listed
   - If not, add to appropriate position (likely after tools section)
   - Follow existing navigation order from Phase 4 decisions (16-03, decision 180)
   - Navigation order: tutorials -> guides -> examples -> tools -> api-reference -> best-practices -> advanced -> integration

3. **Verify build with OpenAPI docs:**
   - Run: `cd docs && bun run build`
   - Build should succeed (Next.js build completes)
   - Check console output for API pages count (fumadocs logs generated pages)
   - Expected: Multiple API endpoint pages generated from schema

4. **Verify generated pages structure:**
   - After build, check `.next/` directory for generated routes
   - API pages should appear under /docs/api-reference
   - Note: These are virtual pages (no physical MDX files created)

**Why meta.json needed:** Fumadocs requires metadata for navigation sections (established pattern from Phase 2).

**Why root: true:** Indicates top-level navigation section (not nested under another section).

**Build verification critical:** Ensures schema is valid, configuration correct, no runtime errors (RESEARCH.md verification approach).
  </action>
  <verify>
1. Check file exists: `cat docs/content/docs/api-reference/meta.json`
2. Verify root meta.json includes api-reference
3. Run build: `cd docs && bun run build`
4. Build succeeds with 0 errors
5. Console shows API pages generated (look for fumadocs-openapi logs)
6. Check .next/server/app/docs/api-reference directory exists after build
  </verify>
  <done>
- [x] api-reference/meta.json created with title and description
- [x] Root meta.json updated with api-reference navigation entry
- [x] Build succeeds: `bun run build` completes without errors
- [x] API reference pages generated from OpenAPI schema
- [x] Navigation displays API Reference section
- [x] Virtual pages accessible at /docs/api-reference/[endpoints]
  </done>
</task>

</tasks>

<verification>
**Overall phase validation:**

1. **Configuration updated:**
   - openapi.ts points to data.gv.at-openapi.yaml
   - source.tsx baseDir set to 'api-reference'
   - Navigation metadata created

2. **Build succeeds:**
   - `cd docs && bun run build` completes successfully
   - No OpenAPI-related errors in build output
   - fumadocs-openapi generates API pages

3. **Navigation works:**
   - API Reference section appears in sidebar
   - Virtual pages render at /docs/api-reference/[endpoints]
   - OpenAPI documentation is accessible

4. **Requirements coverage:**
   - API-01: OpenAPI specification integrated ✓
   - API-02: Auto-generated from spec ✓
   - API-03: Examples (depends on schema content - verified in 07-01)
   - API-04: Integrated into navigation ✓

**Success indicators:**
- ✓ Build produces API documentation pages
- ✓ No placeholder scalar.yaml references
- ✓ Clear navigation structure
- ✓ Ready for automation (Phase 7 Plan 3)
</verification>

<success_criteria>
- [x] openapi.ts updated to use data.gv.at schema
- [x] source.tsx baseDir set to 'api-reference'
- [x] Navigation metadata created for API reference section
- [x] Build succeeds with OpenAPI documentation generated
- [x] API reference pages accessible at /docs/api-reference
- [x] Requirements API-01, API-02, API-04 satisfied
- [x] Foundation ready for automated weekly updates (Plan 3)
</success_criteria>

<output>
After completion, create `.planning/phases/07-openapi-integration/07-02-SUMMARY.md`

Include in summary:
- Build output statistics (total pages, API pages generated)
- Navigation structure (where API reference appears)
- Any issues with schema quality affecting documentation
- Files modified
- Requirements satisfied (API-01, API-02, API-04)
</output>
