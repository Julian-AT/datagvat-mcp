---
phase: 17.1-chat-foundation-reset
plan: 10
type: execute
wave: 5
depends_on: [17.1-06, 17.1-07, 17.1-08, 17.1-09]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Database schema migrated to Vercel's chat/message/document tables"
    - "User sends message and sees conversation history in AI responses"
    - "Visualizations render in Canvas component (not blob images)"
    - "MCP tools (dataset search, execute-python) work after migration"
    - "No TypeScript compilation errors"
    - "Migration executed successfully via psql"
  artifacts:
    - path: "docs/db/schema.ts"
      provides: "Vercel schema in production database"
      contains: "chat.*message.*document"
  key_links:
    - from: "Database"
      to: "Application code"
      via: "Schema migration applied, queries use new tables"
      pattern: "chat.*message.*getMessagesByChatId"
---

<objective>
Execute database migration and verify complete Vercel foundation rebuild works end-to-end.

Purpose: Final verification that all three issues are resolved - conversation history loading, artifact/canvas rendering, and proper schema in database. Also verify MCP tools continue working post-migration.
Output: Production-ready chat system using Vercel's proven architecture with our MCP tools integrated.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianschmidt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/PROJECT.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/ROADMAP.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/STATE.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/phases/17.1-chat-foundation-reset/17.1-RESEARCH.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/phases/17.1-chat-foundation-reset/17.1-05-CHECKPOINT-REJECTION.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/phases/17.1-chat-foundation-reset/17.1-06-PLAN.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/phases/17.1-chat-foundation-reset/17.1-07-PLAN.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/phases/17.1-chat-foundation-reset/17.1-08-PLAN.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/phases/17.1-chat-foundation-reset/17.1-09-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-action>Execute database migration to Vercel schema</what-action>
  <why>
Migration requires psql with DATABASE_URL access - only user has credentials.

The migration script (docs/db/migrations/0005_vercel_schema_migration.sql) created in Plan 17.1-06:
- Creates chat, message, document tables with UUID primary keys
- Migrates existing conversations → chat
- Migrates existing messages → message (preserving data)
- Idempotent (IF NOT EXISTS guards, duplicate checks)
  </why>
  <instructions>
1. Verify migration file exists:
   ```bash
   cat docs/db/migrations/0005_vercel_schema_migration.sql | head -20
   ```

2. Execute migration:
   ```bash
   psql $DATABASE_URL < docs/db/migrations/0005_vercel_schema_migration.sql
   ```

3. Verify tables created:
   ```bash
   psql $DATABASE_URL -c "\dt" | grep -E "(chat|message|document)"
   ```

   Expected output: chat, message, document tables listed

4. Verify data migrated (if had existing conversations):
   ```bash
   psql $DATABASE_URL -c "SELECT COUNT(*) FROM chat;"
   psql $DATABASE_URL -c "SELECT COUNT(*) FROM message;"
   ```

5. Check for migration errors:
   - If migration reports errors, share the error message
   - Common issue: duplicate data (safe to ignore if using IF NOT EXISTS)
  </instructions>
  <resume-signal>
Type "migration complete" when tables created and data verified.
If errors occurred, describe them for troubleshooting.
  </resume-signal>
</task>

<task type="auto">
  <name>Run TypeScript compilation check</name>
  <files>N/A</files>
  <action>
Verify all TypeScript changes compile without errors.

```bash
cd docs && bun run typecheck
```

Expected: Zero TypeScript errors. All schema changes, query functions, route updates, and component refactors should compile cleanly.

If errors found:
1. Read error messages
2. Fix type mismatches (likely in schema imports or Message type references)
3. Re-run typecheck until clean
  </action>
  <verify>
    - [ ] TypeScript compiles: cd docs && bun run typecheck exits with code 0
    - [ ] No type errors in schema files (docs/db/schema.ts)
    - [ ] No type errors in query files (docs/lib/db/queries.ts)
    - [ ] No type errors in route files (docs/app/api/chat/route.ts)
  </verify>
  <done>TypeScript compilation succeeds with no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Vercel foundation rebuild:
- Database using chat/message/document schema (Plan 17.1-06)
- Query layer with conversation history loading (Plan 17.1-07)
- Chat route with proper message persistence and streaming (Plan 17.1-08)
- UI components using artifact/canvas pattern (Plan 17.1-09)
  </what-built>
  <how-to-verify>
**Test 1: Conversation History Loading (Issue 1 - CRITICAL)**

1. Start dev server: `cd docs && bun run dev`

2. Open browser: `http://localhost:3000` (or wherever chat UI is)

3. Send first message: "What Austrian datasets are available for Vienna?"

4. Wait for response (should use data.gv.at MCP tool)

5. Send follow-up message: "Can you search for air quality data?"

6. **VERIFY:** Response shows awareness of previous question about Vienna
   - AI should reference the prior context
   - Should NOT ask what location you're interested in (already knows Vienna)

**Expected behavior:** AI maintains conversation context across messages.

**Issue 1 was:** "Only most recent message in LLM context, no conversation history"

**Fix applied:** getMessagesByChatId + convertToUIMessages + merge with new messages

---

**Test 2: Visualization Rendering (Issue 2)**

1. Send message: "Create a bar chart showing random data"

2. Wait for execute-python tool to run

3. **VERIFY:** Visualization appears in Canvas component
   - Should see green status indicator
   - Should see "Visualization" header with format badge (PNG/SVG/HTML)
   - Should see fullscreen button
   - Should NOT be a plain `<img>` tag with blob URL

**Expected behavior:** Visualization renders in Canvas with Vercel's artifact UI pattern.

**Issue 2 was:** "Trying to render visualizations as images with blob upload"

**Fix applied:** Canvas component with artifact/canvas pattern from Vercel

---

**Test 3: Database Schema (Issue 3)**

1. Check database schema:
   ```bash
   psql $DATABASE_URL -c "\d chat"
   psql $DATABASE_URL -c "\d message"
   ```

2. **VERIFY:**
   - chat table has: id (uuid), user_id (text), title (text), created_at (timestamp), visibility (varchar)
   - message table has: id (uuid), chat_id (uuid), role (varchar), parts (jsonb), attachments (jsonb), created_at (timestamp)

**Expected behavior:** Database uses Vercel's schema, not custom conversations/messages.

**Issue 3 was:** "User unhappy with current DB schema and would prefer vercels one"

**Fix applied:** Complete schema replacement with chat/message/document tables

---

**Test 4: MCP Tools Integration (FOUNDATION-05)**

1. **Test dataset search tool:**
   - Send: "Search for Vienna air quality datasets"
   - **VERIFY:** AI calls dataset search tool (not generic web search)
   - **VERIFY:** Results display dataset names, descriptions, update frequency
   - **VERIFY:** Tool results stored in message parts array

2. **Test execute-python tool:**
   - Send: "Create a simple bar chart with matplotlib"
   - **VERIFY:** AI generates Python code
   - **VERIFY:** execute-python tool executes in E2B sandbox
   - **VERIFY:** Visualization uploads to Vercel Blob (URL in network tab)
   - **VERIFY:** Visualization renders in Canvas component
   - **VERIFY:** No base64 data in message parts (check browser DevTools)

**Expected behavior:** MCP tools work seamlessly with new schema and persistence layer.

**Requirement FOUNDATION-05:** "Integrate our MCP tools (dataset search, execute-python)"

**Fix applied:** Tools integrated with chat route via getAvailableTools, results stored in parts array

---

**Test 5: Build Verification**

1. Run production build:
   ```bash
   cd docs && bun run build
   ```

2. **VERIFY:** Build completes without errors
   - No TypeScript errors
   - No missing dependencies
   - No runtime errors during build

---

**Success criteria:**
- [ ] Conversation history works (follow-up messages show context awareness)
- [ ] Visualizations render in Canvas component (not plain images)
- [ ] Database schema matches Vercel's (chat/message/document tables with UUIDs)
- [ ] Dataset search tool works and results display correctly
- [ ] Execute-python tool works and visualizations render correctly
- [ ] Build completes successfully
  </how-to-verify>
  <resume-signal>
Type "verified" if all tests pass.

If issues found, describe:
- Which test failed
- What behavior you observed
- Any error messages
  </resume-signal>
</task>

</tasks>

<verification>
1. Migration executed: `psql $DATABASE_URL -c "\dt" | grep -E "(chat|message|document)"` shows 3 tables
2. TypeScript clean: `cd docs && bun run typecheck` exits 0
3. Conversation history loading: User confirms follow-up messages show context
4. Canvas rendering: User confirms visualizations appear in Canvas component
5. Schema correct: `psql $DATABASE_URL -c "\d chat"` shows Vercel's structure
6. MCP tools work: User confirms dataset search and execute-python tools function correctly
</verification>

<success_criteria>
1. Database migration executed successfully
2. chat, message, document tables exist with UUID primary keys
3. Existing data migrated to new schema (if applicable)
4. TypeScript compilation succeeds with zero errors
5. Conversation history loads correctly (Issue 1 resolved)
6. Visualizations render in Canvas component (Issue 2 resolved)
7. Database uses Vercel's schema (Issue 3 resolved)
8. MCP tools (dataset search, execute-python) work correctly (FOUNDATION-05 satisfied)
9. Production build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-chat-foundation-reset/17.1-10-SUMMARY.md` documenting final verification and resolution of all three issues.

**Issues Resolved:**
- Issue 1 (Chat memory not working): ✅ Fixed with getMessagesByChatId + convertToUIMessages
- Issue 2 (Wrong rendering approach): ✅ Fixed with Canvas component using artifact/canvas pattern
- Issue 3 (Database schema mismatch): ✅ Fixed with complete Vercel schema adoption

**Requirements Satisfied:**
- FOUNDATION-05 (MCP tools integration): ✅ Dataset search and execute-python tools working with new architecture

**User Directive Fulfilled:**
> "Use the GSD tools to not try to resolve this issue based on the code thats already there but completley rebuild it based on vercels approach which works much better. Everything where we overlap, vercels approach should be prioritized. This also includes critical decisions like the db schema etc."

✅ Complete rebuild using Vercel's architecture
✅ Their schema, query patterns, streaming approach, and UI components
✅ Our MCP integration and E2B tools preserved and working
</output>
