---
phase: 01-enterprise-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, app/server.py]
autonomous: true

must_haves:
  truths:
    - "Server retries failed API calls with exponential backoff"
    - "Server limits request rate to prevent API overload"
    - "Logs are structured JSON with request tracing"
    - "All requests have correlation IDs"
  artifacts:
    - path: "pyproject.toml"
      provides: "FastMCP 2.14+ dependency"
      contains: "fastmcp>=2.14.0"
    - path: "app/server.py"
      provides: "Built-in middleware registration"
      min_lines: 80
      exports: ["mcp"]
  key_links:
    - from: "app/server.py"
      to: "fastmcp.server.middleware"
      via: "middleware imports"
      pattern: "from fastmcp\\.server\\.middleware"
---

<objective>
Upgrade FastMCP to 2.14+ and integrate built-in enterprise middleware for retries, rate limiting, structured logging, and correlation IDs.

Purpose: Replace manual logging with production-grade middleware that handles retries, rate limiting, structured logging, and request tracing automatically.
Output: Updated dependencies and middleware configuration with FastMCP 2.14+ built-in middleware active.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@pyproject.toml
@app/server.py
@app/middleware.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade FastMCP dependency to 2.14+</name>
  <files>pyproject.toml</files>
  <action>
Update line 9 from `"fastmcp>=2.3.0",` to `"fastmcp>=2.14.0",`.

Why 2.14.0: This version includes RetryMiddleware, RateLimitingMiddleware, ResponseCachingMiddleware, and StructuredLoggingMiddleware that we need for enterprise reliability.

Do NOT change other dependencies. Do NOT add new dependencies (tenacity, structlog) - FastMCP 2.14 includes everything we need.
  </action>
  <verify>
- grep 'fastmcp>=2.14.0' pyproject.toml shows the updated version
- No other dependencies changed
  </verify>
  <done>pyproject.toml specifies fastmcp>=2.14.0, ready for pip install</done>
</task>

<task type="auto">
  <name>Task 2: Install updated dependencies</name>
  <files>None (package installation)</files>
  <action>
Run `pip install -e .` to install FastMCP 2.14.0 and verify the middleware modules are available.

Test imports after installation:
```python
python -c "from fastmcp.server.middleware.error_handling import RetryMiddleware, ErrorHandlingMiddleware; from fastmcp.server.middleware.rate_limiting import RateLimitingMiddleware; from fastmcp.server.middleware.logging import StructuredLoggingMiddleware; print('All middleware imports successful')"
```

If import fails, this indicates FastMCP 2.14 is not installed correctly - investigate and fix before proceeding.
  </action>
  <verify>
- pip list | grep fastmcp shows version 2.14.x
- Test import command succeeds without errors
  </verify>
  <done>FastMCP 2.14+ installed, middleware imports verified</done>
</task>

<task type="auto">
  <name>Task 3: Integrate built-in enterprise middleware</name>
  <files>app/server.py</files>
  <action>
Add middleware imports at top of file (after existing imports, before custom middleware import):

```python
from fastmcp.server.middleware.error_handling import RetryMiddleware, ErrorHandlingMiddleware
from fastmcp.server.middleware.rate_limiting import RateLimitingMiddleware
from fastmcp.server.middleware.logging import StructuredLoggingMiddleware
```

Update the FastMCP instantiation (lines 56-64) to register middleware in this order:
1. StructuredLoggingMiddleware (first - logs everything)
2. ErrorHandlingMiddleware (wraps errors in standard format)
3. RetryMiddleware (retries transient failures)
4. RateLimitingMiddleware (limits request rate)
5. AuditMiddleware (existing - app-specific audit logging)
6. AuthMiddleware (existing - write operation auth)

Configuration:
- RetryMiddleware: max_retries=3, base_delay=1.0, max_delay=60.0, backoff_multiplier=2.0, retry_exceptions=(ConnectionError, TimeoutError)
- RateLimitingMiddleware: max_requests_per_second=10.0, burst_capacity=20, global_limit=False
- StructuredLoggingMiddleware: include_payloads=False, include_payload_length=True, estimate_payload_tokens=True
- ErrorHandlingMiddleware: no parameters needed

Why this order: Logging first (sees everything), error handling second (standardizes errors), retry third (handles transients), rate limit fourth (prevents overload), then app-specific middleware last.

Keep existing lifespan, logging configuration, and tool registrations unchanged. Only replace the middleware list in FastMCP() instantiation.
  </action>
  <verify>
- app/server.py imports all four middleware classes from fastmcp.server.middleware
- FastMCP middleware list includes 6 middleware instances in correct order
- Existing AuditMiddleware and AuthMiddleware still present
- python -m app.server starts without import errors
  </verify>
  <done>Built-in middleware integrated, server starts successfully with all 6 middleware active</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip list | grep fastmcp` shows 2.14.x
- [ ] `python -m app.server` starts without errors
- [ ] Test import of all middleware classes succeeds
- [ ] Middleware order: StructuredLogging → ErrorHandling → Retry → RateLimit → Audit → Auth
</verification>

<success_criteria>

- All tasks completed
- FastMCP upgraded to 2.14+
- All four built-in middleware integrated
- Existing custom middleware preserved
- Server starts successfully
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/01-enterprise-foundation/01-01-SUMMARY.md`
</output>
