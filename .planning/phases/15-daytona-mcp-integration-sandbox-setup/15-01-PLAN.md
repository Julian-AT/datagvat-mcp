---
phase: 15-daytona-mcp-integration-sandbox-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/package.json
  - docs/lib/mcp/e2b-client.ts
  - docs/lib/mcp/datagvat-client.ts
  - docs/lib/mcp/types.ts
autonomous: true

must_haves:
  truths:
    - "User can see E2B connection status via health check endpoint"
    - "User can see data.gv.at MCP connection status via health check endpoint"
    - "Application provides clear error when MCP services unavailable"
  artifacts:
    - path: "docs/lib/mcp/e2b-client.ts"
      provides: "E2B Code Interpreter wrapper with createSandbox function"
      min_lines: 40
      exports: ["createE2BClient", "createSandbox"]
    - path: "docs/lib/mcp/datagvat-client.ts"
      provides: "Data.gv.at MCP HTTP client initialization"
      min_lines: 30
      exports: ["createDataGvatClient"]
    - path: "docs/package.json"
      contains: "@e2b/code-interpreter"
  key_links:
    - from: "docs/lib/mcp/e2b-client.ts"
      to: "@e2b/code-interpreter"
      via: "import { Sandbox }"
      pattern: "from ['\"]@e2b/code-interpreter['\"]"
    - from: "docs/lib/mcp/datagvat-client.ts"
      to: "@ai-sdk/mcp"
      via: "createMCPClient with HTTP transport"
      pattern: "createMCPClient.*type:\\s*['\"]http['\"]"
---

<objective>
Establish MCP client connections for E2B Code Interpreter and data.gv.at, enabling multi-MCP orchestration for dataset search and code execution.

Purpose: Provide foundation for Phase 16's tool aggregation by creating working MCP clients that can be composed in AI SDK calls.

Output: Working E2B sandbox creation and data.gv.at MCP client with type-safe wrappers.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianschmidt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/PROJECT.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/ROADMAP.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/STATE.md
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/.planning/phases/15-daytona-mcp-integration-sandbox-setup/15-RESEARCH.md

Phase 14 outputs (database schema available):
@/Users/julianschmidt/Documents/GitHub/datagvat-mcp/docs/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Install E2B Code Interpreter SDK</name>
  <files>docs/package.json</files>
  <action>
Install @e2b/code-interpreter package:

```bash
cd docs && npm install @e2b/code-interpreter
```

Verify installation completes and package.json includes the dependency.

WHY this specific package: Research (15-RESEARCH.md) confirms Daytona MCP does not exist. E2B Code Interpreter is the standard solution for AI agent code execution with 200ms startup, Firecracker VM isolation, and production-ready sandbox management.
  </action>
  <verify>
```bash
cd docs && grep -q "@e2b/code-interpreter" package.json && echo "E2B installed"
```
  </verify>
  <done>package.json contains @e2b/code-interpreter dependency</done>
</task>

<task type="auto">
  <name>Create E2B client wrapper</name>
  <files>docs/lib/mcp/e2b-client.ts, docs/lib/mcp/types.ts</files>
  <action>
Create type-safe E2B client wrapper following research patterns from 15-RESEARCH.md Pattern 2.

**docs/lib/mcp/types.ts:**
```typescript
export interface SandboxExecutionResult {
  text: string;
  error?: string;
  logs?: string[];
}

export interface E2BClientConfig {
  apiKey: string;
  timeoutMs?: number;
}
```

**docs/lib/mcp/e2b-client.ts:**
Implement createE2BClient and createSandbox functions:

- Accept E2B API key from environment variable (E2B_API_KEY)
- Default timeout: 1 hour (3600000ms) per requirement EXEC-06
- createSandbox() returns Sandbox instance with .runCode() and .kill() methods
- Include try/finally pattern for guaranteed cleanup
- Type-safe wrapper matching SandboxExecutionResult interface

Reference pattern: 15-RESEARCH.md "Pattern 2: E2B Sandbox Lifecycle Management"

WHY wrapper instead of direct SDK: Provides consistent error handling, enforces timeout defaults, simplifies Phase 17 integration.
  </action>
  <verify>
```bash
cd docs && npx tsc --noEmit lib/mcp/e2b-client.ts lib/mcp/types.ts
```
  </verify>
  <done>E2B client exports createE2BClient and createSandbox with 1-hour timeout default, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Create data.gv.at MCP client</name>
  <files>docs/lib/mcp/datagvat-client.ts</files>
  <action>
Create HTTP MCP client for data.gv.at FastMCP server using @ai-sdk/mcp (already installed v1.0.16).

**docs/lib/mcp/datagvat-client.ts:**
```typescript
import { createMCPClient } from '@ai-sdk/mcp';

export async function createDataGvatClient(url: string) {
  return await createMCPClient({
    transport: {
      type: 'http',
      url,
      headers: {
        'Content-Type': 'application/json',
      },
    },
  });
}
```

Features:
- Accept MCP server URL from environment (DATAGVAT_MCP_URL)
- Use HTTP transport (not stdio - serverless compatible)
- Return MCPClient instance for tool discovery
- Include basic error handling with try/catch

Reference pattern: 15-RESEARCH.md "AI SDK MCP Client with HTTP Transport (Production)"

WHY HTTP transport: stdio cannot deploy to Vercel serverless. HTTP transport enables production deployment and matches existing FastMCP server capabilities.

IMPORTANT: Do NOT create the data.gv.at FastMCP server in this task - that's a separate Python service. This task only creates the TypeScript client that connects to an existing server.
  </action>
  <verify>
```bash
cd docs && npx tsc --noEmit lib/mcp/datagvat-client.ts
```
  </verify>
  <done>Data.gv.at MCP client exports createDataGvatClient function accepting URL parameter, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes for all new MCP client files
2. E2B client wrapper enforces 1-hour timeout by default (EXEC-06)
3. Data.gv.at client uses HTTP transport (serverless compatible)
4. Both clients follow research patterns from 15-RESEARCH.md
</verification>

<success_criteria>
- E2B Code Interpreter package installed and importable
- E2B client wrapper provides createSandbox with timeout enforcement
- Data.gv.at MCP client initializes via HTTP transport
- Type definitions support both MCP clients
- No TypeScript compilation errors
- Code follows clean code standards (minimal comments, self-explanatory)
</success_criteria>

<output>
After completion, create `.planning/phases/15-daytona-mcp-integration-sandbox-setup/15-01-SUMMARY.md` following the summary template.
</output>
