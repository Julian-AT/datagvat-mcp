---
phase: 13-video-tutorials
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - docs/public/videos/quickstart.vtt
  - docs/public/videos/workflow.vtt
  - docs/public/videos/architecture.vtt
  - docs/components/VideoPlayer.tsx
  - docs/content/docs/tutorials/videos.mdx
  - docs/content/docs/tutorials/meta.json
  - docs/package.json
  - .planning/phases/13-video-tutorials/build-verification.txt
autonomous: true

user_setup:
  - service: openai-whisper
    why: "Automated caption generation from video audio (optional - can write captions manually)"
    env_vars: []
    manual_steps:
      - task: "Install Whisper (Python package)"
        command: "pip install -U openai-whisper"
        note: "Only needed if generating captions from audio. For silent videos, write VTT files manually."

must_haves:
  truths:
    - "User enables captions on any video and reads synchronized text"
    - "User views video on mobile and it scales responsively without horizontal scroll"
    - "User clicks video page in documentation and sees all three videos embedded"
    - "Developer runs full build and completes in <5 minutes (videos cached)"
    - "Developer changes video source and videos re-render, captions remain valid"
  artifacts:
    - path: "docs/public/videos/quickstart.vtt"
      provides: "WebVTT captions for quickstart video"
      contains: "WEBVTT"
    - path: "docs/public/videos/workflow.vtt"
      provides: "WebVTT captions for workflow video"
      contains: "WEBVTT"
    - path: "docs/public/videos/architecture.vtt"
      provides: "WebVTT captions for architecture video"
      contains: "WEBVTT"
    - path: "docs/components/VideoPlayer.tsx"
      provides: "Accessible video player component"
      exports: ["VideoPlayer"]
      min_lines: 40
    - path: "docs/content/docs/tutorials/videos.mdx"
      provides: "Video tutorial documentation page"
      contains: "<VideoPlayer"
  key_links:
    - from: "docs/content/docs/tutorials/videos.mdx"
      to: "docs/components/VideoPlayer.tsx"
      via: "Component import and usage"
      pattern: "import.*VideoPlayer"
    - from: "docs/components/VideoPlayer.tsx"
      to: "docs/public/videos/*.{mp4,vtt}"
      via: "Video and caption file references"
      pattern: "<source.*src=|<track.*src="
    - from: "HTML5 video element"
      to: "WebVTT caption files"
      via: "Track element with default attribute"
      pattern: "<track.*default"
---

<objective>
Generate accessible WebVTT captions for all videos, create responsive video player component, embed videos in documentation, and verify complete build pipeline.

Purpose: Ensure WCAG 2.1 accessibility compliance through captions, provide seamless documentation integration, and validate that video rendering stays within build time constraints.

Output: Three videos with captions embedded in documentation, passing all build verification checks, completing builds in <5 minutes through effective caching.
</objective>

<execution_context>
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\workflows\execute-plan.md
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\phases\13-video-tutorials\13-RESEARCH.md
@C:\GitHub\datagvat-mcp\.planning\phases\13-video-tutorials\13-01-SUMMARY.md
@C:\GitHub\datagvat-mcp\.planning\phases\13-video-tutorials\13-02-SUMMARY.md

# Research patterns
Pattern 5: Accessible video embedding in MDX
Common Pitfall 2: Missing WCAG accessibility requirements
Code Example: WebVTT caption file format

# Build verification reference
@C:\GitHub\datagvat-mcp\.planning\phases\10-navigation-simplification\10-06-SUMMARY.md
@C:\GitHub\datagvat-mcp\.planning\phases\12-rag-documentation-chat\12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Generate WebVTT caption files for all three videos</name>
  <files>
    docs/public/videos/quickstart.vtt
    docs/public/videos/workflow.vtt
    docs/public/videos/architecture.vtt
  </files>
  <action>
Create manual WebVTT caption files matching the video compositions created in Plan 13-02. Since our videos are programmatically generated animations with text overlays (not spoken audio), we'll create caption files that describe the visual content for accessibility.

Create `docs/public/videos/quickstart.vtt`:

```
WEBVTT

00:00.000 --> 00:03.000
DataGvat MCP Quick Start Guide

00:03.000 --> 00:20.000
Installation Step 1: Install globally with npm install -g @datagvat/mcp-server

00:20.000 --> 00:40.000
Installation Step 2: Verify installation with datagvat --version

00:40.000 --> 01:00.000
Installation Step 3: Configure Claude Desktop by editing claude_desktop_config.json

01:00.000 --> 01:30.000
Configuration: Add mcpServers section with datagvat command and optional environment variables

01:30.000 --> 01:50.000
Your First Query: Ask Claude to find Vienna datasets about public transport

01:50.000 --> 02:20.000
Claude uses MCP tools: search_datasets, get_dataset, and finds 12 relevant datasets

02:20.000 --> 02:30.000
You're ready! Explore the full documentation at datagvat.dev
```

Create `docs/public/videos/workflow.vtt`:

```
WEBVTT

00:00.000 --> 00:03.000
MCP Workflows: Real-World Examples

00:03.000 --> 00:40.000
Workflow 1: Dataset Discovery - Search with filters using search_datasets

00:40.000 --> 01:10.000
Get detailed metadata with get_dataset for climate-data-austria-2024

01:10.000 --> 01:35.000
Analyze quality metrics with analyze_quality tool

01:35.000 --> 02:20.000
Workflow 2: Quality Assessment shows 95% metadata completeness, monthly updates

02:20.000 --> 02:40.000
Quality assessment identifies partial documentation and machine-readable CSV format

02:40.000 --> 03:20.000
Workflow 3: Data Preview displays schema with date, temperature, and location fields

03:20.000 --> 04:00.000
Sample data shows Vienna temperature readings from January 2024
```

Create `docs/public/videos/architecture.vtt`:

```
WEBVTT

00:00.000 --> 00:03.000
Architecture Overview: How DataGvat MCP Works

00:03.000 --> 00:30.000
Layered Architecture: Four layers working together

00:30.000 --> 01:00.000
Layer 1: MCP Protocol Layer handles communication with Claude Desktop

01:00.000 --> 01:30.000
Layer 2: FastMCP Framework provides tool registration and routing

01:30.000 --> 02:00.000
Layer 3: Enterprise Middleware includes retry logic, rate limiting, and structured logging

02:00.000 --> 02:30.000
Layer 4: Piveau Hub API Client interfaces with data.gv.at API

02:30.000 --> 03:30.000
Data Flow Step 1: User asks natural language query about Vienna transport datasets

03:30.000 --> 04:30.000
Data Flow Step 2: MCP tool invocation calls search_datasets with query parameters

04:30.000 --> 05:30.000
Data Flow Step 3: API request sent to Piveau Hub API with search parameters

05:30.000 --> 06:00.000
Data Flow Step 4: Structured response returns 12 datasets with metadata, quality scores, and download links
```

Why manual captions: Our videos contain text overlays and animations but no spoken narration. Manual captions describe the visual content for screen reader users and provide text alternatives. Whisper would be used if we had audio narration, but it's unnecessary for silent animated videos.

Ensure captions are NOT added to .gitignore - they're small text files that should be version controlled, unlike the large MP4 files.
  </action>
  <verify>
```bash
test -f docs/public/videos/quickstart.vtt && echo "✓ QuickStart captions created"
test -f docs/public/videos/workflow.vtt && echo "✓ Workflow captions created"
test -f docs/public/videos/architecture.vtt && echo "✓ Architecture captions created"

# Verify WebVTT format
grep -q "WEBVTT" docs/public/videos/quickstart.vtt && echo "✓ Valid WebVTT format"

# Verify captions are NOT gitignored
! git check-ignore docs/public/videos/*.vtt 2>/dev/null && echo "✓ VTT files tracked in git"
```
  </verify>
  <done>
- Three .vtt files created with proper WebVTT format
- Captions describe visual content and timing synchronized with video scenes
- Caption files tracked in git (not ignored)
- WCAG 2.1 Level A compliance for prerecorded video content
  </done>
</task>

<task type="auto">
  <name>Create responsive VideoPlayer component and embed in documentation</name>
  <files>
    docs/components/VideoPlayer.tsx
    docs/content/docs/tutorials/videos.mdx
    docs/content/docs/tutorials/meta.json
  </files>
  <action>
Create `docs/components/VideoPlayer.tsx`:

```typescript
import type React from 'react';

interface VideoPlayerProps {
  /**
   * Path to video file (relative to /public)
   */
  src: string;
  /**
   * Path to WebVTT caption file (relative to /public)
   */
  captions: string;
  /**
   * Optional poster image (shown before play)
   */
  poster?: string;
  /**
   * Video title for accessibility
   */
  title: string;
}

/**
 * Accessible video player component with responsive sizing and captions
 *
 * Features:
 * - Responsive scaling (100% width, maintains aspect ratio)
 * - WebVTT captions with default enabled
 * - Native browser controls
 * - Fallback download link for unsupported browsers
 */
export function VideoPlayer({ src, captions, poster, title }: VideoPlayerProps) {
  return (
    <figure style={{ margin: '2rem 0' }}>
      <video
        controls
        width={1920}
        height={1080}
        poster={poster}
        preload="metadata"
        style={{
          width: '100%',
          height: 'auto',
          maxWidth: '1920px',
          borderRadius: '8px',
          boxShadow: '0 4px 6px rgba(0, 0, 0, 0.1)',
        }}
        aria-label={title}
      >
        <source src={src} type="video/mp4" />
        <track
          kind="captions"
          src={captions}
          srcLang="en"
          label="English"
          default
        />
        Your browser doesn't support HTML5 video.{' '}
        <a href={src} download style={{ color: '#3b82f6', textDecoration: 'underline' }}>
          Download the video
        </a>
      </video>
      <figcaption style={{
        marginTop: '0.5rem',
        fontSize: '0.875rem',
        color: '#64748b',
        textAlign: 'center',
      }}>
        {title}
      </figcaption>
    </figure>
  );
}
```

Create `docs/content/docs/tutorials/videos.mdx`:

```mdx
---
title: Video Tutorials
description: Watch step-by-step video guides for installation, workflows, and architecture
---

import { VideoPlayer } from '@/components/VideoPlayer';

# Video Tutorials

Learn DataGvat MCP through comprehensive video demonstrations covering installation, key workflows, and system architecture.

## Quick Start Guide

Watch the 2.5-minute quick start video to get from installation to your first query:

<VideoPlayer
  src="/videos/quickstart.mp4"
  captions="/videos/quickstart.vtt"
  title="Quick Start: Installation to First Query"
/>

**What you'll learn:**
- Installing the MCP server globally
- Configuring Claude Desktop
- Running your first dataset search query

## MCP Workflows

This 4-minute video demonstrates three essential workflows with real examples:

<VideoPlayer
  src="/videos/workflow.mp4"
  captions="/videos/workflow.vtt"
  title="MCP Workflows: Real-World Examples"
/>

**Workflows covered:**
1. **Dataset Discovery** - Search, filter, and analyze datasets
2. **Quality Assessment** - Evaluate metadata completeness and update frequency
3. **Data Preview** - Inspect schemas and sample data before downloading

## Architecture Overview

Understand how DataGvat MCP works under the hood with this 6-minute architecture walkthrough:

<VideoPlayer
  src="/videos/architecture.mp4"
  captions="/videos/architecture.vtt"
  title="Architecture Overview: How DataGvat MCP Works"
/>

**Topics explained:**
- MCP Protocol Layer
- FastMCP Framework integration
- Enterprise middleware (retry, rate limiting, logging)
- Piveau Hub API integration
- Complete data flow from user query to structured response

## Accessibility

All videos include synchronized captions. To enable captions:
1. Click the **CC** button in the video player controls
2. Captions will appear at the bottom of the video

For a text alternative, view the caption files:
- [Quick Start captions](/videos/quickstart.vtt)
- [Workflows captions](/videos/workflow.vtt)
- [Architecture captions](/videos/architecture.vtt)
```

Update `docs/content/docs/tutorials/meta.json` to add videos page:

```json
{
  "title": "Tutorials",
  "pages": ["videos"]
}
```

If `meta.json` already exists with other pages, add "videos" to the pages array. If the tutorials folder doesn't exist, this creates the navigation structure.
  </action>
  <verify>
```bash
test -f docs/components/VideoPlayer.tsx && echo "✓ VideoPlayer component created"
test -f docs/content/docs/tutorials/videos.mdx && echo "✓ Videos page created"

# Verify component exports
grep -q "export function VideoPlayer" docs/components/VideoPlayer.tsx

# Verify responsive styling
grep -q "width: '100%'" docs/components/VideoPlayer.tsx && echo "✓ Responsive width"
grep -q "height: 'auto'" docs/components/VideoPlayer.tsx && echo "✓ Maintains aspect ratio"

# Verify captions enabled by default
grep -q '<track.*default' docs/components/VideoPlayer.tsx && echo "✓ Captions default enabled"

# Verify video embeds
grep -c "VideoPlayer" docs/content/docs/tutorials/videos.mdx
# Should show: 3 (one per video)
```
  </verify>
  <done>
- VideoPlayer.tsx exports responsive video component
- Component includes caption track with default attribute
- Responsive styling (100% width, auto height, max-width 1920px)
- videos.mdx embeds all three videos with descriptions
- Navigation structure updated to include videos page
- Fallback download links for unsupported browsers
  </done>
</task>

<task type="auto">
  <name>Run comprehensive build verification</name>
  <files>
    .planning/phases/13-video-tutorials/build-verification.txt
  </files>
  <action>
Run complete build verification covering all five BUILD requirements from the phase:

```bash
cd docs

echo "=== Phase 13 Build Verification ===" > ../.planning/phases/13-video-tutorials/build-verification.txt
echo "Timestamp: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> ../.planning/phases/13-video-tutorials/build-verification.txt
echo "" >> ../.planning/phases/13-video-tutorials/build-verification.txt

# BUILD-01: TypeScript compilation
echo "BUILD-01: TypeScript Compilation" >> ../.planning/phases/13-video-tutorials/build-verification.txt
bun run type-check 2>&1 | tee -a ../.planning/phases/13-video-tutorials/build-verification.txt
if [ ${PIPESTATUS[0]} -eq 0 ]; then
  echo "✓ PASS: TypeScript compilation successful" >> ../.planning/phases/13-video-tutorials/build-verification.txt
else
  echo "✗ FAIL: TypeScript errors detected" >> ../.planning/phases/13-video-tutorials/build-verification.txt
fi
echo "" >> ../.planning/phases/13-video-tutorials/build-verification.txt

# BUILD-02: Biome linting
echo "BUILD-02: Biome Linting" >> ../.planning/phases/13-video-tutorials/build-verification.txt
bun run lint 2>&1 | tee -a ../.planning/phases/13-video-tutorials/build-verification.txt
if [ ${PIPESTATUS[0]} -eq 0 ]; then
  echo "✓ PASS: Zero Biome errors" >> ../.planning/phases/13-video-tutorials/build-verification.txt
else
  echo "✗ FAIL: Biome lint errors detected" >> ../.planning/phases/13-video-tutorials/build-verification.txt
fi
echo "" >> ../.planning/phases/13-video-tutorials/build-verification.txt

# BUILD-03: Next.js build (<5 min with video caching)
echo "BUILD-03: Full Build (<5 min)" >> ../.planning/phases/13-video-tutorials/build-verification.txt
echo "Starting build at: $(date +%H:%M:%S)" >> ../.planning/phases/13-video-tutorials/build-verification.txt
BUILD_START=$(date +%s)

# Run build (videos already rendered in Plan 13-02, should be cached)
bun run build 2>&1 | tee -a ../.planning/phases/13-video-tutorials/build-verification.txt
BUILD_EXIT=$?

BUILD_END=$(date +%s)
BUILD_DURATION=$((BUILD_END - BUILD_START))
echo "Build completed at: $(date +%H:%M:%S)" >> ../.planning/phases/13-video-tutorials/build-verification.txt
echo "Build duration: ${BUILD_DURATION}s" >> ../.planning/phases/13-video-tutorials/build-verification.txt

if [ $BUILD_EXIT -eq 0 ] && [ $BUILD_DURATION -lt 300 ]; then
  echo "✓ PASS: Build successful in ${BUILD_DURATION}s (<5 min)" >> ../.planning/phases/13-video-tutorials/build-verification.txt
elif [ $BUILD_EXIT -ne 0 ]; then
  echo "✗ FAIL: Build failed (exit code: $BUILD_EXIT)" >> ../.planning/phases/13-video-tutorials/build-verification.txt
else
  echo "✗ FAIL: Build exceeded 5-minute constraint (${BUILD_DURATION}s)" >> ../.planning/phases/13-video-tutorials/build-verification.txt
fi
echo "" >> ../.planning/phases/13-video-tutorials/build-verification.txt

# BUILD-04: Backend tests (mcp/ directory)
echo "BUILD-04: Backend Tests" >> ../.planning/phases/13-video-tutorials/build-verification.txt
cd ../mcp
pytest -v 2>&1 | tee -a ../.planning/phases/13-video-tutorials/build-verification.txt
PYTEST_EXIT=${PIPESTATUS[0]}
cd ../docs

if [ $PYTEST_EXIT -eq 0 ]; then
  echo "✓ PASS: All backend tests passing" >> ../.planning/phases/13-video-tutorials/build-verification.txt
else
  echo "✗ FAIL: Backend test failures detected" >> ../.planning/phases/13-video-tutorials/build-verification.txt
fi
echo "" >> ../.planning/phases/13-video-tutorials/build-verification.txt

# BUILD-05: Zero errors summary
echo "BUILD-05: Zero Errors Summary" >> ../.planning/phases/13-video-tutorials/build-verification.txt
if [ $BUILD_EXIT -eq 0 ] && [ $PYTEST_EXIT -eq 0 ]; then
  echo "✓ PASS: All verification checks passed" >> ../.planning/phases/13-video-tutorials/build-verification.txt
  echo "" >> ../.planning/phases/13-video-tutorials/build-verification.txt
  echo "Phase 13 build verification: SUCCESS" >> ../.planning/phases/13-video-tutorials/build-verification.txt
  EXIT_CODE=0
else
  echo "✗ FAIL: One or more verification checks failed" >> ../.planning/phases/13-video-tutorials/build-verification.txt
  echo "" >> ../.planning/phases/13-video-tutorials/build-verification.txt
  echo "Phase 13 build verification: FAILED" >> ../.planning/phases/13-video-tutorials/build-verification.txt
  EXIT_CODE=1
fi

cat ../.planning/phases/13-video-tutorials/build-verification.txt

exit $EXIT_CODE
```

Why <5 min matters: The build time constraint ensures CI/CD efficiency and prevents deployment bottlenecks. With video caching (from Plan 13-01), the build should complete in ~2-3 minutes since videos don't re-render. If videos were re-rendered every build, it would take 15-25 minutes and violate the constraint.

If build fails: Check that videos are cached properly. Run `bun run scripts/render-videos.ts` manually and verify it shows "cached" messages for all three videos.
  </action>
  <verify>
```bash
test -f .planning/phases/13-video-tutorials/build-verification.txt && echo "✓ Verification log created"

# Check for PASS markers
grep -q "✓ PASS.*TypeScript" .planning/phases/13-video-tutorials/build-verification.txt && echo "✓ TypeScript passing"
grep -q "✓ PASS.*Biome" .planning/phases/13-video-tutorials/build-verification.txt && echo "✓ Biome passing"
grep -q "✓ PASS.*Build successful" .planning/phases/13-video-tutorials/build-verification.txt && echo "✓ Build passing"
grep -q "✓ PASS.*backend tests" .planning/phases/13-video-tutorials/build-verification.txt && echo "✓ Backend tests passing"

# Verify build duration
grep "Build duration:" .planning/phases/13-video-tutorials/build-verification.txt | grep -oE "[0-9]+" | awk '{if ($1 < 300) print "✓ Build under 5 min (" $1 "s)"}'
```
  </verify>
  <done>
- All five BUILD requirements verified (BUILD-01 through BUILD-05)
- TypeScript compilation succeeds with zero errors
- Biome linting passes with zero errors
- Full build completes in <5 minutes (videos cached, not re-rendered)
- Backend pytest suite passes completely
- build-verification.txt documents all results with timestamps
- Phase 13 ready for completion
  </done>
</task>

</tasks>

<verification>
1. Verify caption files exist and format:
   ```bash
   for vtt in docs/public/videos/*.vtt; do
     head -n 5 "$vtt"
     echo "---"
   done
   # Should show WEBVTT header and timestamped captions
   ```

2. Verify VideoPlayer component:
   ```bash
   cd docs
   grep -A 10 "export function VideoPlayer" components/VideoPlayer.tsx
   # Should show responsive styling and caption track
   ```

3. Verify video embeds in documentation:
   ```bash
   grep -c "VideoPlayer" docs/content/docs/tutorials/videos.mdx
   # Should show: 3
   ```

4. Verify build completion time:
   ```bash
   grep "Build duration:" .planning/phases/13-video-tutorials/build-verification.txt
   # Should show <300s (5 minutes)
   ```

5. Verify all verification checks passed:
   ```bash
   grep "Phase 13 build verification" .planning/phases/13-video-tutorials/build-verification.txt
   # Should show: SUCCESS
   ```

6. Test video accessibility in browser (manual):
   ```bash
   cd docs
   bun run dev
   # Visit http://localhost:3000/docs/tutorials/videos
   # Enable captions via CC button
   # Verify responsive scaling on mobile viewport
   ```
</verification>

<success_criteria>
- Three WebVTT caption files created with synchronized timestamps
- VideoPlayer component renders responsively (100% width, maintains 16:9 aspect)
- Captions enabled by default via `<track default>` attribute
- Videos embedded in docs/tutorials/videos.mdx with descriptions
- Navigation includes tutorials/videos page
- TypeScript compilation passes (BUILD-01)
- Biome linting passes zero errors (BUILD-02)
- Full build completes in <5 minutes with video caching (BUILD-03)
- Backend tests pass completely (BUILD-04)
- Zero errors across all checks (BUILD-05)
- build-verification.txt documents complete results
- WCAG 2.1 Level A compliance for video accessibility
- Videos scale properly on mobile without horizontal scroll
</success_criteria>

<output>
After completion, create `.planning/phases/13-video-tutorials/13-03-SUMMARY.md` documenting:
- Caption file creation approach (manual vs Whisper)
- VideoPlayer component features and accessibility
- Build verification results with timing breakdown
- Total file sizes (videos + captions)
- Decision on Vercel Blob migration (if total >100MB)
- Known issues or future improvements
- Confirmation that all VIDEO-01 through VIDEO-08 requirements met
- Confirmation that all BUILD-01 through BUILD-05 requirements met
</output>
