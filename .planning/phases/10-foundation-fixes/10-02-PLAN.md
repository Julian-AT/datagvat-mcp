---
phase: 10-foundation-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/lib/source.ts
  - docs/app/global.css
  - docs/tailwind.config.ts
autonomous: true

must_haves:
  truths:
    - "All internal documentation links navigate correctly without 404 errors"
    - "Styling is consistent across all pages with proper Tailwind CSS theming"
    - "Page navigation works for both /en and /de routes"
  artifacts:
    - path: "docs/lib/source.ts"
      provides: "Page tree and source configuration for both locales"
      min_lines: 10
    - path: "docs/app/global.css"
      provides: "Tailwind directives and Fumadocs styling"
      min_lines: 5
      contains: "@tailwind"
    - path: "docs/tailwind.config.ts"
      provides: "Tailwind configuration with Fumadocs presets"
      min_lines: 10
  key_links:
    - from: "docs/app/[lang]/docs/layout.tsx"
      to: "docs/lib/source.ts"
      via: "source.pageTree import"
      pattern: "source\\.pageTree"
    - from: "docs/app/[lang]/layout.tsx"
      to: "docs/app/global.css"
      via: "CSS import"
      pattern: "import.*global\\.css"
---

<objective>
Fix navigation links and ensure consistent styling across documentation pages.

Purpose: Ensures all internal links work correctly and Tailwind CSS styling is properly configured for Fumadocs UI components.

Output: Working page navigation, consistent styling, proper Tailwind CSS integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/lib/source.ts
@docs/app/global.css
@docs/source.config.ts
@docs/app/[lang]/docs/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix source.ts to properly load page trees for both locales</name>
  <files>docs/lib/source.ts</files>
  <action>
Verify and fix docs/lib/source.ts to ensure:

1. Uses loader() from fumadocs-core/source with proper i18n parameter
2. Imports docs config from @/source.config
3. Creates source object with pageTree for both 'en' and 'de' locales
4. Exports getPage() and getPages() functions for content retrieval

Expected pattern (from Fumadocs docs):
```typescript
import { loader } from 'fumadocs-core/source';
import { docs } from '@/source.config';
import { i18n } from '@/lib/i18n';

export const source = loader({
  source: docs,
  i18n,
  url: (slugs) => `/${slugs.join('/')}`,
});
```

Read current source.ts and verify it matches this pattern. If not, update to use proper loader() with i18n configuration.

The loader automatically handles both locales based on i18n config (languages: ['en', 'de']).
  </action>
  <verify>
1. source.ts imports loader from fumadocs-core/source
2. Passes i18n config to loader()
3. source.pageTree exists and has both 'en' and 'de' keys
4. TypeScript compiles without errors
  </verify>
  <done>
source.ts properly configured with i18n loader. Requirement FOUND-03 partially satisfied (page tree structure correct).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix Tailwind CSS configuration for Fumadocs</name>
  <files>docs/tailwind.config.ts, docs/app/global.css</files>
  <action>
Check and fix Tailwind CSS setup for Fumadocs UI:

**1. Verify docs/tailwind.config.ts:**
- Uses Fumadocs preset: createPreset() from fumadocs-ui/tailwind-plugin
- Has proper content paths for scanning components
- Extends Fumadocs theme configuration

Expected structure:
```typescript
import { createPreset } from 'fumadocs-ui/tailwind-plugin';
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './content/**/*.{md,mdx}',
    './mdx-components.tsx',
    './node_modules/fumadocs-ui/dist/**/*.js',
  ],
  presets: [createPreset()],
};

export default config;
```

Create this file if it doesn't exist. If it exists, verify it follows this pattern.

**2. Verify docs/app/global.css:**
- Contains Tailwind directives: @tailwind base; @tailwind components; @tailwind utilities;
- No conflicting CSS that overrides Fumadocs theming

Expected content:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

Fix if current global.css is missing directives or has conflicts.

Note: Tailwind CSS v4 uses CSS-based config but we're using v3 with Fumadocs (per STATE.md decision). Ensure compatibility.
  </action>
  <verify>
1. tailwind.config.ts exists with createPreset()
2. Content paths include fumadocs-ui dist
3. global.css has @tailwind directives
4. No console errors about missing Tailwind classes
5. npm run build succeeds with Tailwind compilation
  </verify>
  <done>
Tailwind CSS properly configured with Fumadocs preset. Requirement FOUND-04 satisfied (consistent styling).
  </done>
</task>

<task type="auto">
  <name>Task 3: Build and verify navigation + styling works</name>
  <files>None (verification only)</files>
  <action>
Build and verify all fixes:

1. Run full build: `cd docs && npm run build`
2. Check for:
   - No broken links warnings
   - Tailwind CSS compilation success
   - All pages rendered for both locales
   - No missing component errors

3. Verify build output:
   - Static pages generated in .next/server/app/[lang]/docs/
   - CSS bundle includes Fumadocs theming
   - Page tree properly structured

If build fails:
- Check error messages for missing imports
- Verify all Fumadocs UI components are properly installed
- Fix any TypeScript errors
  </action>
  <verify>
1. cd docs && npm run build completes successfully
2. No warnings about broken links
3. Tailwind CSS compiles without errors
4. Page tree generated for both en/ and de/ routes
5. .next/static contains compiled CSS bundle
  </verify>
  <done>
Build succeeds, navigation works, styling consistent. Requirements FOUND-03 and FOUND-04 fully satisfied.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docs/lib/source.ts uses loader() with i18n config
- [ ] docs/tailwind.config.ts has Fumadocs preset
- [ ] docs/app/global.css has Tailwind directives
- [ ] cd docs && npm run build succeeds
- [ ] No broken link warnings in build output
- [ ] CSS bundle includes Fumadocs theming
</verification>

<success_criteria>
- All tasks completed
- Build succeeds with no navigation or styling errors
- Tailwind CSS properly configured with Fumadocs
- Page tree loads correctly for both locales
- Requirements FOUND-03 and FOUND-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-fixes/10-02-SUMMARY.md`
</output>
