---
phase: 20-guides-and-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guides/searching.mdx
  - docs/guides/data-preview.mdx
  - docs/guides/quality-metrics.mdx
autonomous: true

must_haves:
  truths:
    - "User finds guide for accomplishing specific search task (not tool reference)"
    - "User switches between Basic and Advanced tabs to match their expertise level"
    - "User follows task-oriented structure ('Finding Datasets' not 'search_datasets Guide')"
    - "User sees error handling examples for common failures"
    - "User views parameter documentation with types using TypeTable"
  artifacts:
    - path: "docs/guides/searching.mdx"
      provides: "Task-oriented search guide with Basic/Advanced progressive disclosure"
      min_lines: 200
    - path: "docs/guides/data-preview.mdx"
      provides: "Schema inspection and data preview guide with error handling"
      min_lines: 150
    - path: "docs/guides/quality-metrics.mdx"
      provides: "Quality scoring and related datasets analysis guide"
      min_lines: 150
  key_links:
    - from: "docs/guides/searching.mdx"
      to: "Tabs component with groupId='search-complexity'"
      via: "progressive disclosure pattern"
      pattern: "groupId=\"search-complexity\""
    - from: "docs/guides/data-preview.mdx"
      to: "TypeTable component"
      via: "parameter documentation"
      pattern: "<TypeTable.*type=\\{\\{"
    - from: "docs/guides/quality-metrics.mdx"
      to: "Error handling examples"
      via: "QUAL-04 requirement"
      pattern: "Common Errors|Error handling|try.*except"
---

<objective>
Create task-oriented guides for searching, data preview, and quality analysis with progressive disclosure serving both analysts and developers.

Purpose: Enable users to accomplish specific tasks (finding datasets, inspecting data, assessing quality) through goal-oriented guides rather than tool-centric reference documentation. Guides follow Diataxis how-to pattern with Basic/Advanced tabs for progressive disclosure.

Output: 3 comprehensive guide files covering GUIDE-01, GUIDE-02, GUIDE-03 requirements with consistent task-oriented structure, Tabs components, TypeTable parameter documentation, and error handling examples.
</objective>

<execution_context>
@C:\Users\travis\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\travis\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\STATE.md
@C:\GitHub\datagvat-mcp\.planning\REQUIREMENTS.md
@C:\GitHub\datagvat-mcp\.planning\phases\20-guides-and-workflows\20-RESEARCH.md
@C:\GitHub\datagvat-mcp\.planning\phases\18-documentation-foundation\18-01-SUMMARY.md
@C:\GitHub\datagvat-mcp\.planning\phases\19-getting-started-content\19-01-SUMMARY.md
@C:\GitHub\datagvat-mcp\docs\guides\meta.json
</context>

<tasks>

<task type="auto">
  <name>Create searching.mdx - Task-Oriented Search Guide</name>
  <files>docs/guides/searching.mdx</files>
  <action>
Create comprehensive search guide (GUIDE-01) following Diataxis how-to pattern and task-oriented structure from 20-RESEARCH.md Pattern 1.

**Structure:**
```markdown
---
title: Finding Datasets
description: Locate datasets matching your search criteria using text queries and filters
---

# Finding Datasets

Discover datasets by searching with natural language, keywords, or structured filters.

## When to Use This Guide

- You need to locate datasets about a specific topic
- You want to filter by theme, format, or publisher
- You need to refine broad search results

## Prerequisites

- [ ] Austria MCP connected in Claude Desktop
- [ ] Understanding of your search requirements (topic, format preferences)

## Quick Search

[Natural language approach - Basic tab]

## Filtered Search

<Tabs items={['Basic', 'Advanced']} groupId="search-complexity" persist>
  <Tab value="Basic">
    ### Ask Claude with Requirements

    Tell Claude your filters in natural language:
    "Find CSV datasets about health from Vienna"

    Claude uses semantic_search_datasets or search_datasets automatically.

    **Try it:**
    - "Show me environment datasets"
    - "Find recent population data in JSON format"
    - "Search for social datasets from Austrian government"
  </Tab>

  <Tab value="Advanced">
    ### Direct API Call with Faceted Filters

    ```python
    search_datasets(
        query="population",
        themes=["SOCI"],
        formats=["CSV", "JSON"],
        publishers=["stadt-wien"],
        min_date="2024-01-01",
        boost_quality=True,
        sort_by="modified_desc",
        limit=50
    )
    ```

    **Parameters:**

    <TypeTable type={{
      query: {
        type: "string",
        description: "Search query for titles, descriptions, keywords. Supports fuzzy matching (~), wildcards (*), phrases (quotes)",
        default: "None"
      },
      themes: {
        type: "string[]",
        description: "Filter by EU DCAT-AP theme codes: AGRI, ECON, EDUC, ENER, ENVI, GOVE, HEAL, INTR, JUST, REGI, SOCI, TECH, TRAN",
        default: "None"
      },
      formats: {
        type: "string[]",
        description: "Filter by format: CSV, JSON, XML, PDF, XLS, etc.",
        default: "None"
      },
      publishers: {
        type: "string[]",
        description: "Filter by publisher ID (use autocomplete_suggestions to find IDs)",
        default: "None"
      },
      boost_quality: {
        type: "boolean",
        description: "Prioritize datasets with quality score >80 (2x boost), 60-80 (1.5x boost)",
        default: false
      }
    }} />

    **Returns:**

    ```json
    {
      "results": [...],
      "count": 42,
      "facets": {
        "themes": {"SOCI": 30, "HEAL": 12},
        "formats": {"CSV": 25, "JSON": 17}
      }
    }
    ```

    **Error Handling:**

    ```python
    try:
        results = search_datasets(query="population")
    except ToolError as e:
        print(f"Search failed: {e}")
        # API may be temporarily unavailable
    ```
  </Tab>
</Tabs>

## Semantic Search for Natural Queries

<Tabs items={['Basic', 'Advanced']} groupId="search-complexity" persist>
  <Tab value="Basic">
    ### Natural Language Queries

    Use complete questions or descriptions:

    "Find datasets about Vienna's air quality monitoring stations"

    Claude automatically expands query with synonyms and related themes.

    **When to use semantic search:**
    - You have a question in natural language
    - You want related datasets (not exact keyword matches)
    - You need multi-language support (German/English)
  </Tab>

  <Tab value="Advanced">
    ### Direct Semantic Search Call

    ```python
    semantic_search_datasets(
        natural_query="climate data for Vienna",
        formats=["CSV"],
        boost_quality=True
    )
    ```

    **What happens:**
    1. Language detection (German vs English)
    2. Query expansion via LLM (adds synonyms, related themes)
    3. Standard search with expanded terms
    4. Quality boost applied if enabled

    **Response includes expansion info:**

    ```json
    {
      "results": [...],
      "count": 15,
      "expansion_info": {
        "detected_language": "en",
        "semantic_themes": ["ENVI"],
        "confidence": 0.85
      }
    }
    ```

    **Error Handling:**

    If semantic expansion fails, falls back to original query:

    ```json
    {
      "results": [...],
      "expansion_info": {
        "fallback": true,
        "reason": "Low confidence expansion"
      }
    }
    ```
  </Tab>
</Tabs>

## Troubleshooting

### Search Returns No Results

**Symptom:** Count is 0, no datasets found

**Causes:**
- Query too specific
- Filters too restrictive
- Spelling errors in query

**Solutions:**
1. Try broader query terms
2. Remove format/publisher filters
3. Check theme codes (must be uppercase: SOCI not soci)
4. Use semantic_search_datasets for query expansion

### Semantic Search Returns Irrelevant Results

**Symptom:** Results don't match query intent

**Cause:** Language detection incorrect or semantic expansion too broad

**Solutions:**
1. Check `expansion_info.detected_language` in response
2. If wrong language, use direct `search_datasets()` instead
3. Add explicit theme filters to constrain expansion
4. Use more specific query terms

### Quality Boost Returns No Results

**Symptom:** Search with boost_quality=True returns empty

**Cause:** Quality filter too strict for domain

**Solutions:**
1. Try search without boost_quality first
2. Review quality scores of all results
3. Lower quality threshold expectations for niche domains

## Next Steps

- **[Quality Metrics Guide](/guides/quality-metrics)** - Understand quality scoring
- **[Data Preview Guide](/guides/data-preview)** - Inspect data before downloading
- **[Dataset Discovery Workflow](/workflows/discovery)** - Complete search-to-download workflow
- **[Search Tools Reference](/tools#search-tools)** - Complete API documentation
```

**Target:** 200-250 lines covering basic search, semantic search, faceted filtering with Basic/Advanced tabs throughout. Follow 20-RESEARCH.md Pattern 1 (task-oriented structure) and Pattern 2 (progressive disclosure).
  </action>
  <verify>
```bash
cd C:/GitHub/datagvat-mcp/docs/guides && \
  grep -c "groupId=\"search-complexity\"" searching.mdx && \
  grep -c "<TypeTable" searching.mdx && \
  grep -c "Error Handling" searching.mdx && \
  wc -l searching.mdx
```

Expected:
- 2+ instances of groupId="search-complexity" (at least 2 tab sections)
- 1+ TypeTable components (parameter documentation)
- 2+ error handling sections (Advanced tabs)
- 200-250 lines total
  </verify>
  <done>
searching.mdx exists with task-oriented title ("Finding Datasets"), 2+ Tabs sections with search-complexity groupId, TypeTable for parameters, error handling examples in Advanced tabs, troubleshooting section, 200+ lines
  </done>
</task>

<task type="auto">
  <name>Create data-preview.mdx - Schema and Data Inspection Guide</name>
  <files>docs/guides/data-preview.mdx</files>
  <action>
Create data preview guide (GUIDE-02) with schema inspection and data preview patterns.

**Structure:**
```markdown
---
title: Previewing Data Structures
description: Inspect schemas and sample data before downloading full datasets
---

# Previewing Data Structures

Verify data structure, column types, and content before committing to full dataset download.

## When to Use This Guide

- You need to verify required columns exist
- You want to check data types before processing
- You need to estimate data quality from samples
- You want to avoid downloading incompatible formats

## Prerequisites

- [ ] Dataset ID or download URL
- [ ] Understanding of your data requirements (required columns, types)

## Quick Schema Preview

<Tabs items={['Basic', 'Advanced']} groupId="preview-complexity" persist>
  <Tab value="Basic">
    ### Ask Claude to Preview

    "Show me the schema for dataset [dataset-id]"

    Claude fetches distributions, selects downloadable format, previews schema automatically.

    **What you'll see:**
    - Column names
    - Inferred data types
    - Sample values
    - Row count estimate
  </Tab>

  <Tab value="Advanced">
    ### Direct Schema Preview

    ```python
    # First, get download URL
    distributions = get_dataset_distributions(dataset_id="dataset-123")
    csv_url = next(d['downloadURL'] for d in distributions
                   if d.get('format') == 'CSV')

    # Preview schema
    schema = preview_schema(url=csv_url, format="csv")
    ```

    **Parameters:**

    <TypeTable type={{
      url: {
        type: "string",
        description: "HTTPS URL to dataset file (from distributions)",
        required: true
      },
      format: {
        type: "string",
        description: "File format: csv, json, xml. Auto-detected from URL if omitted",
        default: "auto-detect"
      },
      max_bytes: {
        type: "integer",
        description: "Maximum bytes to fetch for preview (default 64KB, max 512KB)",
        default: 65536
      }
    }} />

    **Returns:**

    ```json
    {
      "url": "https://data.wien.gv.at/.../data.csv",
      "format": "csv",
      "partial_fetch": true,
      "bytes_fetched": 65536,
      "columns": [
        {
          "name": "year",
          "type": "integer",
          "sample_values": [2022, 2023, 2024]
        },
        {
          "name": "region",
          "type": "string",
          "sample_values": ["Innere Stadt", "Leopoldstadt"]
        }
      ]
    }
    ```

    **Error Handling:**

    **NetworkError:**
    ```json
    {"error": "NetworkError", "message": "Failed to fetch URL"}
    ```
    Solution: URL may be stale, fetch fresh distributions

    **FormatError:**
    ```json
    {"error": "FormatError", "message": "Could not detect delimiter"}
    ```
    Solution: Specify format explicitly: `format="csv"`
  </Tab>
</Tabs>

## Validating Required Columns

[Task-oriented section for column validation]

<Tabs items={['Basic', 'Advanced']} persist groupId="preview-complexity">
  <Tab value="Basic">
    Ask Claude: "Does dataset [id] have columns X, Y, Z?"
  </Tab>

  <Tab value="Advanced">
    ```python
    required_columns = ["year", "region", "cases"]
    actual_columns = [c['name'] for c in schema['columns']]

    missing = set(required_columns) - set(actual_columns)
    if missing:
        print(f"Missing columns: {missing}")
    else:
        print("All required columns present")

    # Verify types
    year_col = next(c for c in schema['columns'] if c['name'] == 'year')
    if year_col['type'] != 'integer':
        print("Warning: Year column not integer type")
    ```
  </Tab>
</Tabs>

## Previewing Data Samples

[Section on preview_data for row samples]

<Tabs items={['Basic', 'Advanced']} persist groupId="preview-complexity">
  <Tab value="Basic">
    "Show me sample rows from dataset [id]"
  </Tab>

  <Tab value="Advanced">
    ```python
    data = preview_data(url=csv_url, format="csv", max_rows=10)
    ```

    **Parameters:**

    <TypeTable type={{
      url: {
        type: "string",
        description: "HTTPS URL to dataset file",
        required: true
      },
      format: {
        type: "string",
        description: "File format: csv, json, xml",
        default: "auto-detect"
      },
      max_rows: {
        type: "integer",
        description: "Maximum rows to return (1-100)",
        default: 10
      }
    }} />

    **Returns:**

    ```json
    {
      "url": "...",
      "format": "csv",
      "rows": [
        {"year": 2024, "region": "Wien", "cases": 150},
        {"year": 2024, "region": "Graz", "cases": 120}
      ],
      "row_count": 10,
      "estimated_total_rows": 1000
    }
    ```
  </Tab>
</Tabs>

## Troubleshooting

### Preview Fails on Valid URL

**Symptom:** URL works in browser but preview fails

**Cause:** Server doesn't support HTTP Range requests

**Solutions:**
1. Preview falls back to full download (may be slow)
2. Use smaller max_bytes parameter
3. Check if different format available

### Type Inference Incorrect

**Symptom:** Column shows "string" but contains numbers

**Cause:** Sample rows have inconsistent types or headers

**Solutions:**
1. Increase max_bytes to sample more rows
2. Check if first rows are headers or metadata
3. Use preview_data to see actual values

### Cannot Detect Format

**Symptom:** FormatError on auto-detection

**Cause:** Unusual file format or delimiter

**Solutions:**
1. Specify format explicitly: `format="csv"`
2. Check file extension matches actual content
3. Download sample manually to inspect

## Next Steps

- **[Quality Metrics Guide](/guides/quality-metrics)** - Assess data quality
- **[Searching Guide](/guides/searching)** - Find datasets to preview
- **[Dataset Discovery Workflow](/workflows/discovery)** - Complete workflow including preview
- **[Preview Tools Reference](/tools#preview-tools)** - Complete API documentation
```

**Target:** 150-200 lines covering schema preview, data preview, column validation with Basic/Advanced tabs.
  </action>
  <verify>
```bash
cd C:/GitHub/datagvat-mcp/docs/guides && \
  grep -c "groupId=\"preview-complexity\"" data-preview.mdx && \
  grep -c "<TypeTable" data-preview.mdx && \
  grep -c "Error Handling\|Common Errors" data-preview.mdx && \
  wc -l data-preview.mdx
```

Expected:
- 3+ instances of groupId="preview-complexity"
- 2+ TypeTable components
- 3+ error handling sections
- 150-200 lines total
  </verify>
  <done>
data-preview.mdx exists with task-oriented title, 3+ Tabs sections with preview-complexity groupId, TypeTable for parameters, error handling examples, troubleshooting section, 150+ lines
  </done>
</task>

<task type="auto">
  <name>Create quality-metrics.mdx - Quality Scoring and Related Datasets Guide</name>
  <files>docs/guides/quality-metrics.mdx</files>
  <action>
Create quality analysis guide (GUIDE-03) covering quality scoring and finding related datasets.

**Structure:**
```markdown
---
title: Assessing Dataset Quality
description: Evaluate dataset quality and discover related datasets
---

# Assessing Dataset Quality

Determine dataset reliability through quality scores and discover related datasets for comparison.

## When to Use This Guide

- You need to verify data quality before using in research/production
- You want to understand what DQV quality scores mean
- You need to find similar or related datasets
- You want to compare quality across dataset options

## Prerequisites

- [ ] Dataset ID from search results
- [ ] Understanding of your quality requirements (research vs exploration)

## Understanding Quality Scores

[Explanation of DQV scoring 0-100]

Quality scores (0-100) measure metadata completeness, update frequency, standards compliance.

**Score ranges:**
- 85-100: Excellent (research, citations)
- 70-84: Good (production applications)
- 50-69: Acceptable (exploratory analysis)
- <50: Poor (verify carefully)

**Score components:**
- Completeness (40 points): Title, description, license, contact, keywords, themes, spatial, temporal coverage
- Timeliness (20 points): Recent updates, modification date
- Compliance (20 points): DCAT-AP standard compliance
- Accessibility (20 points): Valid download links, open formats

## Quick Quality Check

<Tabs items={['Basic', 'Advanced']} groupId="quality-complexity" persist>
  <Tab value="Basic">
    ### Ask Claude for Quality Assessment

    "What's the quality score for dataset [dataset-id]?"

    Claude runs analyze_dataset_quality and explains results.

    **What you'll learn:**
    - Overall quality score (0-100)
    - What metadata is present/missing
    - Whether quality meets your needs
  </Tab>

  <Tab value="Advanced">
    ### Direct Quality Analysis

    ```python
    quality = analyze_dataset_quality(dataset_id="dataset-123")
    ```

    **Parameters:**

    <TypeTable type={{
      dataset_id: {
        type: "string",
        description: "Dataset identifier from search results",
        required: true
      }
    }} />

    **Returns:**

    ```json
    {
      "dataset_id": "dataset-123",
      "metadata": {
        "has_title": true,
        "has_description": true,
        "has_license": true,
        "has_contact": false,
        "has_keywords": true,
        "has_themes": true,
        "has_spatial": true,
        "has_temporal": false,
        "completeness_score": 75
      },
      "metrics": {
        "overall_score": 82,
        "completeness": 75,
        "timeliness": 12,
        "compliance": 18,
        "accessibility": 18
      },
      "degraded": false
    }
    ```

    **Quality decision matrix:**

    | Score | Use For | Verification |
    |-------|---------|--------------|
    | 85-100 | Research, citations | Proceed with confidence |
    | 70-84 | Production apps | Verify critical fields present |
    | 50-69 | Exploration | Check schema, preview data |
    | <50 | Caution | Manual verification required |

    **Error Handling:**

    **Degraded Service:**
    ```python
    if quality.get('degraded'):
        print("Quality service degraded - using cached metrics")
        # Verify critical fields manually
    ```

    **Dataset Not Found:**
    ```json
    {"error": "NotFoundError", "message": "Dataset not found"}
    ```
    Solution: Dataset ID may be incorrect or stale
  </Tab>
</Tabs>

## Quality-Aware Search

<Tabs items={['Basic', 'Advanced']} persist groupId="quality-complexity">
  <Tab value="Basic">
    ### Search with Quality Preference

    "Find high-quality datasets about health"

    Claude enables boost_quality automatically when quality is mentioned.
  </Tab>

  <Tab value="Advanced">
    ### Quality Boost in Search

    ```python
    search_datasets(
        query="health",
        boost_quality=True
    )
    ```

    **What happens:**
    - Datasets with score >80 get 2x relevance boost
    - Datasets with score 60-80 get 1.5x relevance boost
    - Results sorted by boosted relevance

    **Note:** Quality boost only active when query is provided (not on facet-only searches)
  </Tab>
</Tabs>

## Finding Related Datasets

<Tabs items={['Basic', 'Advanced']} persist groupId="quality-complexity">
  <Tab value="Basic">
    "Find datasets related to [dataset-id]"

    Claude finds datasets with similar themes, keywords, or same publisher.
  </Tab>

  <Tab value="Advanced">
    ```python
    related = find_related_datasets(
        dataset_id="dataset-123",
        min_score=20
    )
    ```

    **Parameters:**

    <TypeTable type={{
      dataset_id: {
        type: "string",
        description: "Reference dataset ID",
        required: true
      },
      min_score: {
        type: "integer",
        description: "Minimum similarity score (0-100, default 20)",
        default: 20
      }
    }} />

    **Scoring algorithm:**
    - Theme match: 30 points per shared theme (max 60)
    - Keyword match: 10 points per shared keyword (max 30)
    - Same publisher: 15 points bonus

    **Returns:**

    ```json
    {
      "reference_dataset_id": "dataset-123",
      "related_datasets": [
        {
          "id": "dataset-456",
          "title": "Related Dataset",
          "similarity_score": 75,
          "match_reasons": {
            "theme_matches": ["HEAL", "SOCI"],
            "keyword_matches": ["health", "statistics"],
            "same_publisher": true
          }
        }
      ]
    }
    ```

    **Error Handling:**

    ```python
    try:
        related = find_related_datasets(dataset_id="dataset-123")
    except ToolError as e:
        print(f"Related search failed: {e}")
    ```
  </Tab>
</Tabs>

## Troubleshooting

### All Datasets Have Low Quality Scores

**Symptom:** No results with score >70

**Cause:** Domain has sparse metadata

**Solutions:**
1. Lower threshold to 60 for acceptable quality
2. Check what metadata is missing (use analyze_dataset_quality)
3. Verify critical fields (license, description) present even if score low

### Quality Analysis Returns Degraded

**Symptom:** degraded: true in response

**Cause:** Quality service temporarily unavailable

**Solutions:**
1. Response includes cached metrics (may be stale)
2. Verify critical fields manually from dataset metadata
3. Retry after few minutes if current metrics needed

### Find Related Returns No Results

**Symptom:** Empty related_datasets array

**Cause:** min_score too high or dataset very unique

**Solutions:**
1. Lower min_score parameter (try 10-15)
2. Check reference dataset has themes and keywords
3. Search by same publisher or themes instead

## Next Steps

- **[Searching Guide](/guides/searching)** - Find datasets with quality filtering
- **[Data Preview Guide](/guides/data-preview)** - Verify data structure
- **[Quality Assessment Workflow](/workflows/quality-assessment)** - Complete quality evaluation workflow
- **[Analysis Tools Reference](/tools#analysis-tools)** - Complete API documentation
```

**Target:** 150-200 lines covering quality scoring, interpretation, quality-aware search, related datasets with Basic/Advanced tabs.
  </action>
  <verify>
```bash
cd C:/GitHub/datagvat-mcp/docs/guides && \
  grep -c "groupId=\"quality-complexity\"" quality-metrics.mdx && \
  grep -c "<TypeTable" quality-metrics.mdx && \
  grep -c "Error Handling\|Troubleshooting" quality-metrics.mdx && \
  wc -l quality-metrics.mdx
```

Expected:
- 3+ instances of groupId="quality-complexity"
- 3+ TypeTable components
- 4+ error handling sections
- 150-200 lines total
  </verify>
  <done>
quality-metrics.mdx exists with task-oriented title, 3+ Tabs sections with quality-complexity groupId, TypeTable for parameters, error handling examples, troubleshooting section, 150+ lines
  </done>
</task>

</tasks>

<verification>
After completion:

1. **Component Usage:**
```bash
cd C:/GitHub/datagvat-mcp/docs/guides && \
  grep -r "groupId=" *.mdx | wc -l && \
  grep -r "<TypeTable" *.mdx | wc -l && \
  grep -r "Error Handling\|Troubleshooting" *.mdx | wc -l
```
Expected: 8+ groupId usages, 6+ TypeTable components, 10+ error sections

2. **Task-Oriented Structure:**
```bash
grep -r "^# Finding\|^# Previewing\|^# Assessing" docs/guides/*.mdx
```
Expected: Task-oriented titles (verb + object), not tool names

3. **Progressive Disclosure:**
```bash
grep -r "persist" docs/guides/*.mdx | wc -l
```
Expected: All Tabs have persist attribute for state preservation

4. **File Creation:**
```bash
ls docs/guides/searching.mdx docs/guides/data-preview.mdx docs/guides/quality-metrics.mdx
```
Expected: All 3 files exist

5. **Build Validation:**
```bash
cd docs && npm run build 2>&1 | grep -i "error\|warn" | head -20
```
Expected: Clean build with no errors
</verification>

<success_criteria>
1. searching.mdx covers basic search, semantic search, faceted filtering with Basic/Advanced tabs (GUIDE-01 satisfied)
2. data-preview.mdx covers schema inspection and data preview with error handling (GUIDE-02 satisfied)
3. quality-metrics.mdx covers quality scoring and related datasets (GUIDE-03 satisfied)
4. All guides use task-oriented structure ("Finding" not "search_datasets") (GUIDE-06 satisfied)
5. All guides have Basic/Advanced progressive disclosure with persistent tabs (GUIDE-05 satisfied)
6. TypeTable components used for parameter documentation (QUAL-03 satisfied)
7. Error handling examples in Advanced tabs (QUAL-04 satisfied)
8. Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-guides-and-workflows/20-01-SUMMARY.md` following summary template.
</output>
