---
phase: 19-getting-started-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started/quickstart.mdx
  - docs/getting-started/first-query.mdx
  - docs/getting-started/installation.mdx
autonomous: true

must_haves:
  truths:
    - "User completes first dataset query in under 5 minutes following quickstart"
    - "User sees expected output after each quickstart step"
    - "User understands how to verify successful installation"
    - "First query tutorial shows complete workflow from search to data preview"
  artifacts:
    - path: "docs/getting-started/quickstart.mdx"
      provides: "<5 minute quickstart guide with action-first structure"
      min_lines: 100
    - path: "docs/getting-started/first-query.mdx"
      provides: "Complete first query tutorial with expected outputs"
      min_lines: 150
    - path: "docs/getting-started/installation.mdx"
      provides: "Installation guide with OS-specific tabs and verification steps"
      contains: "Tabs items"
  key_links:
    - from: "docs/getting-started/quickstart.mdx"
      to: "docs/getting-started/installation.mdx"
      via: "Prerequisites section links to installation"
      pattern: "\\[Installation Guide\\]\\(/getting-started/installation\\)"
    - from: "docs/getting-started/quickstart.mdx"
      to: "docs/getting-started/first-query.mdx"
      via: "Next Steps section links to deeper tutorial"
      pattern: "\\[First Query Tutorial\\]\\(/getting-started/first-query\\)"
---

<objective>
Create core Getting Started content that gets new users from zero to first successful Austria MCP query in under 5 minutes, with complete tutorials showing expected outputs at every step.

Purpose: Provide the critical first-touch experience that builds user confidence through guaranteed success, action-first guidance, and verification at each step. This is the difference between users who succeed immediately and users who abandon the product.

Output: Three MDX files following Diataxis tutorial pattern with Fumadocs components (Callout, Tabs, code blocks with syntax highlighting).
</objective>

<execution_context>
@C:\Users\julia\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\julia\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-getting-started-content/19-RESEARCH.md
@.planning/phases/18-documentation-foundation/18-01-SUMMARY.md
@docs/getting-started/index.mdx
@docs/getting-started/installation.mdx
@docs/getting-started/first-query.mdx
@docs/getting-started/meta.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Quickstart Guide (START-01)</name>
  <files>docs/getting-started/quickstart.mdx</files>
  <action>
Create quickstart.mdx following Diataxis tutorial pattern optimized for <5 minute time constraint.

**Structure (from 19-RESEARCH.md Pattern 1):**
- Frontmatter: title "Quickstart Guide", description "Get your first Austria MCP query results in under 5 minutes"
- One-line goal: "Search for Austrian open datasets and see results in under 5 minutes."
- Prerequisites section with checkboxes: Claude Desktop installed, Austria MCP configured, Claude Desktop restarted
- Step 1: Verify Connection (look for MCP server indicator in toolbar)
- Step 2: Run Your First Search (exact query: "Find datasets about Vienna population")
  - Show what happens behind scenes (bullet list)
  - Expected output section with Claude's natural language response example
- Step 3: Get Dataset Details (query: "Show me details for dataset [ID]")
  - Expected output: metadata components listed
- Verify Success section with checkboxes
- What You've Learned section (4 bullet points)
- Next Steps with 3 subsections: Learn More, Common Workflows, Troubleshooting

**Key requirements:**
- Action-first: First action within 2 paragraphs (no long explanations)
- Expected output after EVERY command
- Fumadocs Callout for tips: `<Callout type="info">**First time?** Follow...</Callout>`
- Checkboxes for prerequisites and success criteria
- Links to deeper content (installation, first-query, troubleshooting)
- Copy-paste ready queries (not code blocks, plain text user can type to Claude)
- Total length: ~100-150 lines to support <5 min reading time

**Reference:** 19-RESEARCH.md "Complete Quickstart Page Structure" example (lines 752-869)

**Why quickstart is separate from index.mdx:** index.mdx serves as overview/landing with workflow examples. quickstart.mdx is time-boxed tutorial with guaranteed success path. Different Diataxis patterns.
  </action>
  <verify>
Check quickstart.mdx contains:
- [ ] Frontmatter with title and description
- [ ] Prerequisites section with checkboxes
- [ ] At least 2 steps with "Expected output" subsections
- [ ] Verify Success section with checkboxes
- [ ] Callout component used correctly
- [ ] Links to installation, first-query, troubleshooting
- [ ] File is 100-150 lines (cat quickstart.mdx | wc -l)
  </verify>
  <done>quickstart.mdx created with action-first structure, expected outputs for all steps, and <5 minute reading time verified by line count</done>
</task>

<task type="auto">
  <name>Task 2: Enhance First Query Tutorial (START-03)</name>
  <files>docs/getting-started/first-query.mdx</files>
  <action>
Read existing first-query.mdx (85 lines) and enhance to complete tutorial standard with expected outputs and verification steps.

**Current state:** Basic tutorial exists but likely missing expected output verification and complete workflow.

**Enhancement requirements:**
- Expand to ~150-200 lines for complete tutorial (deeper than quickstart)
- Add "Expected output" section after EVERY step
- Add data preview workflow (Step 3: Get distributions, Step 4: Preview data)
- Show actual JSON response examples (not just descriptions)
- Add Callout warnings for common mistakes
- Add verification checkboxes at end of each major section
- Link back to quickstart as prerequisite
- Link forward to guides for advanced usage

**Structure to follow (19-RESEARCH.md Pattern 1):**
- Prerequisites (with link to quickstart)
- Step 1: Search for datasets (with expected JSON output example)
- Step 2: Get dataset details (with expected JSON output example)
- Step 3: Get dataset distributions (show distribution list)
- Step 4: Preview data (show CSV preview with schema)
- Verify Success section
- What You've Learned section
- Next Steps (links to guides/workflows)

**Expected output examples should use:**
- Code blocks with `json` or `csv` language tags for syntax highlighting
- Real data.gv.at dataset IDs and titles (existing examples in index.mdx)
- Fumadocs code blocks automatically get copy button

**Reference:** 19-RESEARCH.md Pattern 4 "Expected Output Verification" (lines 294-326)

**Do NOT:** Add long explanations (that's for guides). Stay tutorial-focused: action → verify → next action.
  </action>
  <verify>
Check first-query.mdx contains:
- [ ] At least 4 steps (search, details, distributions, preview)
- [ ] "Expected output" subsection after each step
- [ ] Code blocks with json/csv language tags
- [ ] Callout components for tips/warnings
- [ ] Verification checkboxes
- [ ] Links to prerequisite (quickstart) and next steps (guides)
- [ ] File is 150-200 lines (cat first-query.mdx | wc -l)
  </verify>
  <done>first-query.mdx enhanced with complete workflow, expected outputs for all steps, and data preview tutorial completed</done>
</task>

<task type="auto">
  <name>Task 3: Verify Installation.mdx Completeness (START-02)</name>
  <files>docs/getting-started/installation.mdx</files>
  <action>
Read existing installation.mdx (560 lines) and verify it meets START-02 requirements. Add missing elements if needed.

**Verify includes (19-RESEARCH.md Pattern 2 - Installation with OS-Specific Tabs):**
- [ ] Prerequisites section with Python version verification commands
- [ ] OS-specific installation tabs (Tabs with groupId="os" persist)
- [ ] Claude Desktop vs self-hosted configuration options
- [ ] Working directory setup (clone, cd commands)
- [ ] Dependency installation (uv vs pip tabs)
- [ ] Test installation section with expected output
- [ ] Claude Desktop configuration with JSON examples
- [ ] Restart instructions (OS-specific)
- [ ] Verification section (how to check server appears)
- [ ] Next Steps linking to quickstart

**If installation.mdx already has all above:** Add START-02 verification comment to file header and verify Tabs components use correct props:
- `groupId="os"` for OS selection persistence
- `persist` prop for cross-page persistence
- Callout type="warn" for platform-specific gotchas

**If missing elements:** Add them following 19-RESEARCH.md Pattern 2 "Installation Page with OS-Specific Tabs" (lines 873-1093).

**Expected output verification pattern (19-RESEARCH.md Pattern 4):**
After test commands, show:
```
**Expected output:**
```
✅ Server loads successfully
```

**If you see errors:**
- `ModuleNotFoundError: ...` → [Link to troubleshooting]
```

**Reference:** Existing installation.mdx (560 lines suggests comprehensive), 19-RESEARCH.md Pattern 2
  </action>
  <verify>
Check installation.mdx contains:
- [ ] Tabs components with groupId="os" and persist props (grep "Tabs.*groupId" installation.mdx)
- [ ] Expected output after test installation command
- [ ] Callout components for warnings (grep "Callout" installation.mdx)
- [ ] Both uv and pip installation options
- [ ] Claude Desktop config JSON for all 3 OS platforms
- [ ] Verification section at end
- [ ] Links to next steps (quickstart)
  </verify>
  <done>installation.mdx verified complete with OS-specific tabs, expected outputs, and proper component usage. START-02 requirement confirmed satisfied.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Content completeness:**
   - quickstart.mdx exists and is <5 min readable (~100-150 lines)
   - first-query.mdx enhanced to complete tutorial (~150-200 lines)
   - installation.mdx verified comprehensive

2. **Component usage:**
   ```bash
   grep -l "Callout" docs/getting-started/*.mdx
   grep -l "Tabs.*groupId" docs/getting-started/*.mdx
   ```
   Should find Callout in quickstart, first-query, installation
   Should find Tabs in installation

3. **Expected outputs:**
   ```bash
   grep -c "Expected output" docs/getting-started/quickstart.mdx
   grep -c "Expected output" docs/getting-started/first-query.mdx
   ```
   quickstart should have 2+ occurrences
   first-query should have 4+ occurrences

4. **Cross-references:**
   ```bash
   grep "/getting-started/" docs/getting-started/*.mdx | grep -E "(installation|quickstart|first-query|troubleshooting)"
   ```
   Should find links between files

5. **Syntax highlighting:**
   ```bash
   grep -E '```(json|csv|python|bash)' docs/getting-started/*.mdx
   ```
   Should find language-tagged code blocks
</verification>

<success_criteria>
Phase 19 Plan 01 succeeds when:

1. User can follow quickstart.mdx and complete first query in under 5 minutes
2. Every step in quickstart and first-query has expected output shown
3. User can verify installation success using provided commands
4. Installation guide covers both Claude Desktop and self-hosted setup
5. All code blocks use proper syntax highlighting (json, csv, python, bash)
6. Fumadocs components (Callout, Tabs) used correctly with proper props
7. Cross-references connect quickstart → installation, quickstart → first-query, quickstart → troubleshooting
8. No broken internal links in getting-started section
</success_criteria>

<output>
After completion, create `.planning/phases/19-getting-started-content/19-01-SUMMARY.md` following summary template.
</output>
