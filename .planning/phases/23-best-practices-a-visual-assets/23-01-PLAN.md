---
phase: 23-best-practices-a-visual-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/best-practices/quality-interpretation.mdx
  - docs/best-practices/rate-limiting.mdx
  - docs/best-practices/caching-strategies.mdx
  - docs/best-practices/comparison-tables.mdx
  - docs/best-practices/meta.json
autonomous: true

must_haves:
  truths:
    - User interprets DQV quality score and understands component breakdown
    - User implements rate limiting handling with retry strategy
    - User implements caching with TTL for repeated operations
    - User chooses correct tool using comparison tables
  artifacts:
    - path: "docs/best-practices/quality-interpretation.mdx"
      provides: "DQV 8-component scoring explanation with interpretation thresholds"
      min_lines: 200
    - path: "docs/best-practices/rate-limiting.mdx"
      provides: "Rate limit guidance with error handling patterns"
      min_lines: 150
    - path: "docs/best-practices/caching-strategies.mdx"
      provides: "Caching patterns with TTL and LRU examples"
      min_lines: 150
    - path: "docs/best-practices/comparison-tables.mdx"
      provides: "When-to-use decision tables for tool selection"
      min_lines: 150
    - path: "docs/best-practices/meta.json"
      provides: "Navigation with 5 best practice pages"
      contains: "quality-interpretation"
  key_links:
    - from: "docs/best-practices/quality-interpretation.mdx"
      to: "mcp/app/tools/analysis.py"
      via: "references DQV scoring implementation"
      pattern: "analyze_dataset_quality|get_dataset_metrics"
    - from: "docs/best-practices/rate-limiting.mdx"
      to: "middleware retry patterns"
      via: "documents automatic retry behavior"
      pattern: "10 req/s|exponential backoff"
    - from: "docs/best-practices/comparison-tables.mdx"
      to: "docs/api/api/tools.mdx"
      via: "cross-references tool documentation"
      pattern: "search_datasets|search_semantic"
---

<objective>
Create comprehensive best practices guides covering quality interpretation, rate limiting, caching strategies, and tool comparison tables to help users optimize Austria MCP usage and make informed decisions.

Purpose: Enable users to interpret quality metrics, handle API limits gracefully, implement effective caching, and choose appropriate tools for their use cases.

Output: 4 new best practice guides (600+ lines total) with actionable patterns and decision frameworks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-best-practices-a-visual-assets/23-RESEARCH.md
@.planning/phases/20-guides-and-workflows/20-03b-SUMMARY.md
@.planning/phases/21-auto-generated-tools-reference/21-02-SUMMARY.md

# Existing best practices content
@docs/best-practices/optimization.mdx
@docs/best-practices/meta.json

# Quality scoring implementation
@mcp/app/tools/analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Quality Interpretation Guide</name>
  <files>docs/best-practices/quality-interpretation.mdx</files>
  <action>
Create comprehensive guide explaining DQV quality metrics with 8-component breakdown:

**Structure:**
- Understanding Quality Scores (0-100 scale overview)
- Core Metadata (40 points): title, description, publisher
- Discovery Metadata (30 points): keywords, themes, spatial coverage
- Access Metadata (20 points): distributions, license
- Temporal Metadata (10 points): issued date, modified date
- Quality Interpretation Thresholds:
  - 90-100: Excellent (complete metadata, EU portal ready)
  - 70-89: Good (minor gaps, publication-ready)
  - 50-69: Fair (missing key components)
  - 0-49: Poor (insufficient for discovery)
- Taking Action section: How to use get_dataset_metrics for detailed breakdown
- Component-level improvement guidance (which metadata fields to add)
- Code examples showing quality score filtering and interpretation

**Based on:**
- mcp/app/tools/analysis.py implementation (analyze_dataset_quality, get_dataset_metrics)
- W3C DQV vocabulary structure from research
- Existing quality boost patterns from optimization.mdx

**Cross-references:**
- Link to quality-metrics guide in guides section
- Link to analyze_dataset_quality tool in API reference
- Link to workflows using quality thresholds (publication-research, quality-assessment)

**Target:** 200+ lines with detailed component explanations and actionable guidance
  </action>
  <verify>
```bash
# Verify file created with required content
grep -i "DQV\|8-component\|90-100.*Excellent\|70-89.*Good" docs/best-practices/quality-interpretation.mdx

# Verify minimum length
wc -l docs/best-practices/quality-interpretation.mdx  # Should be 200+

# Verify cross-references exist
grep -E "quality-metrics|analyze_dataset_quality|get_dataset_metrics" docs/best-practices/quality-interpretation.mdx
```
  </verify>
  <done>Quality interpretation guide exists with 200+ lines, explains 8-component DQV scoring, provides interpretation thresholds (90-100, 70-89, 50-69, 0-49), includes actionable improvement guidance, and links to relevant tools/guides</done>
</task>

<task type="auto">
  <name>Task 2: Create Rate Limiting Guide</name>
  <files>docs/best-practices/rate-limiting.mdx</files>
  <action>
Create guide documenting rate limiting behavior and handling strategies:

**Structure:**
- Rate Limit Overview: 10 req/s sustained, 20 burst capacity
- Error Response Format (ToolError with retry_after, limit, window fields)
- Automatic Retry Behavior: FastMCP middleware handles 3 attempts with exponential backoff (1-60s)
- Avoiding Rate Limits:
  - Batch operations efficiently (code example)
  - Use pagination wisely (fetch 100 not 10x10)
  - Cache frequent queries
  - Implement request queuing for high-volume scenarios
- Detecting Rate Limit Errors (pattern matching on error messages)
- Custom Retry Strategy (when to implement beyond automatic retries)
- Rate Limit Testing (how to test handling without hitting production limits)

**Code examples:**
- Batch fetching with delay between batches
- Error handling for rate limit exceeded
- Request queue implementation for sustained high volume
- Retry with exponential backoff pattern

**Based on:**
- STATE.md decisions: Rate limit 10 req/s, burst 20, retry 3 attempts
- Research rate limiting patterns
- Existing middleware documentation from Phase 22

**Cross-references:**
- Link to middleware stack documentation
- Link to error-handling guide
- Link to caching-strategies (next guide)

**Target:** 150+ lines with practical rate limit handling patterns
  </action>
  <verify>
```bash
# Verify file created with rate limit details
grep -E "10 req/s|burst.*20|exponential backoff|3 attempts" docs/best-practices/rate-limiting.mdx

# Verify minimum length
wc -l docs/best-practices/rate-limiting.mdx  # Should be 150+

# Verify code examples exist
grep -c "```" docs/best-practices/rate-limiting.mdx  # Should be 8+ (4 code blocks)

# Verify cross-references
grep -E "middleware|error-handling|caching" docs/best-practices/rate-limiting.mdx
```
  </verify>
  <done>Rate limiting guide exists with 150+ lines, documents 10 req/s limit with burst 20, explains automatic retry (3 attempts, exponential backoff), provides batch operation examples, includes error handling patterns, and links to related guides</done>
</task>

<task type="auto">
  <name>Task 3: Create Caching Strategies Guide</name>
  <files>docs/best-practices/caching-strategies.mdx</files>
  <action>
Create guide covering caching patterns for Austria MCP operations:

**Structure:**
- When to Cache: Metadata (stable), quality metrics (expensive), search results (transient)
- When NOT to Cache: Real-time operations, draft management, user-specific data
- TTL-Based Caching:
  - Search results: 5-15 minutes
  - Dataset metadata: 1 hour
  - Quality metrics: 1 hour
  - Vocabularies: 24 hours (rarely change)
- Cache Implementations:
  - Simple dict cache (in-memory, development)
  - TTL cache with expiration (production pattern from optimization.mdx)
  - LRU cache with size limit (memory-bounded)
  - Redis/Memcached integration patterns (external cache)
- Cache Invalidation Strategies:
  - Time-based (TTL expiration)
  - Event-based (after dataset update)
  - Manual invalidation (cache.clear())
- Cache Key Design: Consistent hashing for deterministic keys
- Multi-Level Caching: Memory (L1) + Redis (L2) pattern

**Code examples:**
- TTL cache implementation (from optimization.mdx, expand with more detail)
- LRU cache with OrderedDict (from optimization.mdx)
- Cache decorator pattern for tools
- Redis integration example
- Cache warming strategy for common queries

**Based on:**
- optimization.mdx existing cache patterns (TTL, LRU)
- Research caching strategies section
- Common MCP usage patterns from workflows

**Cross-references:**
- Link to performance optimization guide
- Link to rate-limiting guide (caching reduces rate limit pressure)
- Link to workflows showing cache usage

**Target:** 150+ lines with multiple caching patterns and implementation examples
  </action>
  <verify>
```bash
# Verify file created with caching patterns
grep -E "TTL|LRU|Redis|cache invalidation" docs/best-practices/caching-strategies.mdx

# Verify minimum length
wc -l docs/best-practices/caching-strategies.mdx  # Should be 150+

# Verify multiple cache types documented
grep -c "```python" docs/best-practices/caching-strategies.mdx  # Should be 10+ examples

# Verify TTL recommendations exist
grep -E "5-15 minutes|1 hour|24 hours" docs/best-practices/caching-strategies.mdx

# Verify cross-references
grep -E "optimization|rate-limiting|performance" docs/best-practices/caching-strategies.mdx
```
  </verify>
  <done>Caching strategies guide exists with 150+ lines, documents TTL patterns (search 5-15min, metadata 1hr, vocabularies 24hr), provides LRU and TTL cache implementations, covers cache invalidation strategies, includes Redis integration example, and links to performance and rate-limiting guides</done>
</task>

<task type="auto">
  <name>Task 4: Create Tool Comparison Tables Guide</name>
  <files>docs/best-practices/comparison-tables.mdx</files>
  <action>
Create guide with decision tables helping users choose appropriate tools:

**Structure:**
- Search Strategy Comparison:
  - search_datasets vs search_semantic vs find_similar_datasets
  - Table: Query Type, Speed, Accuracy, Pagination, Facets, Use When
  - Decision guide for each tool
- Quality Boost Decision:
  - When to enable quality_boost (production, research) vs disable (exploration, speed)
  - Table: Scenario, quality_boost value, Why
  - Performance vs relevance tradeoff explanation
- Preview Tools Comparison:
  - preview_schema vs preview_data
  - Table: Data Transfer, Use Case, Speed, Information Gained
  - Schema-first optimization pattern
- Analysis Tools Comparison:
  - get_dataset_metrics vs analyze_dataset_quality
  - Table: Scope, Performance, Output Detail, Use When
  - Detailed vs quick quality check tradeoff
- Management Operations:
  - create_draft vs update_draft vs publish_draft
  - Workflow decision tree for dataset publishing
- Pagination Strategy:
  - Small result sets vs large result sets vs streaming
  - Table: Result Count, Strategy, Memory Usage, Performance

**Tables use markdown format:**
```markdown
| Feature | Option A | Option B | Option C |
|---------|----------|----------|----------|
| ... | ... | ... | ... |
```

**Decision guides follow pattern:**
- Use X when: [3-5 scenarios]
- Don't use X when: [2-3 anti-patterns]
- Consider Y instead if: [alternatives]

**Based on:**
- Research comparison table patterns
- Phase 20 workflow decision matrices
- optimization.mdx performance comparisons
- All 25 MCP tools from API reference

**Cross-references:**
- Link to each tool in API reference
- Link to guides demonstrating tool usage
- Link to workflows showing tool combinations

**Target:** 150+ lines with 6+ comparison tables covering major tool categories
  </action>
  <verify>
```bash
# Verify file created with comparison tables
grep -c "^|" docs/best-practices/comparison-tables.mdx  # Should be 40+ table rows

# Verify minimum length
wc -l docs/best-practices/comparison-tables.mdx  # Should be 150+

# Verify tool comparisons exist
grep -E "search_datasets.*search_semantic|preview_schema.*preview_data|get_dataset_metrics.*analyze_dataset_quality" docs/best-practices/comparison-tables.mdx

# Verify decision guide patterns
grep -c "Use.*when:" docs/best-practices/comparison-tables.mdx  # Should be 6+ tool categories

# Verify cross-references
grep -E "/api/tools|/guides|/workflows" docs/best-practices/comparison-tables.mdx
```
  </verify>
  <done>Comparison tables guide exists with 150+ lines, contains 6+ comparison tables (search strategies, quality boost, preview tools, analysis tools, management ops, pagination), provides decision guides with "Use X when" patterns, includes markdown tables for scannable reference, and cross-references API docs and guides</done>
</task>

<task type="auto">
  <name>Task 5: Update Best Practices Navigation</name>
  <files>docs/best-practices/meta.json</files>
  <action>
Update meta.json to include all 5 best practice pages in logical order:

**Order:**
1. optimization (existing - general performance patterns)
2. quality-interpretation (new - understanding metrics)
3. comparison-tables (new - choosing tools)
4. rate-limiting (new - handling limits)
5. caching-strategies (new - implementing caching)

**Rationale for order:**
- optimization first: broad patterns everyone should know
- quality-interpretation second: foundational concept for many workflows
- comparison-tables third: helps choose right tool before diving deep
- rate-limiting fourth: operational concern after understanding tools
- caching-strategies fifth: advanced optimization technique

Update description in meta.json to reflect comprehensive coverage:
"Performance optimization, quality metrics interpretation, tool selection guidance, and production deployment best practices"

Verify icon is appropriate (Lightbulb for best practices) and root is true.
  </action>
  <verify>
```bash
# Verify meta.json updated with all 5 pages
grep -c '"optimization"\|"quality-interpretation"\|"comparison-tables"\|"rate-limiting"\|"caching-strategies"' docs/best-practices/meta.json  # Should be 5

# Verify order matches plan
cat docs/best-practices/meta.json

# Verify description updated
grep "quality metrics interpretation" docs/best-practices/meta.json

# Verify structure valid
node -e "require('./docs/best-practices/meta.json')"  # Should not error
```
  </verify>
  <done>meta.json updated with all 5 pages in logical order (optimization, quality-interpretation, comparison-tables, rate-limiting, caching-strategies), description updated to reflect comprehensive coverage, icon and root properties preserved</done>
</task>

</tasks>

<verification>
**Overall checks:**

1. All 4 new guides created with minimum line counts:
```bash
ls -la docs/best-practices/*.mdx | grep -E "quality-interpretation|rate-limiting|caching-strategies|comparison-tables"
wc -l docs/best-practices/quality-interpretation.mdx  # 200+
wc -l docs/best-practices/rate-limiting.mdx  # 150+
wc -l docs/best-practices/caching-strategies.mdx  # 150+
wc -l docs/best-practices/comparison-tables.mdx  # 150+
```

2. Navigation complete:
```bash
cat docs/best-practices/meta.json | grep -c '"optimization"\|"quality-interpretation"\|"comparison-tables"\|"rate-limiting"\|"caching-strategies"'  # Should be 5
```

3. Cross-references established:
```bash
# Quality guide links to tools and workflows
grep -E "/api/tools|/guides|/workflows" docs/best-practices/quality-interpretation.mdx

# Rate limiting links to middleware and error handling
grep -E "middleware|error-handling" docs/best-practices/rate-limiting.mdx

# Caching links to optimization and rate-limiting
grep -E "optimization|rate-limiting" docs/best-practices/caching-strategies.mdx

# Comparison tables link to API reference
grep -E "/api/tools" docs/best-practices/comparison-tables.mdx
```

4. Build succeeds:
```bash
cd docs && npm run build
# Should complete without errors, generating static pages
```
</verification>

<success_criteria>
1. quality-interpretation.mdx created with 200+ lines explaining DQV 8-component scoring and interpretation thresholds
2. rate-limiting.mdx created with 150+ lines documenting 10 req/s limit, burst capacity, and retry strategies
3. caching-strategies.mdx created with 150+ lines covering TTL, LRU, and multi-level caching patterns
4. comparison-tables.mdx created with 150+ lines providing 6+ tool comparison tables with decision guides
5. meta.json updated with all 5 pages in logical order (optimization → quality-interpretation → comparison-tables → rate-limiting → caching-strategies)
6. All guides include code examples and cross-references to related documentation
7. Documentation builds successfully with new pages accessible in navigation
</success_criteria>

<output>
After completion, create `.planning/phases/23-best-practices-a-visual-assets/23-01-SUMMARY.md`
</output>
