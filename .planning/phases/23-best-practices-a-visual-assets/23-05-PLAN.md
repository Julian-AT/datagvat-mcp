---
phase: 23-best-practices-a-visual-assets
plan: 05
type: execute
wave: 3
depends_on: [23-01]
files_modified:
  - docs/best-practices/quality-interpretation.mdx
  - docs/best-practices/rate-limiting.mdx
  - docs/guides/searching.mdx
  - docs/workflows/discovery.mdx
autonomous: true

must_haves:
  truths:
    - Users understand Austria MCP architecture through Mermaid diagrams
    - Architecture diagram shows MCP client → FastMCP server → Piveau API flow
    - Middleware stack diagram shows execution order and purpose
    - Quality scoring diagram shows 8-component structure
  artifacts:
    - path: "docs/best-practices/quality-interpretation.mdx"
      provides: "Quality scoring component diagram"
      contains: "<Mermaid"
    - path: "docs/best-practices/rate-limiting.mdx"
      provides: "Middleware stack diagram"
      contains: "<Mermaid"
  key_links:
    - from: "Mermaid diagrams"
      to: "theme-aware rendering"
      via: "useMDXComponents with Mermaid component"
      pattern: "mermaid.tsx"
    - from: "Architecture diagram"
      to: "FastMCP internals"
      via: "visualizes documented architecture"
      pattern: "MCP Client.*FastMCP.*Piveau"
---

<objective>
Create Mermaid architecture diagrams visualizing Austria MCP system architecture, middleware stack execution, and quality scoring structure to enhance documentation with visual learning aids.

Purpose: Diagrams help users quickly understand complex system relationships that are difficult to convey with text alone.

Output: 3-4 Mermaid diagrams embedded in best practices guides and workflows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-best-practices-a-visual-assets/23-RESEARCH.md

# Mermaid component already registered in Phase 18
@.planning/phases/18-documentation-foundation/18-02-SUMMARY.md

# Architecture to visualize
@docs/integration/fastmcp-internals.mdx
@mcp/app/middleware.py
@mcp/app/tools/analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Quality Scoring Component Diagram</name>
  <files>docs/best-practices/quality-interpretation.mdx</files>
  <action>
Add Mermaid diagram to quality-interpretation.mdx showing 8-component DQV structure:

**Location:** After "Understanding Quality Scores" intro, before detailed component sections

**Diagram structure:**
```markdown
## Understanding Quality Scores

Quality scores range from 0-100 and measure metadata completeness across 8 components.

<Mermaid chart={`
graph TD
    A[Quality Score 0-100] --> B[Core Metadata 40pts]
    A --> C[Discovery Metadata 30pts]
    A --> D[Access Metadata 20pts]
    A --> E[Temporal Metadata 10pts]

    B --> B1[Title 10pts]
    B --> B2[Description 15pts]
    B --> B3[Publisher 15pts]

    C --> C1[Keywords 10pts]
    C --> C2[Themes 10pts]
    C --> C3[Spatial 10pts]

    D --> D1[Distributions 15pts]
    D --> D2[License 5pts]

    E --> E1[Issued 5pts]
    E --> E2[Modified 5pts]

    style A fill:#f9f,stroke:#333,stroke-width:4px
    style B fill:#bbf,stroke:#333,stroke-width:2px
    style C fill:#bbf,stroke:#333,stroke-width:2px
    style D fill:#bbf,stroke:#333,stroke-width:2px
    style E fill:#bbf,stroke:#333,stroke-width:2px
`} />

The diagram shows how the 100-point quality score breaks down into 4 dimensions and 10 individual components.

### Core Metadata (40 points)
...
```

**Mermaid syntax:**
- Use `graph TD` (top-down) for hierarchical structure
- Show point values in labels (e.g., "Title 10pts")
- Use styles to differentiate root, categories, and components
- Keep diagram simple (avoid excessive detail)

**Important:**
- Use template literal syntax: `chart={\`...\`}` for multi-line charts
- Mermaid component already registered (Phase 18)
- Theme-aware rendering works automatically

**Verify diagram renders:**
```bash
cd docs && npm run dev
# Visit /best-practices/quality-interpretation
# Verify Mermaid diagram displays correctly in light and dark themes
```
  </action>
  <verify>
```bash
# Verify Mermaid component used
grep "<Mermaid" docs/best-practices/quality-interpretation.mdx

# Verify quality scoring nodes present
grep -E "Core Metadata|Discovery Metadata|Access Metadata|Temporal Metadata" docs/best-practices/quality-interpretation.mdx

# Verify point values shown
grep -E "[0-9]+pts" docs/best-practices/quality-interpretation.mdx

# Count diagram complexity (should be ~20-25 lines)
grep -A 30 "<Mermaid" docs/best-practices/quality-interpretation.mdx | grep -c "^"
```
  </verify>
  <done>Quality scoring diagram added to quality-interpretation.mdx showing 4-tier hierarchy (overall score → 4 dimensions → 10 components) with point values, styled for clarity, verified rendering in dev server</done>
</task>

<task type="auto">
  <name>Task 2: Create Middleware Stack Diagram</name>
  <files>docs/best-practices/rate-limiting.mdx</files>
  <action>
Add Mermaid diagram to rate-limiting.mdx showing middleware execution order:

**Location:** After "Automatic Retry Behavior" section

**Diagram structure:**
```markdown
## Middleware Stack

FastMCP middleware processes requests in strict order. Rate limiting occurs late in the stack, after logging and error handling:

<Mermaid chart={`
graph LR
    A[MCP Request] --> B[Logging Middleware]
    B --> C[Error Handler]
    C --> D[Retry Middleware]
    D --> E[Rate Limit Middleware]
    E --> F[Tool Execution]
    F --> G[MCP Response]

    D -.->|Max 3 attempts| D
    E -.->|429 Rate Limited| D

    style E fill:#f99,stroke:#333,stroke-width:2px
    style D fill:#9f9,stroke:#333,stroke-width:2px
    style F fill:#99f,stroke:#333,stroke-width:2px
`} />

Notice how:
1. Rate limiting happens **after** retry middleware
2. Rate limit errors (429) trigger retry attempts automatically
3. Maximum 3 retry attempts with exponential backoff (1-60s)

### Avoiding Rate Limits
...
```

**Diagram features:**
- Left-to-right flow (LR) showing request progression
- Highlight rate limit and retry middleware
- Show feedback loop (retry on rate limit)
- Use dotted lines for conditional paths
- Style key middleware differently

**Based on:**
- STATE.md middleware order: Logging → Error → Retry → RateLimit → Audit → Auth
- Simplify to essential middleware for rate limiting explanation
- Omit Audit/Auth for clarity (not relevant to rate limiting)
  </action>
  <verify>
```bash
# Verify Mermaid diagram added
grep "<Mermaid" docs/best-practices/rate-limiting.mdx

# Verify middleware nodes present
grep -E "Rate Limit|Retry|Error Handler|Tool Execution" docs/best-practices/rate-limiting.mdx

# Verify flow shows order
grep "graph LR" docs/best-practices/rate-limiting.mdx

# Verify feedback loop shown
grep "\.\->" docs/best-practices/rate-limiting.mdx  # Dotted lines for retry
```
  </verify>
  <done>Middleware stack diagram added to rate-limiting.mdx showing execution order (logging → error → retry → rate limit → tool), highlights retry behavior on rate limit errors, styled for emphasis on rate limiting, verified rendering</done>
</task>

<task type="auto">
  <name>Task 3: Create System Architecture Diagram</name>
  <files>docs/guides/searching.mdx</files>
  <action>
Check if docs/guides/searching.mdx exists and has architecture context. If appropriate, add high-level architecture diagram.

**If searching.mdx exists and has "How it Works" or architecture section:**

Add Mermaid diagram showing MCP → FastMCP → Piveau flow:

```markdown
## How Austria MCP Works

Austria MCP connects Claude Desktop to the Austrian Open Data Portal through FastMCP and Piveau API:

<Mermaid chart={`
graph TD
    A[Claude Desktop] -->|MCP Protocol| B[FastMCP Server]
    B --> C[Tool Registry]
    B --> D[Middleware Stack]
    D --> E[Piveau API Client]
    E -->|HTTPS| F[data.gv.at API]

    C -->|25 Tools| D
    D -->|Rate Limit<br/>Retry<br/>Auth| E
    F -->|JSON Response| E
    E --> D
    D --> B
    B -->|MCP Response| A

    style B fill:#9f9,stroke:#333,stroke-width:2px
    style E fill:#99f,stroke:#333,stroke-width:2px
    style F fill:#f9f,stroke:#333,stroke-width:2px
`} />

Components:
- **Claude Desktop**: MCP client (you interact with this)
- **FastMCP Server**: Tool registry and middleware
- **Piveau API Client**: HTTP client with retry/rate limiting
- **data.gv.at API**: Austrian Open Data Portal backend

### Search Tools
...
```

**If searching.mdx doesn't exist or lacks architecture section:**
- Find alternative guide location (discovery.mdx, getting-started.mdx)
- Or skip if no appropriate location found
- Document decision in summary

**Diagram purpose:**
Help users understand where Austria MCP fits in data flow from Claude to API.
  </action>
  <verify>
```bash
# Check if searching.mdx exists
test -f docs/guides/searching.mdx && echo "File exists"

# If diagram added, verify
grep "<Mermaid" docs/guides/searching.mdx 2>/dev/null

# Verify components shown
grep -E "Claude Desktop|FastMCP|Piveau|data.gv.at" docs/guides/searching.mdx 2>/dev/null

# Alternative: Check where diagram was added if not searching.mdx
find docs -name "*.mdx" -exec grep -l "Claude Desktop.*FastMCP.*Piveau" {} \;
```
  </verify>
  <done>If appropriate location found: architecture diagram added showing MCP protocol flow from Claude Desktop through FastMCP to Piveau API with component descriptions. If no suitable location: documented in summary</done>
</task>

<task type="auto">
  <name>Task 4: Create Workflow Decision Tree</name>
  <files>docs/workflows/discovery.mdx</files>
  <action>
Add Mermaid decision tree to discovery workflow showing tool selection logic:

**Location:** Before Steps component, after "Use This Workflow When" section

**Diagram structure:**
```markdown
## Workflow Overview

Choose your search approach based on what you know:

<Mermaid chart={`
graph TD
    A{What do you have?} -->|Keywords| B[search_datasets]
    A -->|Natural question| C[search_semantic]
    A -->|Example dataset ID| D[find_similar_datasets]

    B --> E{Need quality focus?}
    E -->|Yes| F[Enable quality_boost]
    E -->|No| G[Default relevance ranking]

    F --> H[Get dataset details]
    G --> H
    C --> H
    D --> H

    H --> I[Preview data]
    I --> J{Suitable?}
    J -->|Yes| K[Use dataset]
    J -->|No| A

    style A fill:#f9f,stroke:#333,stroke-width:2px
    style B fill:#9f9,stroke:#333,stroke-width:2px
    style C fill:#9f9,stroke:#333,stroke-width:2px
    style D fill:#9f9,stroke:#333,stroke-width:2px
    style K fill:#99f,stroke:#333,stroke-width:2px
`} />

The flowchart shows three entry points (keywords, natural language, example dataset) converging into a common evaluation process.

<Steps>
### Step 1: Search for Datasets
...
```

**Decision tree purpose:**
- Show different search entry points
- Visualize quality_boost decision
- Illustrate iteration pattern (try again if not suitable)
- Guide users to appropriate tool

**Alternative location if discovery.mdx doesn't exist:**
- docs/workflows/dataset-discovery.mdx
- docs/guides/searching.mdx (as "Which Tool to Use" section)
- docs/best-practices/comparison-tables.mdx (as visual decision aid)

Choose best location based on what exists.
  </action>
  <verify>
```bash
# Find discovery workflow
find docs -name "*discovery*.mdx" -path "*/workflows/*" 2>/dev/null

# If diagram added, verify decision nodes
grep -E "\{.*\?\}" docs/workflows/discovery.mdx 2>/dev/null  # Mermaid decision syntax

# Verify tool options shown
grep -E "search_datasets|search_semantic|find_similar" docs/workflows/discovery.mdx 2>/dev/null

# Alternative: Find where decision tree was added
find docs -name "*.mdx" -exec grep -l "What do you have?" {} \;
```
  </verify>
  <done>Decision tree diagram added showing workflow tool selection logic (keywords → search_datasets, natural language → search_semantic, example → find_similar_datasets) with quality boost decision and iteration pattern, placed in discovery workflow or best alternative location</done>
</task>

</tasks>

<verification>
**Overall checks:**

1. All Mermaid diagrams render:
```bash
cd docs && npm run dev
# Visit pages with diagrams and verify rendering in both light/dark themes:
# - /best-practices/quality-interpretation (quality scoring diagram)
# - /best-practices/rate-limiting (middleware stack diagram)
# - /guides/searching or alternative (architecture diagram)
# - /workflows/discovery or alternative (decision tree diagram)
```

2. Diagram syntax valid:
```bash
grep -r "<Mermaid" docs --include="*.mdx" | wc -l  # Should be 3-4
grep -r "graph TD\|graph LR" docs --include="*.mdx" | wc -l  # Should match Mermaid count
```

3. Theme-aware rendering:
```bash
# In dev server, toggle light/dark mode
# Verify diagrams remain readable in both themes
# Mermaid should auto-adjust based on theme
```

4. Context provided:
```bash
# Verify each diagram has explanatory text before/after
grep -B 3 -A 3 "<Mermaid" docs/best-practices/*.mdx docs/guides/*.mdx docs/workflows/*.mdx
# Should show context explaining diagram purpose
```

5. Build succeeds:
```bash
cd docs && npm run build
# Should complete without Mermaid rendering errors
```
</verification>

<success_criteria>
1. Quality scoring diagram added to quality-interpretation.mdx showing 8-component DQV structure with point values
2. Middleware stack diagram added to rate-limiting.mdx showing execution order and retry behavior
3. Architecture diagram added showing Claude Desktop → FastMCP → Piveau API flow (in appropriate guide/workflow)
4. Decision tree diagram added showing workflow tool selection logic (in discovery workflow or alternative)
5. All diagrams use Mermaid component with template literal syntax
6. Diagrams render correctly in both light and dark themes
7. Context text provided before/after each diagram explaining purpose
8. Build succeeds with all diagrams rendering
</success_criteria>

<output>
After completion, create `.planning/phases/23-best-practices-a-visual-assets/23-05-SUMMARY.md`
</output>
