---
phase: 02-navigation-restructuring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/content/docs/(advanced)/integration/meta.json
  - docs/content/docs/(advanced)/best-practices/meta.json
  - docs/content/docs/(advanced)/advanced/meta.json
autonomous: true

must_haves:
  truths:
    - "Integration, Best Practices, and Advanced appear grouped together in sidebar"
    - "URLs remain unchanged (/docs/integration/*, /docs/best-practices/*, /docs/advanced/*)"
    - "Folder group provides visual separation without adding click depth"
  artifacts:
    - path: "docs/content/docs/(advanced)/"
      provides: "Visual grouping container (no route impact)"
    - path: "docs/content/docs/(advanced)/integration/"
      provides: "Integration docs under advanced group"
    - path: "docs/content/docs/(advanced)/integration/meta.json"
      provides: "Updated schema path for group nesting"
      contains: '"$schema": "../../.source'
    - path: "docs/content/docs/(advanced)/best-practices/"
      provides: "Best practices docs under advanced group"
    - path: "docs/content/docs/(advanced)/best-practices/meta.json"
      provides: "Updated schema path for group nesting"
      contains: '"$schema": "../../.source'
    - path: "docs/content/docs/(advanced)/advanced/"
      provides: "Advanced topics under advanced group"
    - path: "docs/content/docs/(advanced)/advanced/meta.json"
      provides: "Updated schema path for group nesting"
      contains: '"$schema": "../../.source'
  key_links:
    - from: "Root meta.json"
      to: "(advanced) folder group"
      via: "folder group reference"
      pattern: '\(advanced\)'
    - from: "Each meta.json in (advanced)/"
      to: "schema file"
      via: "relative path"
      pattern: '\$schema.*\.\./\.\./\.source'
---

<objective>
Create (advanced) folder group and move integration, best-practices, and advanced folders into it using git mv.

Purpose: Group conceptually related advanced topics (integration guides, best practices, internal architecture) under a single visual section in the sidebar, reducing top-level navigation from 7-8 tabs to 4 clear sections.

Output: (advanced) folder group containing three root folders, all with updated schema paths. Routes remain unchanged (folder groups don't affect URLs).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\REQUIREMENTS.md
@C:\GitHub\datagvat-mcp\.planning\phases\02-navigation-restructuring\02-CONTEXT.md
@C:\GitHub\datagvat-mcp\.planning\phases\02-navigation-restructuring\02-RESEARCH.md
@C:\GitHub\datagvat-mcp\docs\content\docs\meta.json
@C:\GitHub\datagvat-mcp\docs\content\docs\integration\meta.json
@C:\GitHub\datagvat-mcp\docs\content\docs\best-practices\meta.json
@C:\GitHub\datagvat-mcp\docs\content\docs\advanced\meta.json
</context>

<tasks>

<task type="auto">
  <name>Create (advanced) folder group and move folders</name>
  <files>
    docs/content/docs/(advanced)/
    docs/content/docs/(advanced)/integration/
    docs/content/docs/(advanced)/best-practices/
    docs/content/docs/(advanced)/advanced/
  </files>
  <action>
1. Create (advanced) folder group directory: `mkdir -p "docs/content/docs/(advanced)"`

2. Move integration folder preserving history: `git mv docs/content/docs/integration "docs/content/docs/(advanced)/integration"`

3. Move best-practices folder preserving history: `git mv docs/content/docs/best-practices "docs/content/docs/(advanced)/best-practices"`

4. Move advanced folder preserving history: `git mv docs/content/docs/advanced "docs/content/docs/(advanced)/advanced"`

5. Update schema paths in all three meta.json files from `"../.source/json-schema/docs.meta.json"` to `"../../.source/json-schema/docs.meta.json"` (now nested under folder group):
   - `docs/content/docs/(advanced)/integration/meta.json`
   - `docs/content/docs/(advanced)/best-practices/meta.json`
   - `docs/content/docs/(advanced)/advanced/meta.json`

6. Verify each meta.json still has:
   - `"root": true` (they remain root folders, just visually grouped)
   - Existing title, description, icon (don't change)
   - Existing pages arrays (don't change)

Why this approach: Folder groups (parentheses) provide visual sidebar grouping WITHOUT affecting routes (Pitfall 4 from research). URLs remain /docs/integration/*, not /docs/advanced/integration/*. git mv preserves history (Pitfall 5). Schema paths must be updated because files are now one level deeper.
  </action>
  <verify>
1. Run: `ls "docs/content/docs/(advanced)"` - shows integration, best-practices, advanced subdirectories
2. Run: `git log --follow "docs/content/docs/(advanced)/integration/meta.json"` - shows history from old location
3. Check schema paths: `grep '\$schema' "docs/content/docs/(advanced)"/*/meta.json` - all show `../../.source`
4. Verify root flags: `grep '"root"' "docs/content/docs/(advanced)"/*/meta.json` - all show `"root": true`
5. Verify old locations removed: `ls docs/content/docs/integration 2>/dev/null || echo "removed"` - should show "removed"
  </verify>
  <done>
- (advanced) folder group exists at docs/content/docs/(advanced)/
- Integration moved to (advanced)/integration/ using git mv
- Best-practices moved to (advanced)/best-practices/ using git mv
- Advanced moved to (advanced)/advanced/ using git mv
- All three meta.json files have schema path updated to ../../.source
- All three meta.json files retain "root": true flag
- Git history preserved for all moved files
- Routes remain unchanged (/docs/integration/*, not /docs/advanced/integration/*)
  </done>
</task>

<task type="auto">
  <name>Stage and commit (advanced) folder group creation</name>
  <files>
    .git/
  </files>
  <action>
Stage all changes from the (advanced) folder group creation:

```bash
git add "docs/content/docs/(advanced)/"
git commit -m "$(cat <<'EOF'
refactor(nav): create (advanced) folder group for integration, best-practices, advanced

- Create docs/content/docs/(advanced)/ folder group
- Move integration → (advanced)/integration/ (git mv for history)
- Move best-practices → (advanced)/best-practices/ (git mv for history)
- Move advanced → (advanced)/advanced/ (git mv for history)
- Update all meta.json schema paths for new depth (../../.source)
- Folder group provides visual sidebar grouping without affecting routes
- Reduces top-level navigation from 7 tabs to 4 (getting-started, guides, reference, advanced)

Phase: 02-navigation-restructuring
Plan: 02-02

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

This commits the folder group creation before updating root meta.json (Plan 03 will update root navigation).
  </action>
  <verify>
Run: `git log --oneline -1` - shows commit with "refactor(nav): create (advanced) folder group"
Run: `git status` - clean working directory
  </verify>
  <done>
Changes committed to git with descriptive message referencing phase/plan
  </done>
</task>

</tasks>

<verification>
After plan completion:

1. **Structure verification**:
   - `docs/content/docs/(advanced)/` exists
   - `docs/content/docs/(advanced)/integration/` exists (moved from root)
   - `docs/content/docs/(advanced)/best-practices/` exists (moved from root)
   - `docs/content/docs/(advanced)/advanced/` exists (moved from root)
   - Old root-level integration/, best-practices/, advanced/ no longer exist

2. **Schema path correctness**:
   - All three meta.json files: `"$schema": "../../.source/json-schema/docs.meta.json"`
   - All three retain correct depth calculation (two levels from .source)

3. **Root flag preservation**:
   - All three meta.json files still have `"root": true`
   - They remain sidebar tabs, just visually grouped

4. **Git history preservation**:
   - `git log --follow "docs/content/docs/(advanced)/integration/meta.json"` shows full history
   - Same for best-practices and advanced

5. **Route integrity**:
   - URLs remain /docs/integration/*, /docs/best-practices/*, /docs/advanced/*
   - Folder group parentheses don't affect routes (only sidebar visual grouping)

6. **Navigation readiness**:
   - (advanced) folder group ready to be referenced in root meta.json
   - All three folders retain their root status for sidebar tabs
</verification>

<success_criteria>
- (advanced) folder group created successfully
- Integration, best-practices, advanced moved under (advanced)/ preserving git history
- Schema paths updated correctly for new folder depth
- All root: true flags preserved (remain sidebar tabs)
- Routes unchanged (folder groups don't affect URLs)
- Ready for root meta.json update in Plan 03
- Commit created with changes staged
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-restructuring/02-02-SUMMARY.md`
</output>
