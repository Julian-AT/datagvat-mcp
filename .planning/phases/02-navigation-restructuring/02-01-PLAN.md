---
phase: 02-navigation-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/content/docs/reference/meta.json
  - docs/content/docs/reference/tools/meta.json
autonomous: true

must_haves:
  truths:
    - "Tools appear under Reference section in navigation"
    - "Tools URL remains /docs/tools/* (no route change)"
    - "Reference section shows in sidebar with Library icon"
    - "All tool documentation content remains accessible"
  artifacts:
    - path: "docs/content/docs/reference/"
      provides: "Reference root folder container"
    - path: "docs/content/docs/reference/meta.json"
      provides: "Reference section configuration"
      contains: '"root": true'
      exports: ["title", "icon", "pages"]
    - path: "docs/content/docs/reference/tools/"
      provides: "Tools documentation moved under reference"
    - path: "docs/content/docs/reference/tools/meta.json"
      provides: "Updated schema path for nested location"
      contains: '"$schema": "../../.source'
  key_links:
    - from: "docs/content/docs/reference/meta.json"
      to: "tools folder contents"
      via: "extract operator"
      pattern: '\.\.\.tools'
    - from: "docs/content/docs/reference/tools/meta.json"
      to: "schema file"
      via: "relative path"
      pattern: '\$schema.*\.\./\.\./\.source'
---

<objective>
Create Reference section and move Tools documentation under it using git mv to preserve history.

Purpose: Consolidate technical reference materials (tools, future API docs) under a single "Reference" top-level section, reducing navigation clutter from 7-8 tabs to 4 clear sections.

Output: Reference folder with tools nested inside, proper meta.json configuration with extract operator, all schema paths updated for new depth.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\REQUIREMENTS.md
@C:\GitHub\datagvat-mcp\.planning\phases\02-navigation-restructuring\02-CONTEXT.md
@C:\GitHub\datagvat-mcp\.planning\phases\02-navigation-restructuring\02-RESEARCH.md
@C:\GitHub\datagvat-mcp\docs\content\docs\meta.json
@C:\GitHub\datagvat-mcp\docs\content\docs\tools\meta.json
</context>

<tasks>

<task type="auto">
  <name>Create reference folder structure and move tools</name>
  <files>
    docs/content/docs/reference/
    docs/content/docs/reference/meta.json
    docs/content/docs/reference/tools/
  </files>
  <action>
1. Create reference directory: `mkdir -p docs/content/docs/reference`

2. Create reference/meta.json with:
   - `"$schema": "../.source/json-schema/docs.meta.json"` (one level deeper than root)
   - `"title": "Reference"`
   - `"description": "Complete API and tools reference"`
   - `"icon": "Library"` (use Library from lucide-react)
   - `"root": true`
   - `"pages": ["---[Wrench]Tools---", "...tools"]` (extract operator to pull in tools folder contents)

3. Move tools folder preserving history: `git mv docs/content/docs/tools docs/content/docs/reference/tools`

4. Update tools/meta.json schema path from `"../.source/json-schema/docs.meta.json"` to `"../../.source/json-schema/docs.meta.json"` (now two levels deep: reference/tools instead of tools)

5. Verify tools/meta.json still has:
   - `"title": "Tools Reference"`
   - `"root": true`
   - `"icon": "Wrench"`
   - Keep existing pages array empty (will be populated by auto-generated tools reference in Phase 7)

Why this approach: git mv preserves file history (avoid Pitfall 5 from research). Extract operator `...tools` pulls in tools folder contents without adding navigation depth. Schema paths are relative and must be updated when folder depth changes (Pitfall 2).
  </action>
  <verify>
1. Run: `ls docs/content/docs/reference/tools/meta.json` - file exists at new location
2. Run: `git log --follow docs/content/docs/reference/tools/meta.json` - shows history from old location
3. Check schema path: `grep '\$schema' docs/content/docs/reference/tools/meta.json` - shows `../../.source`
4. Check reference meta.json: `cat docs/content/docs/reference/meta.json` - contains extract operator `...tools`
5. Verify no broken references: `grep -r 'docs/tools' docs/content/docs` - should only find legitimate content references, not meta.json references
6. Verify content files survived move: `find docs/content/docs/reference/tools -name "*.mdx" -type f | wc -l` - count should be 0 (tools will be auto-generated in Phase 7)
7. List moved content files: `find docs/content/docs/reference/tools -name "*.mdx" -type f` - verify specific files present
  </verify>
  <done>
- Reference folder exists at docs/content/docs/reference/
- Reference/meta.json configured with root: true, Library icon, extract operator for tools
- Tools folder moved to docs/content/docs/reference/tools/ using git mv
- Tools/meta.json schema path updated to ../../.source (two levels deep)
- Git history preserved for all moved files
- No broken meta.json references to old tools location
- All tool .mdx content files confirmed present at new location
  </done>
</task>

<task type="auto">
  <name>Stage and commit reference restructuring</name>
  <files>
    .git/
  </files>
  <action>
Stage all changes from the reference folder creation and tools move:

```bash
git add docs/content/docs/reference/
git add docs/content/docs/reference/tools/
git commit -m "$(cat <<'EOF'
refactor(nav): create Reference section and move tools under it

- Create docs/content/docs/reference/ with meta.json
- Move docs/content/docs/tools → docs/content/docs/reference/tools/ (git mv for history)
- Update tools/meta.json schema path for new depth (../../.source)
- Use extract operator (...tools) in reference/meta.json
- Reduces top-level navigation from 8 tabs to 7 (step toward 4)

Phase: 02-navigation-restructuring
Plan: 02-01

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

This commits the folder restructuring before updating root meta.json (Plan 03 will update root navigation).
  </action>
  <verify>
Run: `git log --oneline -1` - shows commit with "refactor(nav): create Reference section"
Run: `git status` - clean working directory
  </verify>
  <done>
Changes committed to git with descriptive message referencing phase/plan
  </done>
</task>

</tasks>

<verification>
After plan completion:

1. **Structure verification**:
   - `docs/content/docs/reference/` exists
   - `docs/content/docs/reference/meta.json` exists with root: true
   - `docs/content/docs/reference/tools/` exists (moved from root)
   - Old `docs/content/docs/tools/` no longer exists at root

2. **Schema path correctness**:
   - Reference meta.json: `"$schema": "../.source/json-schema/docs.meta.json"`
   - Tools meta.json: `"$schema": "../../.source/json-schema/docs.meta.json"`

3. **Git history preservation**:
   - `git log --follow docs/content/docs/reference/tools/meta.json` shows full history

4. **Content integrity**:
   - All .mdx files in tools/ remain unchanged
   - Only meta.json files modified (schema paths)
   - Count of .mdx files verified before and after

5. **Navigation readiness**:
   - Reference folder ready to be added to root meta.json
   - Extract operator configured to pull in tools contents
</verification>

<success_criteria>
- Reference section created with proper meta.json configuration
- Tools moved under reference/ preserving git history
- Schema paths updated correctly for new folder depth
- All tools content files remain unchanged and verified present
- Ready for root meta.json update in Plan 03
- Commit created with changes staged
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-restructuring/02-01-SUMMARY.md`
</output>
