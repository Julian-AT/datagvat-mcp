---
phase: 17-code-execution-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Single-file Python execution completes within 30 seconds"
    - "Multi-file project with imports executes successfully"
    - "Execution errors are returned with full traceback"
    - "Timeout errors include helpful optimization guidance"
    - "Stdout and stderr output is visible to user"
  artifacts: []
  key_links:
    - from: "execute-python tool"
      to: "AI error recovery"
      via: "Structured ExecutionError enables AI to fix and retry"
      pattern: "AI receives traceback, generates fixed code, retries successfully"
---

<objective>
Verify that the enhanced execution pipeline handles real-world scenarios: single-file scripts, multi-file projects with imports, timeout enforcement, error recovery, and proper output visibility.

Purpose: Validate that Phase 17 implementation meets all success criteria through manual testing before shipping to Phase 18 (approval flow integration).

Output: Human confirmation that execution pipeline works correctly with representative workloads.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julianschmidt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-code-execution-pipeline/17-CONTEXT.md
@.planning/phases/17-code-execution-pipeline/17-01-SUMMARY.md
@docs/app/api/chat/route.ts
@docs/lib/ai/prompts.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Enhanced execute-python tool with:
- 30-second timeout enforcement
- Multi-file project support with filesystem operations
- Structured error forwarding for AI recovery
- Separated stdout/stderr capture
- Timeout optimization guidance

All functionality is automated - you're verifying Claude's implementation works correctly.
  </what-built>

  <how-to-verify>
**Prerequisites:**
- Ensure E2B_API_KEY is set in environment
- Ensure DATABASE_URL, BETTER_AUTH_SECRET, BLOB_READ_WRITE_TOKEN are configured
- Development server is running: `cd docs && npm run dev`

**Test 1: Single-file execution with stdout visibility**

1. Open chat UI at http://localhost:3000
2. Send message: "Load dataset https://data.statistik.gv.at/data/OGD_krebs_ext_KREBS_1.csv and show the first 5 rows"
3. Wait for AI to generate and execute code
4. **Expected behavior:**
   - Code executes successfully (within 30 seconds)
   - Stdout output is visible (print statements, DataFrame head)
   - No timeout error

**Test 2: Intentional timeout (verify 30-second limit)**

1. In same conversation, send: "Generate a loop that runs for 40 seconds to test timeout enforcement"
2. Wait for AI to generate code with time.sleep(40) or similar
3. **Expected behavior:**
   - Execution stops at 30 seconds (not 60)
   - Error message includes timeout guidance: "Breaking into smaller chunks, Reducing dataset size, Optimizing loops"
   - AI may attempt to retry with optimized code (optional)

**Test 3: Execution error with recovery**

1. In same conversation, send: "Calculate the mean of column 'NONEXISTENT_COLUMN'"
2. Wait for AI to generate code that references invalid column
3. **Expected behavior:**
   - Execution fails with KeyError or similar
   - Error includes traceback (full Python error context)
   - AI automatically retries with fixed code (e.g., first checks available columns)
   - Second attempt succeeds

**Test 4: Multi-file project with imports**

1. In same conversation, send: "Create a multi-file analysis: separate file for data cleaning (clean_data function) and main analysis that imports it"
2. Wait for AI to generate multiple files and execute
3. **Expected behavior:**
   - AI generates files parameter with utils.py (or similar) and main.py
   - Execution succeeds with proper import resolution
   - No ImportError or ModuleNotFoundError

**Test 5: Stderr visibility**

1. In same conversation, send: "Generate code that prints warnings to stderr using warnings.warn()"
2. Wait for execution
3. **Expected behavior:**
   - Stderr output is visible (warnings shown to user)
   - Separated from stdout (if implementation separates streams)

**Verification checklist:**
- [ ] Test 1: Single-file execution works, stdout visible
- [ ] Test 2: Timeout at 30 seconds (not 60), guidance message appears
- [ ] Test 3: Error includes traceback, AI retries and fixes
- [ ] Test 4: Multi-file imports work correctly
- [ ] Test 5: Stderr output is captured and visible

**If any test fails:**
- Check browser console for errors
- Check server logs for execution details
- Note which specific test failed and what happened
- Describe the failure when responding
  </how-to-verify>

  <resume-signal>
Type "approved" if all tests pass, or describe specific failures and Claude will fix them.
  </resume-signal>
</task>

</tasks>

<verification>
All success criteria from Phase 17 ROADMAP.md verified through manual testing:

1. **AI can execute Python code** - Tested in Test 1, 3, 4
2. **Code executes in isolated E2B sandbox** - Verified by E2B SDK usage (sandboxes are isolated by design)
3. **Execution has 30-second timeout** - Verified in Test 2 (timeout at 30s, not 60s)
4. **Multi-file projects supported** - Verified in Test 4 (imports work)
5. **Errors forwarded to AI** - Verified in Test 3 (AI receives error and retries)
6. **Stdout/stderr visible to user** - Verified in Test 1 and 5
7. **Timeout errors include helpful message** - Verified in Test 2 (optimization guidance)
</verification>

<success_criteria>
Human confirms:
- Single-file Python execution works correctly with stdout visibility
- 30-second timeout is enforced (not 60 seconds)
- Timeout errors include optimization guidance
- Multi-file projects with imports execute successfully
- Execution errors include full traceback for AI recovery
- AI automatically retries and fixes errors (when possible)
- Stderr output is captured and visible
</success_criteria>

<output>
After completion, create `.planning/phases/17-code-execution-pipeline/17-02-SUMMARY.md` documenting:
- Which tests passed
- Any issues discovered and fixed
- Confirmation that all Phase 17 success criteria are met
- Any observations about error recovery behavior or timeout handling
</output>
