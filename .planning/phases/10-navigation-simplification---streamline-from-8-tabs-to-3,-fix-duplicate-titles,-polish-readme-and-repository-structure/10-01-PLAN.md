---
phase: 10-navigation-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/content/docs/meta.json
  - docs/content/docs/docs/meta.json
  - docs/content/docs/api/meta.json
  - docs/content/docs/getting-started/meta.json
  - docs/content/docs/(guides)/guides/meta.json
  - docs/content/docs/(guides)/workflows/meta.json
  - docs/content/docs/(guides)/examples/meta.json
  - docs/content/docs/(advanced)/integration/meta.json
  - docs/content/docs/(advanced)/best-practices/meta.json
  - docs/content/docs/(advanced)/advanced/meta.json
  - docs/content/docs/reference/meta.json
  - docs/content/docs/api-reference/meta.json
  - docs/content/docs/tutorials/meta.json
  - docs/next.config.mjs
autonomous: true

must_haves:
  truths:
    - "User sees 3 main navigation tabs (Docs, API, Try) instead of 11 tabs"
    - "User clicks old documentation URL and arrives at correct new page"
    - "User browses documentation and all internal navigation works"
  artifacts:
    - path: "docs/content/docs/docs/meta.json"
      provides: "Root tab 1 (Docs) configuration"
      min_lines: 15
    - path: "docs/content/docs/api/meta.json"
      provides: "Root tab 2 (API) configuration"
      min_lines: 10
    - path: "docs/next.config.mjs"
      provides: "Comprehensive redirect mapping"
      contains: "async redirects()"
  key_links:
    - from: "docs/content/docs/meta.json"
      to: "docs/content/docs/docs/meta.json"
      via: "references docs folder as page"
      pattern: '"docs"'
    - from: "docs/content/docs/docs/meta.json"
      to: "nested content folders"
      via: "pages array references"
      pattern: '"pages".*getting-started'
    - from: "docs/next.config.mjs"
      to: "old URLs"
      via: "301 permanent redirects"
      pattern: "source.*docs/(getting-started|guides|workflows)"
---

<objective>
Consolidate documentation navigation from 11 root tabs to 3 clear tabs (Docs/API/Try), restructure content folders accordingly, and implement comprehensive redirect mapping to preserve all existing URLs.

Purpose: Simplify navigation for users by reducing cognitive load from 11 tabs to 3 intuitive categories, while maintaining SEO and external link compatibility through permanent redirects.

Output: Restructured folder hierarchy with 2 root folders (docs, api), updated meta.json files removing root flags from nested content, and Next.js redirects configuration covering all URL migrations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-RESEARCH.md

# Current navigation structure
@docs/content/docs/meta.json
@docs/content/docs/getting-started/meta.json
@docs/next.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create new root folder structure (docs, api) with proper meta.json</name>
  <files>
    docs/content/docs/docs/meta.json
    docs/content/docs/api/meta.json
  </files>
  <action>
Create two new root folders under docs/content/docs/:

**1. docs/content/docs/docs/meta.json** (Tab 1: Documentation):
```json
{
  "$schema": "../../.source/json-schema/docs.meta.json",
  "title": "Documentation",
  "description": "Complete guides, workflows, and examples for data.gv.at MCP Server",
  "icon": "BookOpen",
  "root": true,
  "defaultOpen": true,
  "pages": [
    "getting-started",
    "---[Map]Guides---",
    "guides",
    "workflows",
    "examples",
    "---[Settings]Advanced---",
    "integration",
    "best-practices",
    "advanced"
  ]
}
```

**2. docs/content/docs/api/meta.json** (Tab 2: API Reference):
```json
{
  "$schema": "../../.source/json-schema/docs.meta.json",
  "title": "API Reference",
  "description": "Complete MCP tools reference and OpenAPI documentation",
  "icon": "Code",
  "root": true,
  "defaultOpen": false,
  "pages": [
    "tools",
    "openapi"
  ]
}
```

**Why this structure:**
- Only 2 root folders (Docs, API) become tabs
- Try remains external link in root meta.json
- All content nested under these 2 tabs without root flags
- Matches research recommendation for 3-tab target (Docs/API/Try external)
  </action>
  <verify>
Run: `grep -r '"root".*true' docs/content/docs --include="meta.json" | wc -l`
Expected: 2 (only docs/docs/meta.json and docs/api/meta.json)
  </verify>
  <done>
Two new folders exist with root: true, all other folders will have root: true removed in next task
  </done>
</task>

<task type="auto">
  <name>Task 2: Move existing content folders under new structure and remove root flags</name>
  <files>
    docs/content/docs/docs/getting-started/
    docs/content/docs/docs/guides/
    docs/content/docs/docs/workflows/
    docs/content/docs/docs/examples/
    docs/content/docs/docs/integration/
    docs/content/docs/docs/best-practices/
    docs/content/docs/docs/advanced/
    docs/content/docs/api/tools/
    docs/content/docs/api/openapi/
  </files>
  <action>
Move existing content folders into new structure and update their meta.json files:

**Move to docs/content/docs/docs/**:
1. `mv docs/content/docs/getting-started docs/content/docs/docs/getting-started`
2. `mv docs/content/docs/(guides)/guides docs/content/docs/docs/guides`
3. `mv docs/content/docs/(guides)/workflows docs/content/docs/docs/workflows`
4. `mv docs/content/docs/(guides)/examples docs/content/docs/docs/examples`
5. `mv docs/content/docs/(advanced)/integration docs/content/docs/docs/integration`
6. `mv docs/content/docs/(advanced)/best-practices docs/content/docs/docs/best-practices`
7. `mv docs/content/docs/(advanced)/advanced docs/content/docs/docs/advanced`

**Move to docs/content/docs/api/**:
8. `mv docs/content/docs/reference/tools docs/content/docs/api/tools`
9. `mv docs/content/docs/api-reference docs/content/docs/api/openapi`

**Update each moved folder's meta.json**:
- Remove `"root": true` line
- Update `$schema` path (add one `../` level: `../../.source/...` → `../../../.source/...`)
- Keep all other properties (title, description, icon, pages)

**Remove empty folders**:
- `rm -rf docs/content/docs/(guides)`
- `rm -rf docs/content/docs/(advanced)`
- `rm -rf docs/content/docs/reference`

**Handle tutorials folder**:
- Review content: If empty or outdated, delete entirely
- If has content: Move to docs/content/docs/docs/getting-started/ or archive

**Critical:** Test that schema paths resolve correctly by opening any meta.json in VSCode and verifying autocomplete works.
  </action>
  <verify>
Run: `ls -la docs/content/docs/docs/` (should show 7 folders: getting-started, guides, workflows, examples, integration, best-practices, advanced)
Run: `ls -la docs/content/docs/api/` (should show 2 folders: tools, openapi)
Run: `grep -r '"root".*true' docs/content/docs/docs --include="meta.json"` (should be empty - no root flags in nested folders)
  </verify>
  <done>
All content folders nested under docs/docs/ or docs/api/, no root flags except on parent folders, empty folder groups removed
  </done>
</task>

<task type="auto">
  <name>Task 3: Update root meta.json and implement comprehensive redirects</name>
  <files>
    docs/content/docs/meta.json
    docs/next.config.mjs
  </files>
  <action>
**1. Update docs/content/docs/meta.json**:
```json
{
  "$schema": "..\\..\\..\\docs\\.source\\json-schema\\docs.meta.json",
  "pages": [
    "docs",
    "api",
    "---[Zap]Interactive---",
    "external:[Try MCP Server](/try)",
    "---[ExternalLink]Resources---",
    "external:[Official data.gv.at API](https://www.data.gv.at/katalog/api/3/)",
    "external:[GitHub Repository](https://github.com/datagvat/datagvat-mcp)"
  ]
}
```

**2. Add comprehensive redirects to docs/next.config.mjs**:

Insert after line 6 (after `reactStrictMode: true,`):
```javascript
  async redirects() {
    return [
      // === DOCS TAB REDIRECTS ===
      // Getting Started (root → nested under docs)
      {
        source: '/docs/getting-started/:path*',
        destination: '/docs/docs/getting-started/:path*',
        permanent: true,
      },
      // Guides (from folder group → nested under docs)
      {
        source: '/docs/guides/:path*',
        destination: '/docs/docs/guides/:path*',
        permanent: true,
      },
      // Workflows (from folder group → nested under docs)
      {
        source: '/docs/workflows/:path*',
        destination: '/docs/docs/workflows/:path*',
        permanent: true,
      },
      // Examples (from folder group → nested under docs)
      {
        source: '/docs/examples/:path*',
        destination: '/docs/docs/examples/:path*',
        permanent: true,
      },
      // Integration (from advanced folder group → nested under docs)
      {
        source: '/docs/integration/:path*',
        destination: '/docs/docs/integration/:path*',
        permanent: true,
      },
      // Best Practices (from advanced folder group → nested under docs)
      {
        source: '/docs/best-practices/:path*',
        destination: '/docs/docs/best-practices/:path*',
        permanent: true,
      },
      // Advanced Topics (from advanced folder group → nested under docs)
      {
        source: '/docs/advanced/:path*',
        destination: '/docs/docs/advanced/:path*',
        permanent: true,
      },

      // === API TAB REDIRECTS ===
      // Reference → API consolidation
      {
        source: '/docs/reference/:path*',
        destination: '/docs/api/:path*',
        permanent: true,
      },
      // Tools Reference → API/Tools
      {
        source: '/docs/tools/:path*',
        destination: '/docs/api/tools/:path*',
        permanent: true,
      },
      // API Reference → API/OpenAPI
      {
        source: '/docs/api-reference/:path*',
        destination: '/docs/api/openapi/:path*',
        permanent: true,
      },

      // === DEPRECATED CONTENT ===
      // Tutorials → Getting Started
      {
        source: '/docs/tutorials/:path*',
        destination: '/docs/docs/getting-started/:path*',
        permanent: true,
      },
    ];
  },
```

**Why permanent: true**: 301 redirects preserve SEO, tell search engines and browsers the move is permanent.

**Critical:** Preserve existing `async rewrites()` function below redirects (don't remove or modify).
  </action>
  <verify>
Run: `bun run dev` and test redirects with curl:
```bash
curl -I http://localhost:3000/docs/getting-started
curl -I http://localhost:3000/docs/guides
curl -I http://localhost:3000/docs/reference
```
Each should return `HTTP/1.1 308 Permanent Redirect` with correct Location header.
  </verify>
  <done>
Root meta.json references only 2 content tabs (docs, api) plus external links. Next.js config has 12 redirect rules covering all moved content. Dev server shows 3 tabs instead of 11.
  </done>
</task>

<task type="auto">
  <name>Task 4: Programmatically verify comprehensive redirect coverage</name>
  <files>docs/scripts/verify-redirects.ts</files>
  <action>
Create automated redirect verification script to ensure all old URLs are covered:

**Create docs/scripts/verify-redirects.ts**:
```typescript
import { readFileSync } from 'fs';
import { join } from 'path';

// Parse redirects from next.config.mjs
const configPath = join(process.cwd(), 'next.config.mjs');
const configContent = readFileSync(configPath, 'utf-8');

// Extract redirect source patterns
const redirectMatches = configContent.matchAll(/source:\s*['"]([^'"]+)['"]/g);
const redirectSources = Array.from(redirectMatches).map(m => m[1]);

console.log(`Found ${redirectSources.length} redirect rules in next.config.mjs\n`);

// Define all old URL patterns that should redirect
const expectedOldUrls = [
  '/docs/getting-started/:path*',
  '/docs/guides/:path*',
  '/docs/workflows/:path*',
  '/docs/examples/:path*',
  '/docs/integration/:path*',
  '/docs/best-practices/:path*',
  '/docs/advanced/:path*',
  '/docs/reference/:path*',
  '/docs/tools/:path*',
  '/docs/api-reference/:path*',
  '/docs/tutorials/:path*',
];

// Check coverage
const missing: string[] = [];
const covered: string[] = [];

for (const expectedUrl of expectedOldUrls) {
  if (redirectSources.includes(expectedUrl)) {
    covered.push(expectedUrl);
  } else {
    missing.push(expectedUrl);
  }
}

// Report results
console.log('=== REDIRECT COVERAGE REPORT ===\n');

console.log(`✓ Covered (${covered.length}/${expectedOldUrls.length}):`);
covered.forEach(url => console.log(`  ${url}`));

if (missing.length > 0) {
  console.log(`\n✗ Missing coverage (${missing.length}):`);
  missing.forEach(url => console.log(`  ${url}`));
  console.log('\n❌ FAIL: Not all old URLs have redirects');
  process.exit(1);
} else {
  console.log('\n✅ PASS: All expected old URLs have redirect coverage');
  process.exit(0);
}
```

**Run verification:**
```bash
cd docs
bun run tsx scripts/verify-redirects.ts
```

**Expected output:**
```
Found 12 redirect rules in next.config.mjs

=== REDIRECT COVERAGE REPORT ===

✓ Covered (11/11):
  /docs/getting-started/:path*
  /docs/guides/:path*
  /docs/workflows/:path*
  /docs/examples/:path*
  /docs/integration/:path*
  /docs/best-practices/:path*
  /docs/advanced/:path*
  /docs/reference/:path*
  /docs/tools/:path*
  /docs/api-reference/:path*
  /docs/tutorials/:path*

✅ PASS: All expected old URLs have redirect coverage
```

**If script fails:**
- Review missing URLs in output
- Add missing redirect rules to next.config.mjs
- Re-run verification until all covered
  </action>
  <verify>
Run: `cd docs && bun run tsx scripts/verify-redirects.ts` (should exit 0)
Run: `grep "✅ PASS" <(cd docs && bun run tsx scripts/verify-redirects.ts)` (should find pass message)
  </verify>
  <done>
Redirect verification script created and passed. All expected old URLs have redirect coverage. Programmatic assertion ensures no gaps in redirect mapping.
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

**Navigation structure:**
```bash
# Count root folders (should be 2)
grep -r '"root".*true' docs/content/docs --include="meta.json" | wc -l

# Verify folder structure
ls -la docs/content/docs/docs/
ls -la docs/content/docs/api/

# Check no orphaned folders remain
ls docs/content/docs/ | grep -E "^\(|^getting-started|^reference|^api-reference"
```

**Redirects working:**
```bash
bun run dev
# In another terminal:
curl -I http://localhost:3000/docs/getting-started | grep "308"
curl -I http://localhost:3000/docs/guides/search | grep "308"
curl -I http://localhost:3000/docs/reference | grep "308"
```

**Redirect coverage:**
```bash
cd docs && bun run tsx scripts/verify-redirects.ts
# Should output: ✅ PASS: All expected old URLs have redirect coverage
```

**Schema paths valid:**
Open `docs/content/docs/docs/getting-started/meta.json` in VSCode, verify JSON autocomplete works (no red squiggles under $schema).
</verification>

<success_criteria>
**Measurable completion:**

1. Exactly 2 files contain `"root": true` (docs/docs/meta.json, docs/api/meta.json)
2. All 7 documentation folders exist under docs/content/docs/docs/
3. Both API folders exist under docs/content/docs/api/
4. 12 redirect rules exist in next.config.mjs
5. Redirect verification script exists and passes
6. All 11 expected old URL patterns have redirect coverage
7. Dev server shows 3 navigation tabs (Docs, API, Try)
8. Old URLs redirect with 308 status to new locations
9. No broken $schema references (VSCode autocomplete works in all meta.json files)
10. Folder groups (guides) and (advanced) removed
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-01-SUMMARY.md`
</output>
