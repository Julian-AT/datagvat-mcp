---
phase: 10-navigation-simplification
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - .editorconfig
  - .gitignore
  - docs/biome.json
  - docs/tsconfig.json
autonomous: true

must_haves:
  truths:
    - "Developer opens any file in any editor and sees consistent formatting"
    - "Developer runs git status and sees clean working tree (no temp files)"
  artifacts:
    - path: ".editorconfig"
      provides: "Cross-editor consistency settings"
      min_lines: 15
      contains: "indent_style"
    - path: ".gitignore"
      provides: "Updated ignore patterns"
      min_lines: 20
  key_links:
    - from: ".editorconfig"
      to: "biome.json"
      via: "matching format rules"
      pattern: "indent_style.*space"
---

<objective>
Establish cross-editor consistency and clean git working tree by adding .editorconfig (matching Biome settings) and updating .gitignore with comprehensive patterns for Next.js/Bun/Python project.

Purpose: Standardize formatting across all editors (not just VSCode with Biome extension) and prevent temp files from cluttering git status.

Output: .editorconfig with settings matching Biome, updated .gitignore excluding node_modules/.venv/.next/temp files, clean git status.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-RESEARCH.md

# Current configs
@docs/biome.json
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .editorconfig matching Biome settings</name>
  <files>.editorconfig</files>
  <action>
Create .editorconfig at repository root matching existing Biome configuration:

```ini
# EditorConfig is awesome: https://EditorConfig.org

# Top-most EditorConfig file
root = true

# All files
[*]
indent_style = space
indent_size = 2
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true
max_line_length = 100

# Markdown files
[*.md]
trim_trailing_whitespace = false
max_line_length = off

# JSON, YAML files
[*.{json,yml,yaml}]
indent_size = 2

# Python files
[*.py]
indent_size = 4
max_line_length = 88

# Shell scripts
[*.sh]
indent_size = 2
```

**Mapping to Biome (docs/biome.json):**
- `indent_style = space` ← `"indentStyle": "space"`
- `indent_size = 2` ← `"indentWidth": 2`
- `max_line_length = 100` ← `"lineWidth": 100`
- `end_of_line = lf` (Git handles CRLF → LF conversion)
- `charset = utf-8` (default for modern editors)

**Why EditorConfig:**
- Works across ALL editors (VSCode, Vim, IntelliJ, Sublime, etc.) without extension install
- Biome requires VSCode extension or CLI run
- EditorConfig provides baseline, Biome provides enforcement

**Testing:**
After creating file, open any .ts or .tsx file in VSCode and verify:
- Tab key inserts 2 spaces
- Cursor position shows "Spaces: 2" in status bar
- Files save with LF line endings (not CRLF)
  </action>
  <verify>
Run: `ls -la .editorconfig` (file exists)
Run: `grep "indent_style = space" .editorconfig` (matches Biome)
Run: `grep "indent_size = 2" .editorconfig` (matches Biome)

Open VSCode, edit a .tsx file, press Tab, verify 2-space indentation.
  </verify>
  <done>
.editorconfig exists at repository root with settings matching Biome configuration. Cross-editor consistency established.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and update .gitignore</name>
  <files>.gitignore</files>
  <action>
Read current .gitignore and add missing patterns for modern Next.js/Bun/Python project:

**Current .gitignore check:**
```bash
cat .gitignore
```

**Patterns to ensure exist:**

```gitignore
# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
.nyc_output/

# Next.js
.next/
out/
build/
dist/

# Production
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# Debug
.vscode/
.idea/

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json

# Bun
.bun/

# Remotion (for Phase 13)
remotion-cache/
*.mp4
!public/**/*.mp4

# Temporary files (addresses nul, ~/ in git status)
*.tmp
*.temp
.cache/
nul
~/
```

**What NOT to ignore:**
- .planning/ (should be committed based on config.json)
- LICENSE
- README.md
- CONTRIBUTING.md
- .editorconfig

**Verification approach:**
After updating, run `git status` and ensure:
- No .venv/ or node_modules/ appear as untracked
- .planning/ files appear (not ignored)
- Temporary files like nul or ~/ don't appear (now explicitly ignored)
  </action>
  <verify>
Run: `git status` (should not show node_modules, .venv, .next, __pycache__, nul, ~/)
Run: `git check-ignore -v node_modules/package.json` (should be ignored)
Run: `git check-ignore -v .planning/STATE.md` (should NOT be ignored - prints no output)

Check untracked files list: `git status --short | grep "^??"` (should be clean of temp files)
  </verify>
  <done>
.gitignore updated with comprehensive patterns for Next.js, Bun, Python, and temporary files including nul and ~/. Git status shows clean working tree with only intentional files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Consolidate TypeScript and Biome configurations (CLEAN-06)</name>
  <files>docs/biome.json, docs/tsconfig.json</files>
  <action>
Review and consolidate TypeScript and Biome configurations to eliminate conflicts and ensure consistent formatting rules.

**1. Review current configs:**
```bash
cat docs/biome.json
cat docs/tsconfig.json
```

**2. Consolidate Biome configuration:**

Update `docs/biome.json` to ensure all formatting rules are explicit and match .editorconfig:

```json
{
  "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
  "organizeImports": {
    "enabled": true
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  },
  "formatter": {
    "enabled": true,
    "formatWithErrors": false,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100,
    "lineEnding": "lf"
  },
  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "jsxQuoteStyle": "double",
      "trailingCommas": "es5",
      "semicolons": "always"
    }
  }
}
```

**3. Consolidate TypeScript configuration:**

Ensure `docs/tsconfig.json` extends the correct base and has no conflicting rules:

```json
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["./**/*.ts", "./**/*.tsx"],
  "exclude": ["node_modules", ".next", "out"]
}
```

**4. Verify configurations work together:**
```bash
cd docs
bun lint
bun run type-check
```

Both commands should exit 0 with no conflicts between Biome formatting and TypeScript checking.
  </action>
  <verify>
Run: `cd docs && bun lint` (exit code 0, no format conflicts)
Run: `cd docs && bun run type-check` (exit code 0, no type errors)
Run: `grep -E "indentWidth|lineWidth" docs/biome.json` (matches .editorconfig values: 2, 100)
Run: `grep "strict" docs/tsconfig.json` (TypeScript strict mode enabled)

Verify no conflicting rules between configs:
- Indent: .editorconfig (2 spaces) = biome.json (indentWidth: 2) = consistent
- Line width: .editorconfig (100) = biome.json (lineWidth: 100) = consistent
  </verify>
  <done>
Biome and TypeScript configurations consolidated. No conflicting formatting rules. CI lint and type-check commands pass. CLEAN-06 requirement satisfied.
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

**Repository structure:**
```bash
# Files exist
ls -la .editorconfig .gitignore

# No ignored files in git status
git status --short | grep "^??" | grep -E "(node_modules|\.venv|\.next|__pycache__|nul|~)"
```

**EditorConfig validation:**
```bash
# Settings match Biome
grep "indent_size = 2" .editorconfig
grep "indent_style = space" .editorconfig
```

**Config consolidation:**
```bash
cd docs
bun lint
bun run type-check
```
</verification>

<success_criteria>
**Measurable completion:**

1. .editorconfig exists with settings matching Biome (indent_size=2, indent_style=space)
2. .gitignore includes patterns for node_modules, .venv, .next, __pycache__, temp files (nul, ~/)
3. Git status shows clean working tree (no ignored files appearing as untracked)
4. EditorConfig applies in VSCode (Tab inserts 2 spaces)
5. CLEAN-06 TypeScript/Biome configs consolidated (no conflicting rules, lint and type-check pass)
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-04-SUMMARY.md`
</output>
