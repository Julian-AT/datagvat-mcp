---
phase: 10-navigation-simplification
plan: 06
type: execute
wave: 4
depends_on: ["10-04", "10-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Developer runs full build and sees zero TypeScript errors"
    - "Developer runs Biome lint and sees zero errors"
    - "Developer runs backend test suite and all tests pass"
    - "Developer runs full build in under 5 minutes"
  artifacts:
    - path: ".planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/build-verification.txt"
      provides: "Comprehensive build verification report"
      min_lines: 20
  key_links:
    - from: "build verification"
      to: "all modified files"
      via: "TypeScript compilation and lint checks"
      pattern: "bun.*build"
    - from: "test suite"
      to: "backend code"
      via: "pytest execution"
      pattern: "pytest"
---

<objective>
Run comprehensive build verification covering documentation (TypeScript, Biome lint, Next.js build) and backend (pytest), ensuring zero errors across all systems after Phase 10 changes, with build completing in under 5 minutes.

Purpose: Validate that navigation restructure, dependency cleanup, and repository polish haven't broken any functionality. Establish quality baseline before Phase 11.

Output: Verification report showing all checks passed (TypeScript, Biome, link validation, docs build, backend tests), build time < 5 minutes, zero errors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-RESEARCH.md

# Depends on all previous plans
@.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-04-SUMMARY.md
@.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run comprehensive documentation build verification</name>
  <files>.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/build-verification.txt</files>
  <action>
Run full build verification for documentation covering BUILD-01 through BUILD-04:

**CREATE VERIFICATION SCRIPT:**

Create `.planning/phases/10-*/verify-build.sh`:
```bash
#!/bin/bash

# Store results
RESULTS_FILE=".planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/build-verification.txt"
echo "=== BUILD VERIFICATION REPORT ===" > "$RESULTS_FILE"
echo "Date: $(date)" >> "$RESULTS_FILE"
echo "" >> "$RESULTS_FILE"

cd docs

echo "=== DOCUMENTATION BUILD VERIFICATION ==="
echo ""

echo "1. TypeScript type check..."
START_TIME=$(date +%s)
bun run type-check
TS_EXIT=$?
TS_TIME=$(($(date +%s) - START_TIME))

echo ""
echo "2. Biome lint check..."
START_TIME=$(date +%s)
bun lint
LINT_EXIT=$?
LINT_TIME=$(($(date +%s) - START_TIME))

echo ""
echo "3. Link validation..."
START_TIME=$(date +%s)
bun run lint:links
LINK_EXIT=$?
LINK_TIME=$(($(date +%s) - START_TIME))

echo ""
echo "4. Full build..."
START_TIME=$(date +%s)
bun run build
BUILD_EXIT=$?
BUILD_TIME=$(($(date +%s) - START_TIME))

# Write results to file
echo "=== DOCUMENTATION RESULTS ===" >> "$RESULTS_FILE"
echo "TypeScript: $([ $TS_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL (exit $TS_EXIT)") - ${TS_TIME}s" >> "$RESULTS_FILE"
echo "Biome Lint: $([ $LINT_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL (exit $LINT_EXIT)") - ${LINT_TIME}s" >> "$RESULTS_FILE"
echo "Link Check: $([ $LINK_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL (exit $LINK_EXIT)") - ${LINK_TIME}s" >> "$RESULTS_FILE"
echo "Build: $([ $BUILD_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL (exit $BUILD_EXIT)") - ${BUILD_TIME}s" >> "$RESULTS_FILE"
echo "" >> "$RESULTS_FILE"

if [ $BUILD_TIME -gt 300 ]; then
  echo "⚠️  WARNING: Build time ${BUILD_TIME}s exceeds 5 minute target" >> "$RESULTS_FILE"
else
  echo "✓ Build time ${BUILD_TIME}s under 5 minute target" >> "$RESULTS_FILE"
fi

echo ""
echo "=== DOCUMENTATION RESULTS ==="
echo "TypeScript: $([ $TS_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL")"
echo "Biome Lint: $([ $LINT_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL")"
echo "Link Check: $([ $LINK_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL")"
echo "Build: $([ $BUILD_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL") - ${BUILD_TIME}s"

if [ $TS_EXIT -eq 0 ] && [ $LINT_EXIT -eq 0 ] && [ $LINK_EXIT -eq 0 ] && [ $BUILD_EXIT -eq 0 ]; then
  echo ""
  echo "✓ ALL DOCUMENTATION CHECKS PASSED"
  exit 0
else
  echo ""
  echo "✗ SOME DOCUMENTATION CHECKS FAILED"
  exit 1
fi
```

**RUN VERIFICATION:**
```bash
chmod +x .planning/phases/10-*/verify-build.sh
bash .planning/phases/10-*/verify-build.sh
```

**If any check fails:**

1. **TypeScript errors:** Review error messages, fix type issues in affected files
2. **Biome lint errors:** Run `cd docs && bun lint --write` to auto-fix, then manually fix remaining
3. **Link validation errors:** Update broken links identified (should be fixed in 10-02)
4. **Build errors:** Check Next.js error output, fix module resolution or compilation issues

**Success criteria:**
All 4 checks pass (exit code 0), build time under 5 minutes, zero errors in any category.
  </action>
  <verify>
Run: `bash .planning/phases/10-*/verify-build.sh` (should exit with code 0)
Run: `cat .planning/phases/10-*/build-verification.txt` (should show all PASS)
Run: `grep "✓ PASS" .planning/phases/10-*/build-verification.txt | wc -l` (should be 4)
  </verify>
  <done>
Documentation build verification script passes all checks. Zero TypeScript errors, zero Biome lint errors, links validated, full build succeeds in under 5 minutes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run backend test suite</name>
  <files>.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/build-verification.txt</files>
  <action>
Run full backend test suite to ensure Phase 10 changes didn't affect Python code:

**1. Locate backend tests:**
```bash
# Find pytest configuration and test files
ls pytest.ini pyproject.toml setup.py
find . -name "test_*.py" -o -name "*_test.py" | head -10
```

**2. Run backend tests:**
```bash
# Activate Python environment if needed
if [ -d .venv ]; then
  source .venv/bin/activate
fi

# Run pytest with coverage (if configured)
pytest --verbose --tb=short
PYTEST_EXIT=$?

# Or without coverage if not configured
# pytest -v
```

**3. Document results:**
Append to `.planning/phases/10-*/build-verification.txt`:
```bash
echo "" >> build-verification.txt
echo "=== BACKEND TEST RESULTS ===" >> build-verification.txt
echo "pytest: $([ $PYTEST_EXIT -eq 0 ] && echo "✓ PASS" || echo "✗ FAIL (exit $PYTEST_EXIT)")" >> build-verification.txt

# If pytest ran successfully, capture summary
if [ $PYTEST_EXIT -eq 0 ]; then
  pytest --collect-only -q 2>/dev/null | tail -1 >> build-verification.txt
fi
```

**4. If tests fail:**

- Review pytest output for failure details
- Check if failures are related to Phase 10 changes (unlikely, but check imports)
- If failures exist before Phase 10: Document as pre-existing, don't block
- If new failures: Fix before completing phase

**5. If no test suite exists:**

Document in build-verification.txt:
```
=== BACKEND TEST RESULTS ===
pytest: N/A - No test suite configured yet
Note: Backend testing will be added in future phase
```

This is acceptable for Phase 10 (no backend changes made).
  </action>
  <verify>
Run: `pytest --collect-only -q` (shows test count or "no tests found")
Run: `grep "BACKEND TEST RESULTS" .planning/phases/10-*/build-verification.txt` (section exists)

If tests exist:
Run: `pytest -v` (should show all passed or document pre-existing failures)
  </verify>
  <done>
Backend test suite executed (or documented as N/A if doesn't exist). BUILD-05 requirement satisfied. Results appended to build-verification.txt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 10: 3-tab navigation, duplicate title fixes, professional README/CONTRIBUTING, editor config, cleaned dependencies, and verified build quality
  </what-built>
  <how-to-verify>
**1. Navigation verification:**
   - Run `bun run dev` from docs directory
   - Visit http://localhost:3000/docs
   - Verify exactly 3 tabs: Docs, API, Try
   - Click through each section, verify all content loads
   - Test old URLs redirect correctly (e.g., /docs/getting-started → /docs/docs/getting-started)

**2. Title rendering verification:**
   - Visit 5-10 random documentation pages
   - Each page should have exactly ONE H1 (from frontmatter title)
   - No duplicate titles visible

**3. README/Contributing verification:**
   - View README.md in repository root
   - Verify badges display correctly (5+ shields.io badges)
   - Verify Quick Start section is clear (3 steps)
   - View CONTRIBUTING.md, verify development setup instructions

**4. Editor consistency verification:**
   - Open a .tsx file in VSCode
   - Press Tab key
   - Verify 2-space indentation (not 4 spaces or tabs)
   - Check status bar shows "Spaces: 2"

**5. Repository cleanliness:**
   - Run `git status`
   - Should not show: node_modules, .venv, .next, __pycache__, nul, ~/
   - Should show: only intentional uncommitted files

**6. Build verification:**
   - Review `cat .planning/phases/10-*/build-verification.txt`
   - All checks should show "✓ PASS":
     - [ ] TypeScript type check passes
     - [ ] Biome lint passes
     - [ ] Link validation passes
     - [ ] Full build completes successfully
     - [ ] Backend tests pass (or N/A if no tests)
   - Build time under 5 minutes

**7. Final smoke test:**
   - Visit documentation site at localhost:3000
   - Navigate through Docs tab sections (getting-started, guides, workflows, etc.)
   - Navigate through API tab sections (tools, openapi)
   - Click Try link (should go to /try page)
   - All navigation smooth, no broken links, no console errors
   - Mobile viewport test (375px, 768px, 1920px) - navigation accessible at all sizes
  </how-to-verify>
  <resume-signal>
Type "approved" if all 7 verification steps pass and build verification report shows all ✓ PASS, or describe any issues found for fixing
  </resume-signal>
</task>

</tasks>

<verification>
## Overall Phase Checks

**Build verification report:**
```bash
cat .planning/phases/10-*/build-verification.txt

# Should show:
# - TypeScript: ✓ PASS
# - Biome Lint: ✓ PASS
# - Link Check: ✓ PASS
# - Build: ✓ PASS (under 5 minutes)
# - Backend: ✓ PASS or N/A
```

**All Phase 10 requirements satisfied:**
- NAV-01: 3 tabs visible
- NAV-02: No duplicate H1s
- NAV-03: Redirects working
- NAV-04: Mobile navigation tested
- NAV-05: Links validated
- README-01 through README-07: Professional README exists
- CLEAN-01: Unused file detection completed
- CLEAN-02 through CLEAN-05: Repository cleaned
- BUILD-01 through BUILD-05: Build verified (including backend tests)
</verification>

<success_criteria>
**Measurable completion:**

1. Build verification script created and runs successfully
2. Documentation TypeScript check passes (exit 0)
3. Documentation Biome lint passes (exit 0)
4. Documentation link validation passes (exit 0)
5. Documentation build succeeds (exit 0)
6. Documentation build time under 5 minutes
7. Backend test suite executed (pytest) or documented as N/A
8. Build verification report created with all results
9. Report shows all checks passed (or N/A for missing tests)
10. Human verification approved (7-step checklist completed)
11. Phase 10 complete with zero regressions from navigation changes
</success_criteria>

<output>
After completion, create `.planning/phases/10-navigation-simplification---streamline-from-8-tabs-to-3,-fix-duplicate-titles,-polish-readme-and-repository-structure/10-06-SUMMARY.md`
</output>
