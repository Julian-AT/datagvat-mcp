---
phase: 18-documentation-foundation
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - docs/app/[lang]/docs/[[...slug]]/page.tsx
  - docs/app/[lang]/docs/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Documentation pages load in under 2 seconds on standard connections"
    - "Build generates static pages for all routes"
    - "In-page table of contents renders for pages with headings"
    - "Breadcrumb navigation shows current location"
    - "Previous/Next navigation works between pages"
  artifacts:
    - path: "docs/app/[lang]/docs/[[...slug]]/page.tsx"
      provides: "Dynamic page component with static generation"
      contains: "generateStaticParams"
    - path: "docs/app/[lang]/docs/layout.tsx"
      provides: "DocsLayout with breadcrumbs and navigation"
      contains: "DocsLayout"
  key_links:
    - from: "docs/app/[lang]/docs/[[...slug]]/page.tsx"
      to: "source.getPage"
      via: "page data fetching"
      pattern: "source\\.getPage"
    - from: "docs/app/[lang]/docs/layout.tsx"
      to: "source.getPageTree"
      via: "navigation tree generation"
      pattern: "source\\.getPageTree"
---

<objective>
Verify and optimize documentation build performance to meet FOUND-07 (<2s page loads) with static generation for all routes, plus validate FOUND-03, FOUND-04, FOUND-05 navigation features.

Purpose: Ensure documentation infrastructure can handle 60-80 comprehensive pages without performance degradation. Catch any build configuration issues before content creation phases.

Output: Optimized build configuration with verified navigation features (TOC, breadcrumbs, prev/next) and sub-2-second page loads.
</objective>

<execution_context>
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\workflows\execute-plan.md
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\STATE.md
@C:\GitHub\datagvat-mcp\.planning\REQUIREMENTS.md
@C:\GitHub\datagvat-mcp\.planning\phases\18-documentation-foundation\18-RESEARCH.md

Research context - Pitfall 5 (Slow Page Loads) from 18-RESEARCH.md covers generateStaticParams pattern.

Existing layout components:
@C:\GitHub\datagvat-mcp\docs\app\[lang]\docs\layout.tsx
@C:\GitHub\datagvat-mcp\docs\app\[lang]\docs\[[...slug]]\page.tsx
</context>

<tasks>

<task type="auto">
  <name>Verify static generation configuration in page.tsx</name>
  <files>
    docs/app/[lang]/docs/[[...slug]]/page.tsx
  </files>
  <action>
Read existing docs/app/[lang]/docs/[[...slug]]/page.tsx and verify:

1. **generateStaticParams exists**:
   ```typescript
   export function generateStaticParams() {
     return source.generateParams('slug', 'lang');
   }
   ```

2. **revalidate is false** (static, not ISR):
   ```typescript
   export const revalidate = false;
   ```

3. **DocsPage receives toc prop** (FOUND-03 requirement):
   ```typescript
   const { body: Mdx, toc } = await page.data.load();

   <DocsPage toc={toc} ...>
   ```

4. **tableOfContent configured with style** (optional but improves UX):
   ```typescript
   <DocsPage
     toc={toc}
     tableOfContent={{ style: 'clerk', single: false }}
   >
   ```

If any of these are missing, add them. If generateStaticParams uses wrong parameters, fix it.

Verify page.tsx handles not found correctly:
```typescript
if (!page) return notFound();
```

DO NOT change the core routing structure or break existing functionality. Only add missing static generation config.
  </action>
  <verify>
grep "generateStaticParams" docs/app/[lang]/docs/[[...slug]]/page.tsx
grep "revalidate.*false" docs/app/[lang]/docs/[[...slug]]/page.tsx
grep "toc={toc}" docs/app/[lang]/docs/[[...slug]]/page.tsx
  </verify>
  <done>
page.tsx has generateStaticParams, revalidate: false, and passes toc to DocsPage for table of contents rendering
  </done>
</task>

<task type="auto">
  <name>Verify DocsLayout navigation features in layout.tsx</name>
  <files>
    docs/app/[lang]/docs/layout.tsx
  </files>
  <action>
Read existing docs/app/[lang]/docs/layout.tsx and verify DocsLayout configuration includes:

1. **Page tree for navigation** (FOUND-01):
   ```typescript
   <DocsLayout tree={source.getPageTree(lang)} ...>
   ```

2. **Breadcrumb navigation enabled** (FOUND-04) - enabled by default via nav prop:
   ```typescript
   <DocsLayout
     nav={{ title: <Logo /> }}  // Breadcrumbs auto-rendered
   >
   ```

3. **Sidebar configuration for prev/next** (FOUND-05) - enabled by default:
   ```typescript
   sidebar={{
     collapsible: true,  // Enables prev/next navigation
     defaultOpenLevel: 1,
   }}
   ```

4. **Mobile responsive** (FOUND-06) - DocsLayout handles this automatically, but verify no custom CSS overrides breaking mobile.

If any configuration is missing or incorrect, update it. DO NOT break existing i18n or theme configuration.

Verify layout structure:
- Root layout provides <html> wrapper
- [lang] layout provides DocsLayout with page tree
- No conflicting layout nesting
  </action>
  <verify>
grep "DocsLayout" docs/app/[lang]/docs/layout.tsx
grep "tree={source.getPageTree" docs/app/[lang]/docs/layout.tsx
grep "nav={{" docs/app/[lang]/docs/layout.tsx
  </verify>
  <done>
DocsLayout configured with page tree, breadcrumbs (via nav), sidebar with prev/next navigation, all navigation features enabled
  </done>
</task>

<task type="auto">
  <name>Build verification and performance validation</name>
  <files>
    None (verification only)
  </files>
  <action>
Run full documentation build and verify static generation:

```bash
cd docs && npm run build
```

Check build output for:

1. **Static page generation**:
   - Look for "○ (Static)" markers in build output
   - Count total static pages generated
   - Verify no "λ (Dynamic)" pages for documentation routes

2. **Build time**:
   - Measure total build time
   - Should complete in reasonable time (<5 min for current content)

3. **Bundle size**:
   - Check First Load JS size in build output
   - Documentation pages should be <100KB First Load JS

4. **Navigation features in build**:
   - Verify page tree generated successfully
   - Check for any build warnings about TOC, breadcrumbs, or navigation

Example expected output:
```
Route (app)                                Size     First Load JS
┌ ○ /[lang]/docs                          142 B          87.2 kB
├ ○ /[lang]/docs/getting-started          142 B          87.2 kB
├ ○ /[lang]/docs/guides                   142 B          87.2 kB
...
○  (Static)  prerendered as static content
```

If build fails or shows dynamic pages, investigate configuration issues.

After successful build, run production server briefly to verify page load times:

```bash
npm run start &
sleep 5
curl -o /dev/null -s -w "Time: %{time_total}s\n" http://localhost:3000/docs/getting-started
# Should be <2s for static pages
```

Document build results: total static pages, build time, any warnings.
  </action>
  <verify>
cd docs && npm run build 2>&1 | grep -c "○.*Static" (should return >0 indicating static pages)
cd docs && npm run build 2>&1 | grep -c "Build completed" (should succeed)
  </verify>
  <done>
Build succeeds, generates static pages for all documentation routes, no dynamic pages, build output shows navigation features configured correctly
  </done>
</task>

</tasks>

<verification>
Build verification:
```bash
cd docs
npm run build
```

Expected results:
1. Build completes successfully with zero errors
2. All /docs/* routes show "○ (Static)" marker
3. Build time <5 minutes
4. First Load JS <100KB per page
5. No warnings about missing TOC, breadcrumbs, or navigation

Manual verification (after build):
```bash
npm run start
```

1. Visit http://localhost:3000/docs/getting-started
   - Page loads in <2 seconds (measure in Network tab)
   - In-page TOC appears on right sidebar
   - Breadcrumb shows "Getting Started" path at top
   - Prev/Next navigation appears at bottom

2. Visit http://localhost:3000/examples/component-showcase
   - Verify same navigation features
   - Check TOC includes all H2/H3 headings
   - Click prev/next links → navigation works

3. Mobile test (resize to 375px width):
   - Sidebar collapses to hamburger menu
   - TOC remains accessible
   - Navigation doesn't break layout

Performance validation:
```bash
# Use Chrome DevTools or curl to measure
curl -o /dev/null -s -w "Time: %{time_total}s\n" http://localhost:3000/docs/getting-started
# Should show Time: <2.0s
```
</verification>

<success_criteria>
- npm run build succeeds with zero errors
- All documentation routes generate as static pages (○ marker)
- In-page table of contents renders for pages with headings (FOUND-03)
- Breadcrumb navigation shows current location (FOUND-04)
- Previous/Next navigation works between pages (FOUND-05)
- Documentation pages load in under 2 seconds (FOUND-07)
- Mobile responsive layout works correctly (FOUND-06)
- Build output shows no warnings about navigation features
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-foundation/18-03-SUMMARY.md` documenting:
- Build performance metrics (time, page count, bundle sizes)
- Navigation feature verification results (TOC, breadcrumbs, prev/next)
- Page load time measurements (actual times from testing)
- Any optimization opportunities identified
- Build output summary (static vs dynamic pages)
</output>
