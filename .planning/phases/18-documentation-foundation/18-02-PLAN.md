---
phase: 18-documentation-foundation
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - docs/examples/component-showcase.mdx
  - docs/examples/meta.json
  - docs/components/mdx/mermaid.tsx
autonomous: true

must_haves:
  truths:
    - "User sees Tabs component switch between Basic and Advanced examples with state persistence"
    - "User sees Accordion component expand/collapse tool documentation sections"
    - "User sees Mermaid diagram render workflow visualization"
    - "Interactive components work on mobile viewport (<768px)"
  artifacts:
    - path: "docs/examples/component-showcase.mdx"
      provides: "Sample page demonstrating all interactive components"
      min_lines: 100
    - path: "docs/components/mdx/mermaid.tsx"
      provides: "Mermaid component for workflow diagrams"
      exports: ["Mermaid"]
  key_links:
    - from: "docs/examples/component-showcase.mdx"
      to: "fumadocs-ui/components/tabs"
      via: "import statement"
      pattern: "import.*Tabs.*from.*fumadocs-ui"
    - from: "docs/examples/component-showcase.mdx"
      to: "fumadocs-ui/components/accordion"
      via: "import statement"
      pattern: "import.*Accordion.*from.*fumadocs-ui"
---

<objective>
Verify all interactive Fumadocs components (Tabs, Accordion, Mermaid) render correctly with sample Austria MCP documentation content.

Purpose: Prove that COMP-01 (Tabs), COMP-05 (Accordion), and COMP-06 (Mermaid) requirements are met before content creation phases begin. Identify any component configuration issues early.

Output: Working component showcase page demonstrating progressive disclosure patterns, scannable reference layout, and workflow visualization.
</objective>

<execution_context>
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\workflows\execute-plan.md
@C:\GitHub\datagvat-mcp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\GitHub\datagvat-mcp\.planning\PROJECT.md
@C:\GitHub\datagvat-mcp\.planning\ROADMAP.md
@C:\GitHub\datagvat-mcp\.planning\STATE.md
@C:\GitHub\datagvat-mcp\.planning\REQUIREMENTS.md
@C:\GitHub\datagvat-mcp\.planning\phases\18-documentation-foundation\18-RESEARCH.md

Research context - Pattern 3 (Tabs), Pattern 4 (Accordion), Pattern 5 (Mermaid) from 18-RESEARCH.md provide implementation examples.

Existing component:
@C:\GitHub\datagvat-mcp\docs\components\mdx\mermaid.tsx
</context>

<tasks>

<task type="auto">
  <name>Create component showcase page with Tabs, Accordion, and Mermaid examples</name>
  <files>
    docs/examples/component-showcase.mdx
    docs/examples/meta.json
  </files>
  <action>
Create docs/examples/ directory if not exists: mkdir -p docs/examples

Create docs/examples/meta.json:
```json
{
  "$schema": "../.source/json-schema/docs.meta.json",
  "title": "Examples",
  "description": "Example documentation patterns",
  "icon": "FileCode",
  "root": false,
  "pages": ["component-showcase"]
}
```

Add "examples" to docs/meta.json pages array (after "workflows", before "api").

Create docs/examples/component-showcase.mdx with comprehensive component testing:

```mdx
---
title: Component Showcase
description: Demonstration of Fumadocs interactive components for Austria MCP documentation
---

import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Mermaid } from '@/components/mdx/mermaid';

# Component Showcase

This page demonstrates all interactive components used throughout Austria MCP documentation.

## Progressive Disclosure with Tabs

<Tabs items={['Basic', 'Advanced']} groupId="search-complexity" persist>
  <Tab value="Basic">
    ### Basic Dataset Search

    Simple text search for datasets:

    ```typescript
    const results = await mcp.callTool('search_datasets', {
      query: 'gesundheit wien'
    });

    console.log(`Found ${results.count} datasets`);
    ```

    This returns up to 20 datasets matching "gesundheit wien".
  </Tab>

  <Tab value="Advanced">
    ### Advanced Search with Filters

    Search with faceted filtering and quality boost:

    ```typescript
    const results = await mcp.callTool('search_datasets', {
      query: 'gesundheit wien',
      filters: {
        organization: 'Stadt Wien',
        tags: ['Gesundheit', 'COVID-19'],
        license: 'cc-by'
      },
      limit: 50,
      quality_boost: true
    });

    // Access facets for filter UI
    console.log('Available orgs:', results.facets.organization);
    ```

    Quality boost re-ranks results by metadata completeness.
  </Tab>
</Tabs>

**Tab behavior verification:**
- Switch tabs → selection persists across page reload (localStorage)
- `groupId="search-complexity"` → same selection on other pages with same groupId
- Mobile responsive → tabs stack vertically on narrow screens

---

## Scannable Reference with Accordion

<Accordions type="single" collapsible>
  <Accordion title="search_datasets" id="search-datasets">
    Full-text search across the Data.gv.at catalog with faceted filtering.

    ### Parameters

    <TypeTable type={{
      query: {
        type: "string",
        description: "Search query for titles, descriptions, keywords"
      },
      limit: {
        type: "number",
        default: 20,
        description: "Maximum number of results (1-100)"
      },
      filters: {
        type: "object",
        description: "Faceted filters (organization, tags, license, format)",
        default: {}
      },
      quality_boost: {
        type: "boolean",
        default: false,
        description: "Re-rank by metadata quality (DQV score)"
      }
    }} />

    ### Returns

    ```json
    {
      "count": 42,
      "results": [
        {
          "id": "dataset-id",
          "title": "Gesundheitsdaten Wien",
          "description": "...",
          "tags": ["Gesundheit"],
          "organization": "Stadt Wien"
        }
      ],
      "facets": {
        "organization": ["Stadt Wien", "..."],
        "tags": ["Gesundheit", "COVID-19"]
      }
    }
    ```

    ### Example

    ```typescript
    const results = await mcp.callTool('search_datasets', {
      query: 'health',
      limit: 10
    });
    ```
  </Accordion>

  <Accordion title="get_dataset" id="get-dataset">
    Retrieve detailed metadata for a specific dataset by ID.

    ### Parameters

    <TypeTable type={{
      id: {
        type: "string",
        description: "Dataset identifier from search results"
      }
    }} />

    ### Returns

    Full dataset metadata including distributions, quality metrics, and temporal coverage.

    ### Example

    ```typescript
    const dataset = await mcp.callTool('get_dataset', {
      id: 'some-dataset-id'
    });

    console.log('Quality score:', dataset.quality.score);
    ```
  </Accordion>

  <Accordion title="preview_dataset" id="preview-dataset">
    Preview dataset contents (first 64KB) with schema inference.

    ### Parameters

    <TypeTable type={{
      url: {
        type: "string",
        description: "Distribution URL (CSV, JSON, XML)"
      },
      format: {
        type: "string",
        description: "Data format (auto-detected if omitted)"
      }
    }} />

    ### Example

    ```typescript
    const preview = await mcp.callTool('preview_dataset', {
      url: 'https://data.gv.at/.../file.csv'
    });

    console.log('Columns:', preview.schema.columns);
    console.log('Sample:', preview.sample_data);
    ```
  </Accordion>
</Accordions>

**Accordion behavior verification:**
- Only one accordion open at a time (`type="single"`)
- URL hash `#search-datasets` auto-expands correct accordion
- Keyboard navigation: Tab/Enter to expand, Escape to collapse
- Mobile responsive → full-width on narrow screens

---

## Workflow Visualization with Mermaid

<Mermaid chart={`
graph TD
    A[User Query: 'health datasets Vienna'] --> B{Search Strategy}
    B -->|Simple| C[search_datasets]
    B -->|Semantic| D[semantic_search_datasets]

    C --> E[Results: 15 datasets]
    D --> E

    E --> F{User Action}
    F -->|Inspect Dataset| G[get_dataset]
    F -->|Preview Data| H[preview_dataset]
    F -->|Find Similar| I[find_related_datasets]

    G --> J[Full Metadata]
    H --> K[Sample Data + Schema]
    I --> L[Related Datasets]

    J --> M{Quality Check}
    M -->|High Quality| N[Download]
    M -->|Low Quality| E

    style A fill:#e1f5ff
    style E fill:#fff4e1
    style N fill:#e8f5e9
`} />

**Mermaid diagram verification:**
- Renders correctly in light/dark mode (theme-aware)
- Syntax highlighting for node labels
- No hydration errors (client-side rendering)
- Mobile responsive → diagram scales to fit viewport

---

## Component Integration Test Results

After loading this page:

1. **Tabs Component (COMP-01)**
   - [ ] Switches between Basic/Advanced
   - [ ] Selection persists after page reload
   - [ ] Mobile responsive (stacks on <768px)

2. **Accordion Component (COMP-05)**
   - [ ] Only one accordion open at a time
   - [ ] URL hash navigation works (#search-datasets)
   - [ ] Keyboard accessible (Tab/Enter/Escape)

3. **Mermaid Component (COMP-06)**
   - [ ] Diagram renders without errors
   - [ ] Theme switches (light/dark) correctly
   - [ ] Mobile responsive (scales to viewport)

4. **TypeTable Component (COMP-03)**
   - [ ] Parameter tables render with types
   - [ ] Default values shown
   - [ ] Descriptions formatted correctly

All components should work without JavaScript errors in browser console.
```

This creates a comprehensive test page covering all interactive component requirements (COMP-01, COMP-03, COMP-05, COMP-06).
  </action>
  <verify>
ls docs/examples/component-showcase.mdx docs/examples/meta.json
grep "Tabs.*from.*fumadocs-ui" docs/examples/component-showcase.mdx
grep "Accordion.*from.*fumadocs-ui" docs/examples/component-showcase.mdx
grep "Mermaid.*from.*@/components" docs/examples/component-showcase.mdx
  </verify>
  <done>
Component showcase page exists with working examples of Tabs, Accordion, Mermaid, and TypeTable components demonstrating Austria MCP use cases
  </done>
</task>

<task type="auto">
  <name>Verify Mermaid component exists and is properly configured</name>
  <files>
    docs/components/mdx/mermaid.tsx
  </files>
  <action>
Verify that docs/components/mdx/mermaid.tsx exists and exports Mermaid component:

```bash
cat docs/components/mdx/mermaid.tsx | grep "export.*Mermaid"
```

If component doesn't exist or has issues, review existing implementation from research context.

Expected component should:
1. Use 'use client' directive (client-side rendering)
2. Import mermaid library dynamically
3. Switch theme based on next-themes
4. Render SVG output in container div

Verify component is registered in mdx-components.tsx:

```bash
grep -r "Mermaid" docs/mdx-components.tsx
```

If not registered, add to component registry so it's available in MDX files.
  </action>
  <verify>
cat docs/components/mdx/mermaid.tsx (should exist and contain Mermaid export)
grep "export.*Mermaid" docs/components/mdx/mermaid.tsx
  </verify>
  <done>
Mermaid component exists, properly exports Mermaid function, and is available for use in MDX files
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Run `npm run dev` in docs/ directory
2. Navigate to http://localhost:3000/examples/component-showcase
3. Test Tabs component:
   - Click "Advanced" tab → content switches
   - Reload page → "Advanced" still selected (localStorage persistence)
   - Resize browser to <768px → tabs remain functional
4. Test Accordion component:
   - Click "search_datasets" → expands
   - Click "get_dataset" → previous closes, this opens
   - Click URL with #get-dataset hash → auto-expands that accordion
5. Test Mermaid component:
   - Workflow diagram renders
   - Toggle light/dark mode → diagram theme updates
   - Resize to mobile → diagram scales appropriately
6. Check browser console → no JavaScript errors

Build verification:
```bash
cd docs && npm run build
# Should succeed, component showcase page renders statically
```
</verification>

<success_criteria>
- Component showcase page exists at /examples/component-showcase
- Tabs component switches between Basic/Advanced with state persistence
- Accordion component expands/collapses with URL hash support
- Mermaid diagram renders workflow visualization
- TypeTable component displays parameter documentation
- All components work on mobile viewport (<768px)
- Browser console shows no JavaScript errors
- Build succeeds with static generation
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-foundation/18-02-SUMMARY.md` documenting:
- Component verification results (which worked, any issues)
- Browser/mobile testing results
- Any component configuration adjustments needed
- Screenshots or console output showing successful rendering
</output>
