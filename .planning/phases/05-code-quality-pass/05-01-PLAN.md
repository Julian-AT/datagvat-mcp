---
phase: 05-code-quality-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/**/*.ts
  - docs/**/*.tsx
  - docs/components/**/*
  - docs/lib/**/*
  - docs/scripts/**/*
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Biome lint passes with zero warnings"
    - "TypeScript type-check passes with zero errors"
    - "All safe fixes applied automatically"
    - "Unsafe fixes reviewed before application"
  artifacts:
    - path: "docs/**/*.ts"
      provides: "Linted TypeScript source files"
    - path: "docs/**/*.tsx"
      provides: "Linted React component files"
  key_links:
    - from: "bun run lint"
      to: "biome.json"
      via: "Biome configuration"
      pattern: "check \\."
---

<objective>
Fix all Biome linting warnings in the docs codebase, enabling clean builds without `--no-verify`.

Purpose: Phase 1 installed Biome but left 214 warnings in place. This plan resolves those warnings using Biome's safe/unsafe fix distinction to ensure code quality without introducing regressions.

Output: Zero Biome warnings, TypeScript builds pass, validation scripts succeed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-code-quality-pass/05-CONTEXT.md
@.planning/phases/05-code-quality-pass/05-RESEARCH.md
@docs/biome.json
@docs/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Biome safe auto-fixes</name>
  <files>docs/**/*.ts, docs/**/*.tsx</files>
  <action>
Run safe auto-fixes which are guaranteed to not change code semantics:

1. Navigate to docs directory
2. Run baseline lint check to capture current warnings: `bun run lint 2>&1 | tee lint-baseline.txt`
3. Apply safe fixes: `bun run lint:fix`
4. Review changes with `git diff`
5. Run lint again to see remaining warnings: `bun run lint`
6. Commit safe fixes: "fix(05-01): apply Biome safe auto-fixes"

Safe fixes include:
- Import ordering
- Quote style normalization
- Semicolon consistency
- Formatting corrections
- Simple style rule fixes

These changes are purely syntactic and do not affect runtime behavior.
  </action>
  <verify>
- `bun run lint:fix` completes without errors
- `git diff` shows only formatting/style changes
- Commit created successfully
  </verify>
  <done>
Safe Biome fixes applied and committed. Remaining warning count documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply reviewed unsafe fixes</name>
  <files>docs/**/*.ts, docs/**/*.tsx</files>
  <action>
If warnings remain after safe fixes, apply unsafe fixes with careful review:

1. Run unsafe fixes: `bunx biome check --write --unsafe .`
2. Review EVERY file changed with `git diff`
3. Watch for concerning patterns:
   - Variable renames in exported functions
   - Changes to function signatures
   - Modified string literals
   - Import reordering that might affect initialization
4. Run type-check to verify no type errors: `bun run type-check`
5. Run lint to verify zero warnings: `bun run lint`
6. If any semantic changes found, revert those specific files and add biome-ignore comments with explanation
7. Commit: "fix(05-01): apply reviewed Biome unsafe fixes"

Note: Most warnings are useBlockStatements (style) which are safe. Watch for noExplicitAny fixes that might change behavior.
  </action>
  <verify>
- `bun run lint` shows 0 warnings
- `bun run type-check` passes (exit code 0)
- No functional changes to exported APIs
- Commit created successfully
  </verify>
  <done>
All Biome warnings resolved. TypeScript type-check passes. Zero warnings in lint output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate build pipeline</name>
  <files>docs/</files>
  <action>
Verify the complete build pipeline works without `--no-verify`:

1. Run full validation: `bun run validate`
   - This runs: lint && lint:links && type-check
2. Run build: `bun run build`
   - This runs: prebuild && next build && postbuild
3. Verify .next/ directory created with expected structure
4. Check build output for any remaining warnings or errors
5. If build fails, identify the failing component and fix it

Common issues:
- TypeScript errors in components/ai/search.tsx mentioned in blockers - fix if present
- Missing type declarations - add @types packages if needed
- Import resolution issues - fix paths

If build succeeds, document:
- Total files linted
- Build time
- Any remaining non-blocking warnings
  </action>
  <verify>
- `bun run validate` exits with code 0
- `bun run build` completes successfully without `--no-verify`
- .next/ directory exists with build output
  </verify>
  <done>
Build pipeline fully functional. Validation passes, build succeeds, ready for commit without `--no-verify` flag.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

```bash
cd docs

# Verify zero warnings
bun run lint
# Expected: Checked X files in Y. No errors or warnings.

# Verify type-check
bun run type-check
# Expected: No output (success)

# Verify full validation
bun run validate
# Expected: All checks pass

# Verify build
bun run build
# Expected: Build completes, .next/ populated
```
</verification>

<success_criteria>
- `bun run lint` reports 0 errors and 0 warnings
- `bun run type-check` passes with exit code 0
- `bun run validate` passes all checks
- `bun run build` succeeds without any flags
- All changes committed with descriptive messages
- CODE-06 requirement satisfied (Fix all 214 Biome warnings)
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality-pass/05-01-SUMMARY.md`
</output>
