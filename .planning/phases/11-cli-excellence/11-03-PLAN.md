---
phase: 11-cli-excellence
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - packages/cli/package.json
  - packages/cli/CHANGELOG.md
  - docs/scripts/type-check.ts
  - docs/scripts/quality-check.ts
  - packages/cli/README.md
autonomous: true

must_haves:
  truths:
    - "User runs CLI in CI environment and commands succeed without interactive prompts"
    - "Developer runs full build and sees zero TypeScript errors, zero Biome lint errors, all tests passing"
  artifacts:
    - path: "packages/cli/package.json"
      provides: "Semantic versioning with proper version number"
      contains: '"version": "0.2.0"'
    - path: "packages/cli/CHANGELOG.md"
      provides: "Documented changes for v0.2.0 release"
      min_lines: 30
      contains: "## [0.2.0]"
    - path: "packages/cli/README.md"
      provides: "Updated documentation with new commands"
      contains: "update"
      and_contains: "doctor"
  key_links:
    - from: "packages/cli/package.json"
      to: "CHANGELOG.md"
      via: "version number consistency"
      pattern: "0\\.2\\.0"
    - from: "packages/cli/README.md"
      to: "new commands"
      via: "documentation of update and doctor"
      pattern: "update|doctor"
---

<objective>
Finalize Phase 11 with semantic versioning, comprehensive documentation, and full build verification ensuring zero regressions from CLI enhancements.

Purpose: Ensure CLI changes follow semantic versioning (minor bump for new features), are properly documented for users, and pass all quality gates. Every stakeholder (users, developers, CI systems) should have confidence in the new capabilities.

Output: Updated package.json to v0.2.0, comprehensive CHANGELOG.md documenting all Phase 11 changes, enhanced README.md with usage examples for new commands, verified TypeScript compilation, passing Biome lint, passing CLI tests, and verified docs build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-excellence/11-RESEARCH.md

# Previous plan outputs
@.planning/phases/11-cli-excellence/11-01-SUMMARY.md
@.planning/phases/11-cli-excellence/11-02-SUMMARY.md

# Current state
@packages/cli/package.json
@packages/cli/README.md

# Build verification scripts
@docs/scripts/type-check.ts
@docs/scripts/quality-check.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic versioning and CHANGELOG</name>
  <files>
    packages/cli/package.json
    packages/cli/CHANGELOG.md
  </files>
  <action>
Update package.json version from 0.1.0 to 0.2.0:
- This is a minor version bump (new features: update, doctor commands)
- No breaking changes (existing init/add commands unchanged)
- Follows semver: MAJOR.MINOR.PATCH

Create CHANGELOG.md following Keep a Changelog format:

```markdown
# Changelog

All notable changes to @datagvat/mcp-installer will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.2.0] - 2026-01-23

### Added

- **Validation**: Zod schema validation for all command inputs with descriptive error messages
- **CI Detection**: Automatic detection of CI/CD environments with `--yes` mode requirement
- **Update Command**: `datagvat-mcp update` shows diff preview before applying configuration changes
- **Doctor Command**: `datagvat-mcp doctor` runs health checks with diagnostic information and fix suggestions
- **Error Messages**: Structured error formatting with problem + fix + example pattern
- **Non-Interactive Mode**: Full support for CI/CD pipelines with automatic fallbacks

### Changed

- Enhanced `init` command with inline validation and immediate feedback on invalid inputs
- Improved error messages to include actionable fix suggestions and examples
- Updated dependencies: added zod, ci-info, jsdiff, execa

### Fixed

- CI environments now properly detected and handle gracefully without hanging on prompts

## [0.1.0] - 2026-01-22

### Added

- Initial release with shadcn-inspired CLI installer
- `init` command for one-command setup of data.gv.at MCP Server
- `add` command for configuring specific tools
- Support for Claude Desktop, Continue, and Cline
- Automatic tool detection and configuration
- Interactive prompts with beautiful UI (chalk + ora + @inquirer/prompts)

[0.2.0]: https://github.com/yourusername/datagvat-mcp/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/yourusername/datagvat-mcp/releases/tag/v0.1.0
```

Ensure version consistency: package.json and CHANGELOG.md both show 0.2.0.
Use current date (2026-01-23) for release date.
  </action>
  <verify>
Check version consistency:
```bash
cd packages/cli
grep '"version"' package.json  # Should show 0.2.0
grep '## \[0.2.0\]' CHANGELOG.md  # Should exist
```

Verify changelog structure:
- Has proper headings (Added, Changed, Fixed)
- Describes all new features (validation, CI detection, update, doctor)
- Includes comparison link at bottom
</verify>
  <done>
- package.json version is 0.2.0
- CHANGELOG.md exists with complete v0.2.0 entry
- Changelog follows Keep a Changelog format
- All Phase 11 features documented under "Added" section
- Comparison links present at bottom
</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI README with new commands</name>
  <files>
    packages/cli/README.md
  </files>
  <action>
Enhance README.md with comprehensive documentation for new commands.

**If README.md doesn't exist, create from scratch. If exists, enhance it.**

Structure:
```markdown
# @datagvat/mcp-installer

shadcn-like one-command installer for data.gv.at MCP Server

## Features

- ✨ One-command setup for AI tools (Claude Desktop, Continue, Cline)
- 🔍 Automatic tool detection and configuration
- ✅ Schema validation with helpful error messages
- 🔄 Update command with diff preview
- 🏥 Health check diagnostics with fix suggestions
- 🤖 Full CI/CD support with non-interactive mode
- 🎨 Beautiful terminal UI with progress indicators

## Installation

```bash
npm install -g @datagvat/mcp-installer
# or
npx @datagvat/mcp-installer init
```

## Commands

### `init` - Initialize MCP Server

Configure data.gv.at MCP Server in your AI tools.

```bash
# Interactive mode (prompts for tool selection)
datagvat-mcp init

# Configure all detected tools (skip prompts)
datagvat-mcp init --yes

# Configure specific tool only
datagvat-mcp init --tool claude-desktop

# CI/CD mode (requires --yes flag)
CI=true datagvat-mcp init --yes
```

**Options:**
- `--yes` - Skip prompts and configure all detected tools
- `--tool <name>` - Configure specific tool (claude-desktop, continue, or cline)

### `add` - Add to Specific Tool

Shorthand for `init --tool`.

```bash
datagvat-mcp add claude-desktop
datagvat-mcp add continue
datagvat-mcp add cline
```

### `update` - Update Configuration

Update MCP Server configuration with diff preview.

```bash
# Interactive mode (shows diff, asks for confirmation)
datagvat-mcp update

# Skip confirmation and apply all updates
datagvat-mcp update --yes

# Update specific tool only
datagvat-mcp update --tool claude-desktop
```

**Options:**
- `--yes` - Skip diff preview and apply updates
- `--tool <name>` - Update specific tool only

### `doctor` - Health Check

Run diagnostics to check configuration health.

```bash
# Run all health checks
datagvat-mcp doctor
```

**Checks:**
- Config files exist and are valid JSON
- MCP server entry is present
- MCP server paths are valid
- Node.js is available (18+)
- Python is available (3.11+, for MCP server)
- uv is available (recommended)

Exit codes:
- `0` - All checks passed or warnings only
- `1` - Critical issues found

## CI/CD Usage

The installer detects CI environments automatically and requires `--yes` flag:

```yaml
# GitHub Actions
- name: Setup MCP Server
  run: npx @datagvat/mcp-installer init --yes
```

Supported CI platforms: GitHub Actions, GitLab CI, CircleCI, Travis CI, Jenkins, and 50+ others via [ci-info](https://github.com/watson/ci-info).

## Error Messages

All errors include actionable fix suggestions:

```
✗ Error: Interactive prompts not available in CI environment
  Detected: GITHUB_ACTIONS
  Fix: Add --yes flag to use defaults
  Example: datagvat-mcp init --yes
```

## Development

```bash
# Install dependencies
bun install

# Run locally
bun src/index.ts init

# Build
bun run build

# Test
bun test
```

## License

MIT
```

Maintain existing content if README already exists, integrate new sections smoothly.
Use existing documentation style from docs/ if available.
  </action>
  <verify>
Check README completeness:
```bash
cd packages/cli
grep -E "init|update|doctor" README.md  # All three commands present
grep -E "CI/CD|--yes" README.md  # CI usage documented
grep "Error Messages" README.md  # Error documentation present
```

Verify all commands have:
- Description
- Usage examples
- Options list
- CI/CD notes where relevant
</verify>
  <done>
- README.md exists with comprehensive documentation
- All four commands documented: init, add, update, doctor
- Each command has description, examples, and options
- CI/CD usage section present with workflow example
- Error message pattern explained with example
- Development section includes build and test commands
</done>
</task>

<task type="auto">
  <name>Task 3: Comprehensive build verification</name>
  <files>
    (verification only, no file changes)
  </files>
  <action>
Run full build verification pipeline to ensure Phase 11 changes don't break anything:

**1. CLI TypeScript Compilation:**
```bash
cd packages/cli
bun run build
```
Expected: Zero TypeScript errors, dist/ directory created with .js and .d.ts files

**2. CLI Tests:**
```bash
cd packages/cli
bun test
```
Expected: All tests pass (existing + new ci.test.ts and diff.test.ts)

**3. Documentation TypeScript Check:**
```bash
cd docs
bun run type-check
```
Expected: Zero TypeScript errors in documentation site

**4. Documentation Quality Check (Biome):**
```bash
cd docs
bun run quality-check
```
Expected: Zero Biome lint errors, zero format issues

**5. Documentation Build:**
```bash
cd docs
bun run build
```
Expected: Build completes successfully, no 404 errors, all routes generated

**6. Backend Tests (MCP Server):**
```bash
cd mcp
pytest
```
Expected: All Python tests pass (268 tests)

**If any check fails:**
- Identify the specific error
- Determine if caused by Phase 11 changes
- Fix the issue immediately
- Re-run all checks until all pass

**Verification script pattern:**
```bash
#!/bin/bash
set -e  # Exit on any error

echo "Building CLI..."
cd packages/cli && bun run build && cd ../..

echo "Testing CLI..."
cd packages/cli && bun test && cd ../..

echo "Type checking docs..."
cd docs && bun run type-check && cd ..

echo "Quality checking docs..."
cd docs && bun run quality-check && cd ..

echo "Building docs..."
cd docs && bun run build && cd ..

echo "Testing MCP server..."
cd mcp && pytest && cd ..

echo "✓ All checks passed!"
```

Document results in verification section of SUMMARY.md.
  </action>
  <verify>
Create and run verification script:
```bash
cd C:\GitHub\datagvat-mcp

# Run each check individually and capture results
packages/cli: bun run build
packages/cli: bun test
docs: bun run type-check
docs: bun run quality-check
docs: bun run build
mcp: pytest
```

Expected output:
- CLI build: "Compiled successfully"
- CLI tests: "X tests passed"
- Type check: "No errors found"
- Quality check: "All checks passed"
- Docs build: "Build completed in Xs"
- MCP tests: "268 passed"

All checks must exit with code 0 (success).
If any check fails, investigate and fix before marking done.
  </verify>
  <done>
- CLI TypeScript compilation succeeds (0 errors)
- CLI tests pass (all existing + new tests)
- Documentation TypeScript check passes (0 errors)
- Documentation quality check passes (0 Biome errors)
- Documentation build succeeds (all routes generated)
- Backend tests pass (268 tests)
- No regressions introduced by Phase 11 changes
- All verification results documented in SUMMARY.md
</done>
</task>

</tasks>

<verification>
**Semantic versioning:**
- package.json shows version 0.2.0
- CHANGELOG.md documents all Phase 11 features
- Version bump appropriate for added features (minor, not major)

**Documentation:**
- README.md includes all four commands (init, add, update, doctor)
- CI/CD usage documented with examples
- Error message patterns explained

**Build verification:**
All checks pass with exit code 0:
- [ ] CLI build (TypeScript)
- [ ] CLI tests (bun test)
- [ ] Docs type check
- [ ] Docs quality check (Biome)
- [ ] Docs build
- [ ] Backend tests (pytest)

**Phase 11 completion:**
All requirements from ROADMAP.md satisfied:
- [x] CLI-01: Interactive prompts (11-01)
- [x] CLI-02: Clear error messages (11-01)
- [x] CLI-03: Validation feedback (11-01)
- [x] CLI-04: Progress indicators (existing, preserved)
- [x] CLI-05: Diff preview (11-02)
- [x] CLI-06: Health check command (11-02)
- [x] CLI-07: Update command (11-02)
- [x] CLI-08: Config validation (11-01)
- [x] CLI-09: Non-interactive mode (11-01)
- [x] CLI-10: Semantic versioning (11-03)
- [x] BUILD-01 through BUILD-05: Full verification (11-03)
</verification>

<success_criteria>
1. Developer runs `bun run build` in packages/cli and sees zero TypeScript errors
2. Developer runs `bun test` in packages/cli and all tests pass
3. Developer runs `bun run quality-check` in docs and sees zero Biome errors
4. Developer runs full docs build and completes successfully
5. User reads README.md and understands how to use all four commands (init, add, update, doctor)
6. package.json shows version 0.2.0 matching CHANGELOG.md
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-excellence/11-03-SUMMARY.md`
</output>
