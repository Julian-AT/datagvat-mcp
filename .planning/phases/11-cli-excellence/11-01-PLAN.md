---
phase: 11-cli-excellence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/package.json
  - packages/cli/src/schemas.ts
  - packages/cli/src/commands/init.ts
  - packages/cli/src/ui.ts
  - packages/cli/src/ci.ts
autonomous: true

must_haves:
  truths:
    - "User runs interactive setup and receives guidance through configuration options"
    - "User provides invalid input and immediately sees clear error message with solution steps"
    - "User runs CLI in CI environment and commands succeed without interactive prompts"
  artifacts:
    - path: "packages/cli/src/schemas.ts"
      provides: "Zod validation schemas for configuration inputs"
      min_lines: 40
      exports: ["ToolNameSchema", "ConfigSchema"]
    - path: "packages/cli/src/ci.ts"
      provides: "CI environment detection and non-interactive mode handling"
      min_lines: 30
      exports: ["isCI", "requireNonInteractive"]
    - path: "packages/cli/src/commands/init.ts"
      provides: "Enhanced init command with validation and CI support"
      contains: "safeParse"
  key_links:
    - from: "packages/cli/src/commands/init.ts"
      to: "packages/cli/src/schemas.ts"
      via: "import and validate input"
      pattern: "from ['\"].*schemas(\\.js)?['\"]"
    - from: "packages/cli/src/commands/init.ts"
      to: "packages/cli/src/ci.ts"
      via: "check CI environment before prompts"
      pattern: "isCI|requireNonInteractive"
    - from: "@inquirer/prompts"
      to: "zod validation"
      via: "validate option in prompt config"
      pattern: "validate:\\s*\\([^)]+\\)\\s*=>\\s*.*safeParse"
---

<objective>
Transform CLI prompts from basic input collection to professional, validated interactions with clear error messages and CI/CD support.

Purpose: Establish foundation for shadcn-quality UX by adding schema validation, actionable error messages, and automated CI detection. Every invalid input should guide users to success.

Output: Enhanced init command with zod validation, CI detection module, comprehensive error messages with fix suggestions, and non-interactive mode that works in CI/CD pipelines.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-excellence/11-RESEARCH.md

# Current CLI implementation
@packages/cli/src/index.ts
@packages/cli/src/commands/init.ts
@packages/cli/src/ui.ts
@packages/cli/src/types.ts
@packages/cli/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zod validation dependencies and create schemas module</name>
  <files>
    packages/cli/package.json
    packages/cli/src/schemas.ts
  </files>
  <action>
Add dependencies to package.json:
- `zod`: ^3.23.8 (runtime validation with TypeScript inference)
- `ci-info`: ^4.0.0 (CI environment detection)
- `jsdiff`: ^7.0.0 (diff generation for future update command)
- `execa`: ^9.0.0 (shell command execution for health checks)

Create schemas.ts with zod validation schemas:
- `ToolNameSchema`: z.enum(['claude-desktop', 'continue', 'cline']) with custom error message
- `ToolSelectionSchema`: z.array(ToolNameSchema).min(1, 'At least one tool must be selected')
- `InitOptionsSchema`: Object schema for --yes (boolean), --tool (optional ToolNameSchema)
- `ConfigPathSchema`: z.string().min(1).refine for valid file paths (no invalid characters)

Export schemas for use in commands. Include helpful error messages:
- Example: "Expected: claude-desktop, continue, or cline. Got: {value}"
- Example: "Tool selection cannot be empty. Select at least one tool to configure."

Follow existing patterns from ui.ts and types.ts for naming conventions.
</action>
  <verify>
Run `cd packages/cli && bun install` and verify all dependencies install without errors.
Run `cd packages/cli && bun run build` and verify schemas.ts compiles with no TypeScript errors.
Check that schemas.ts exports are available: `grep -E "export (const|type)" packages/cli/src/schemas.ts`
</verify>
  <done>
- package.json contains zod, ci-info, jsdiff, execa in dependencies
- schemas.ts exists with 4+ exported schemas
- TypeScript compilation succeeds with zero errors
- Schemas use descriptive custom error messages
</done>
</task>

<task type="auto">
  <name>Task 2: Create CI detection module and enhance error messages</name>
  <files>
    packages/cli/src/ci.ts
    packages/cli/src/ui.ts
  </files>
  <action>
Create ci.ts module using ci-info library:

**Export isCI():** Returns boolean, checks `ciInfo.isCI || !process.stdout.isTTY`
**Export requireNonInteractive():** Checks if interactive prompts available, throws descriptive error if in CI without --yes flag:
  - Error format: "Error: Interactive prompts not available in CI environment\n  Detected: {CI name or 'non-TTY'}\n  Fix: Add --yes flag to use defaults\n  Example: datagvat-mcp init --yes"

Enhance ui.ts with structured error formatting:
**Add formatError(error: Error):** Returns formatted string with:
  - Red bold "✗ Error" header
  - Error message (white text)
  - Dim help link to docs
  - Empty lines for spacing

**Add formatValidationError(zodError: ZodError):** Extracts and formats zod issues:
  - Each issue as "• {path}: {message}" with proper indentation
  - Returns multi-line string ready for console.log
  - Use existing chalk color scheme (cyan for paths, white for messages, dim for bullets)

Follow existing ui.ts style (chalk-based, clean functions, exported individually).
</action>
  <verify>
Create test file `packages/cli/src/ci.test.ts` and run:
```typescript
import { isCI, requireNonInteractive } from './ci.js';
import { describe, it, expect } from 'bun:test';

// Test CI detection
describe('isCI', () => {
  it('detects CI environment', () => {
    process.env.CI = 'true';
    expect(isCI()).toBe(true);
  });
});
```
Run `cd packages/cli && bun test ci.test.ts` to verify module works.
Verify ui.ts exports new functions: `grep -E "export function format" packages/cli/src/ui.ts`
</verify>
  <done>
- ci.ts exports isCI() and requireNonInteractive()
- requireNonInteractive() throws error with fix instructions
- ui.ts exports formatError() and formatValidationError()
- Error messages include problem, detection info, fix, and example
- All tests pass
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate validation and CI detection into init command</name>
  <files>
    packages/cli/src/commands/init.ts
  </files>
  <action>
Enhance init command with validation and CI support:

**At function start (before detection):**
1. Import schemas (InitOptionsSchema), ci module (isCI, requireNonInteractive), formatError/formatValidationError from ui
2. Validate options parameter with InitOptionsSchema.safeParse(options)
3. If validation fails, use formatValidationError to show errors and exit(1)
4. Call requireNonInteractive() if not options.yes and isCI()

**During tool selection (checkbox prompt):**
1. Add inline validation to checkbox prompt's validate option
2. Use ToolSelectionSchema.safeParse(value) in validate function
3. Return validation error message if invalid, return true if valid
4. This provides immediate feedback as user interacts

**After tool selection (before configuration):**
1. Validate selectedToolNames array with ToolSelectionSchema
2. If invalid (shouldn't happen with inline validation, but defensive), show error and exit

**Error handling block:**
1. Replace generic error message with formatError(err)
2. Check if error is ZodError (err.issues exists), use formatValidationError if so
3. Keep existing ExitPromptError handling (Ctrl+C) unchanged

**CI mode path:**
When options.yes is true OR isCI():
- Log: ui.info('Running in non-interactive mode (CI detected)') if isCI() but no --yes
- Use all detected tools (no prompt)
- Proceed to configuration

DO NOT change detection or configuration logic, only add validation and CI checks.
Keep existing ui.step, ui.spinner, and ui.success patterns.
  </action>
  <verify>
Test validation manually:
```bash
cd packages/cli
# Test invalid option (should show validation error)
bun src/index.ts init --tool invalid-tool

# Test CI mode without --yes (should show CI error with fix)
CI=true bun src/index.ts init

# Test CI mode with --yes (should succeed without prompts)
CI=true bun src/index.ts init --yes

# Test normal interactive mode (should work as before)
bun src/index.ts init
```

Verify error messages are formatted and include fix suggestions.
Check that TypeScript compilation succeeds: `bun run build`
</verify>
  <done>
- init.ts imports schemas, ci module, and format functions
- Options validated at function start with helpful error on invalid input
- CI detection prevents prompts in non-interactive environments
- Checkbox prompt has inline validation with immediate feedback
- Error messages use formatError/formatValidationError for consistency
- CI mode works without prompts when --yes provided
- TypeScript compiles with zero errors
</done>
</task>

</tasks>

<verification>
**Schema validation:**
Run `bun src/index.ts init --tool invalid-tool` and verify error message includes:
- "Expected: claude-desktop, continue, or cline"
- "Got: invalid-tool"

**CI detection:**
Run `CI=true bun src/index.ts init` (without --yes) and verify error includes:
- "Interactive prompts not available"
- "Fix: Add --yes flag"

**Non-interactive mode:**
Run `CI=true bun src/index.ts init --yes` and verify:
- No prompts appear
- All detected tools configured automatically
- Success message shown

**Build verification:**
Run `cd packages/cli && bun run build` and verify zero TypeScript errors.
</verification>

<success_criteria>
1. User provides invalid tool name and sees error: "Expected: claude-desktop, continue, or cline. Got: {input}"
2. User runs init in CI without --yes and sees: "Interactive prompts not available in CI environment. Fix: Add --yes flag"
3. User runs `CI=true init --yes` and CLI configures all detected tools without prompts
4. Developer runs `bun run build` and sees zero TypeScript errors
5. All error messages follow format: problem statement + fix suggestion + example
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-excellence/11-01-SUMMARY.md`
</output>
