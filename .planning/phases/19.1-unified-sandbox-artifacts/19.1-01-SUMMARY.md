---
phase: 19.1-unified-sandbox-artifacts
plan: 01
subsystem: database, ai-tools
tags: [drizzle, postgres, sandbox, e2b, ai-sdk, streaming]

# Dependency graph
requires:
  - phase: 18-e2b-testing
    provides: E2B sandbox lifecycle testing, e2b-client.ts
  - phase: 19-tool-approval-flow
    provides: toolApproval table pattern, AI SDK tool patterns
provides:
  - SandboxState database table with migration
  - CRUD queries for sandbox persistence (saveSandboxState, getSandboxState, getSandboxStatesByChat)
  - openSandbox AI tool with dataStream integration
affects: [19.1-02-sandbox-ui, 19.1-03-execution, 19.1-04-integration]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sandbox state upsert with onConflictDoUpdate"
    - "AI tool streaming data-kind/data-id pattern for artifact creation"

key-files:
  created:
    - docs/lib/db/migrations/0008_sandbox_state.sql
    - docs/lib/ai/tools/open-sandbox.ts
  modified:
    - docs/lib/db/schema.ts
    - docs/lib/db/queries.ts

key-decisions:
  - "sandboxId unique constraint enables upsert pattern for code updates"
  - "outputs stored as json for flexibility (stdout/stderr/visualization)"
  - "hasApprovedOnce tracks if user ever approved this sandbox"
  - "e2bSandboxId enables sandbox reuse within session"
  - "openSandbox needsApproval: false - approval at execution time not creation"

patterns-established:
  - "Sandbox artifact streaming: data-kind -> data-id -> data-title -> data-clear -> data-code -> data-finish"

# Metrics
duration: 6min
completed: 2026-02-03
---

# Phase 19.1 Plan 01: Sandbox Data Layer Summary

**SandboxState table with migration, CRUD queries, and openSandbox AI tool for unified sandbox artifact paradigm**

## Performance

- **Duration:** 6 min
- **Started:** 2026-02-03T11:10:58Z
- **Completed:** 2026-02-03T11:16:52Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- SandboxState table persists sandbox state across page refresh
- Upsert queries support iterative code updates within same sandbox
- openSandbox tool streams artifact metadata following create-document pattern
- Migration applies cleanly with foreign keys to Message_v2 and Chat

## Task Commits

Each task was committed atomically:

1. **Task 1: Create SandboxState schema and migration** - `24afc4b` (feat)
2. **Task 2: Add sandbox state CRUD queries** - `ddda3ee` (feat)
3. **Task 3: Create openSandbox tool** - `eb77612` (feat)

## Files Created/Modified
- `docs/lib/db/schema.ts` - Added SandboxState table with sandboxId, messageId, chatId, code, outputs, hasApprovedOnce, e2bSandboxId
- `docs/lib/db/migrations/0008_sandbox_state.sql` - Migration with foreign keys and unique constraint
- `docs/lib/db/queries.ts` - saveSandboxState (upsert), getSandboxState, getSandboxStatesByChat
- `docs/lib/ai/tools/open-sandbox.ts` - AI tool that streams sandbox metadata to client

## Decisions Made
- Used Message_v2 (not deprecated Message) for messageId foreign key
- JSON type for outputs to support mixed content (stdout, stderr, visualization URLs)
- Upsert pattern allows same sandbox to be updated without creating duplicates
- openSandbox streams data-code separately from other metadata for optional initial code

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
- Build lock file issues required clearing .next directory multiple times during verification
- Resolution: Removed .next/lock and rebuilt fresh

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Database layer ready for sandbox UI components (Plan 02)
- openSandbox tool ready for route.ts integration (Plan 04)
- All artifacts compile without errors

---
*Phase: 19.1-unified-sandbox-artifacts*
*Completed: 2026-02-03*
