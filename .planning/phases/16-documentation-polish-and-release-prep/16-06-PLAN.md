# Phase 16 Plan 06: Comprehensive Parameter Error Fix

**Goal:** Fix all remaining parameter syntax errors across all documentation files

## Context

Re-verification after Plan 16-05 revealed that while search.mdx was fixed, identical errors exist in workflows.mdx and workflows.de.mdx:

**Root Cause of Gap:**
- Plan 16-05 was too narrowly scoped (only fixed search.mdx line 724)
- Did not perform comprehensive grep across all documentation files
- Missed 8 additional instances of the same error pattern

**Current State:**
- workflows.mdx: 4 instances of `sort="modified+desc"` at lines 311, 366, 570, 635
- workflows.de.mdx: 4 instances of `sort="modified+desc"` at lines 311, 366, 570, 635
- Total: 8 parameter errors remaining

**Impact:**
- 20% of examples in workflows files will fail
- Critical user journey scenarios affected (daily monitoring, compliance reporting, etc.)
- Inconsistent documentation across files

**Why Comprehensive Fix Needed:**
This plan uses grep-based discovery to ensure ALL instances are found and fixed, preventing future gap re-openings.

## Tasks

### Task 1: Comprehensive Grep Search for Parameter Errors

**What:** Use grep to find ALL instances of incorrect parameter usage across documentation
**Where:** `docs/content/docs/examples/` directory
**How:**
1. Run grep for pattern: `sort="modified+desc"` (exact error pattern)
2. Run grep for pattern: `\ssort=` (catch any other sort= usage)
3. Document all findings with file paths and line numbers
4. Verify no other parameter name errors exist (e.g., wrong names for other parameters)

**Acceptance Criteria:**
- Grep output captures all 8 known instances
- No additional parameter errors discovered
- Search covers both English and German files
- Results provide exact line numbers for targeted fixes

### Task 2: Fix All Instances in workflows.mdx

**What:** Replace all 4 instances of incorrect parameter syntax in English workflows
**Where:** `docs/content/docs/examples/workflows.mdx` lines 311, 366, 570, 635
**How:**
1. Read file to understand context around each error
2. Replace each `sort="modified+desc"` with `sort_by="modified_desc"`
3. Verify all 4 replacements are made correctly
4. Ensure surrounding code remains unchanged

**Acceptance Criteria:**
- Line 311: `sort_by="modified_desc"` (Daily Monitoring workflow)
- Line 366: `sort_by="modified_desc"` (Trend Analysis workflow)
- Line 570: `sort_by="modified_desc"` (Compliance Reporting workflow)
- Line 635: `sort_by="modified_desc"` (Data Quality Audit workflow)
- All 4 instances use underscore format (not plus sign)
- All 4 instances use correct parameter name (sort_by not sort)

### Task 3: Fix All Instances in workflows.de.mdx

**What:** Replace all 4 instances of incorrect parameter syntax in German workflows
**Where:** `docs/content/docs/examples/workflows.de.mdx` lines 311, 366, 570, 635
**How:**
1. Read file to understand context around each error
2. Replace each `sort="modified+desc"` with `sort_by="modified_desc"`
3. Verify all 4 replacements are made correctly
4. Ensure German text content remains unchanged (only parameter syntax fixed)

**Acceptance Criteria:**
- Line 311: `sort_by="modified_desc"` (Tägliche Überwachung workflow)
- Line 366: `sort_by="modified_desc"` (Trendanalyse workflow)
- Line 570: `sort_by="modified_desc"` (Compliance-Berichte workflow)
- Line 635: `sort_by="modified_desc"` (Datenqualitäts-Audit workflow)
- All 4 instances use underscore format (not plus sign)
- All 4 instances use correct parameter name (sort_by not sort)
- German translations unchanged

### Task 4: Final Verification - No Parameter Errors Remain

**What:** Comprehensive grep verification that all parameter errors are fixed
**Where:** All documentation files in `docs/content/docs/`
**How:**
1. Run grep for `sort="modified+desc"` - should return 0 results
2. Run grep for `\ssort=` - should return 0 results
3. Verify search.mdx (fixed in 16-05) still correct
4. Verify search.de.mdx still correct
5. Confirm all code examples use correct parameter names

**Acceptance Criteria:**
- Zero instances of `sort="` in any documentation file
- All search_datasets calls use `sort_by` parameter
- English and German versions consistent
- No regressions in previously fixed files
- 100% documentation accuracy achieved

## Definition of Done

- [ ] Grep search completed and documented all 8 instances
- [ ] workflows.mdx fixed: all 4 lines (311, 366, 570, 635) use `sort_by="modified_desc"`
- [ ] workflows.de.mdx fixed: all 4 lines (311, 366, 570, 635) use `sort_by="modified_desc"`
- [ ] Final verification grep shows zero remaining errors
- [ ] No regressions in search.mdx or search.de.mdx
- [ ] Changes committed with clear message: "fix: comprehensive parameter syntax fix for all workflow examples"

## Dependencies

**Requires:**
- Phase 16-05 completion (search.mdx already fixed)
- Verification results identifying workflows.mdx and workflows.de.mdx gaps

**Provides:**
- 100% accurate code examples across ALL documentation
- Complete gap closure for Phase 16
- Confidence that no other parameter errors exist

## Time Estimate

**5 minutes** - Grep-based comprehensive fix with verification

**Breakdown:**
- Task 1 (Grep search): 1 minute
- Task 2 (workflows.mdx): 1 minute
- Task 3 (workflows.de.mdx): 1 minute
- Task 4 (Final verification): 2 minutes

## Technical Notes

**Why Plan 16-05 Was Insufficient:**
1. Fixed only the specific line mentioned in verification (search.mdx line 724)
2. Did not search for other instances of the same pattern
3. Did not verify workflows.mdx or workflows.de.mdx
4. Narrowly scoped to close one specific gap rather than eliminating the error pattern

**Comprehensive Approach:**
This plan uses grep as the primary discovery mechanism to ensure:
- All instances of the error pattern are found
- Both English and German files are covered
- No similar errors exist with other parameter names
- Fix is thorough and complete

**Parameter Syntax Reference:**
Per `app/tools/discovery.py` line 230, the correct signature is:
```python
sort_by: Literal["relevance_desc", "modified_desc", "title_asc", "title_desc"] = "relevance_desc"
```

**Correct Values:**
- Parameter name: `sort_by` (not `sort`)
- Value format: `modified_desc` (underscore, not `modified+desc` with plus)

**Impact Assessment:**
- Pre-fix: 8/48+ examples broken (approximately 16% failure rate)
- Post-fix: 0/48+ examples broken (100% accuracy)
- Critical workflows affected: Daily monitoring, trend analysis, compliance reporting, data quality audit
