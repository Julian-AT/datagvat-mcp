---
title: MCP Tools Reference
description: Complete reference for all Austria MCP server tools
---

import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';
import { TypeTable } from 'fumadocs-ui/components/type-table';

# MCP Tools

The Austria MCP server provides comprehensive tools for discovering, analyzing, and managing Austrian Open Data datasets.


## Discovery Tools

<Accordions type="single" collapsible>

  <Accordion title="list_catalogues" id="list-catalogues">
    List available data catalogues in the Austrian Open Data Portal.

    **Parameters:**

    <TypeTable type={
      limit: {
        type: "integer",
        description: "Maximum number of catalogues to return (1-5000)",
        default: 100
      },
      offset: {
        type: "integer",
        description: "Number of catalogues to skip for pagination",
        default: 0
      },
      value_type: {
        type: "string",
        description: "Type of value to return: 'id' for IDs only, 'metadata' for full metadata",
        default: "metadata"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    list_catalogues()
    ```
  </Accordion>

  <Accordion title="get_catalogue" id="get-catalogue">
    Get detailed information about a specific catalogue.

    **Parameters:**

    <TypeTable type={
      catalogue_id: {
        type: "string",
        description: "Unique identifier of the catalogue to retrieve"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_catalogue(catalogue_id=...)
    ```
  </Accordion>

  <Accordion title="search_datasets" id="search-datasets">
    Search for datasets with text queries and faceted filtering. Supports full-text search with fuzzy matching, theme/format/publisher filters, date range filtering, and multiple sort options. Returns paginated results with total count.

    **Parameters:**

    <TypeTable type={
      query: {
        type: "string",
        description: "Search query for titles, descriptions, keywords. Supports fuzzy search (add ~ for typo tolerance, e.g. 'health~'), wildcards (e.g. 'europ*'), phrases (e.g. '"open data"'), and boolean operators (AND, OR, NOT). Leave empty to list all datasets."
      },
      themes: {
        type: "string[]",
        description: "Filter by EU data theme codes. Valid codes: AGRI (Agriculture), ECON (Economy), EDUC (Education), ENER (Energy), ENVI (Environment), GOVE (Government), HEAL (Health), INTR (International), JUST (Justice), REGI (Regions), SOCI (Society), TECH (Technology), TRAN (Transport). Multiple values use OR logic."
      },
      formats: {
        type: "string[]",
        description: "Filter by file format (e.g. CSV, JSON, XML, PDF, GeoJSON). Multiple values use OR logic. Case-insensitive."
      },
      publishers: {
        type: "string[]",
        description: "Filter by publisher/organization ID. Multiple values use OR logic."
      },
      min_date: {
        type: "string",
        description: "Filter datasets modified/issued after this date. Format: YYYY-MM-DD or ISO 8601 (2025-01-01T00:00:00Z)."
      },
      max_date: {
        type: "string",
        description: "Filter datasets modified/issued before this date. Format: YYYY-MM-DD or ISO 8601 (2025-01-01T23:59:59Z)."
      },
      sort_by: {
        type: "string",
        description: "Sort order. Options: relevance (default with query), modified_desc (most recent), modified_asc (oldest), title_asc (alphabetical), title_desc (reverse alphabetical), issued_desc (most recently published), issued_asc (oldest published).",
        default: "relevance"
      },
      boost_quality: {
        type: "boolean",
        description: "Boost high-quality datasets in search results. Uses dataset quality metrics (completeness, freshness, compliance) to re-rank results. Only effective when query is provided.",
        default: false
      },
      limit: {
        type: "integer",
        description: "Maximum number of datasets to return per page (1-100)",
        default: 20
      },
      page: {
        type: "integer",
        description: "Page number for pagination (0-indexed)",
        default: 0
      },
      catalogue_id: {
        type: "string",
        description: "Filter datasets to a specific catalogue ID"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    search_datasets()
    ```
  </Accordion>

  <Accordion title="get_dataset" id="get-dataset">
    Get detailed metadata for a specific dataset.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the dataset to retrieve"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_dataset(dataset_id=...)
    ```
  </Accordion>

  <Accordion title="get_dataset_distributions" id="get-dataset-distributions">
    Get all distributions (downloadable files) for a dataset.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the dataset whose distributions to retrieve"
      },
      limit: {
        type: "integer",
        description: "Maximum number of distributions to return (1-100)",
        default: 50
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_dataset_distributions(dataset_id=..., )
    ```
  </Accordion>

  <Accordion title="get_catalogue_record" id="get-catalogue-record">
    Get the catalogue record (provenance) for a dataset.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the dataset whose catalogue record to retrieve"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_catalogue_record(dataset_id=...)
    ```
  </Accordion>

  <Accordion title="find_related_datasets" id="find-related-datasets">
    Find datasets related to a source dataset based on shared themes and keywords. Returns similar datasets ranked by a similarity score (0-100). Useful for discovering additional relevant data sources.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the source dataset to find related datasets for"
      },
      limit: {
        type: "integer",
        description: "Maximum number of related datasets to return (1-50, default 10).",
        default: 10
      },
      min_score: {
        type: "number",
        description: "Minimum similarity score to include (0-100, default 20).",
        default: 20.0
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    find_related_datasets(dataset_id=..., )
    ```
  </Accordion>

  <Accordion title="semantic_search_datasets" id="semantic-search-datasets">
    Search for datasets using natural language queries. Ask in plain English or German - the system will understand your intent and find relevant datasets. Examples: 'health data from Vienna', 'Gesundheitsdaten aus Wien', 'environmental datasets for research'. Uses AI to expand your query into themes, keywords, and filters.

    **Parameters:**

    <TypeTable type={
      natural_query: {
        type: "string",
        description: "Natural language query in English or German. Examples: 'health data from Vienna', 'transport information for tourists', 'Umweltdaten für die Forschung', 'economic statistics about Austria'. The system will understand your intent and find relevant datasets."
      },
      themes: {
        type: "string[]",
        description: "Additional theme filters (optional). Valid codes: AGRI (Agriculture), ECON (Economy), EDUC (Education), ENER (Energy), ENVI (Environment), GOVE (Government), HEAL (Health), INTR (International), JUST (Justice), REGI (Regions), SOCI (Society), TECH (Technology), TRAN (Transport). Will be combined with AI-detected themes."
      },
      formats: {
        type: "string[]",
        description: "Additional format filters (optional). Examples: CSV, JSON, XML, PDF. Will be combined with AI-detected formats if any."
      },
      publishers: {
        type: "string[]",
        description: "Additional publisher filters (optional). Organization IDs. Will be combined with AI-detected publishers if any."
      },
      min_date: {
        type: "string",
        description: "Filter datasets modified/issued after this date (optional). Format: YYYY-MM-DD or ISO 8601 (2025-01-01T00:00:00Z)."
      },
      max_date: {
        type: "string",
        description: "Filter datasets modified/issued before this date (optional). Format: YYYY-MM-DD or ISO 8601 (2025-01-01T23:59:59Z)."
      },
      sort_by: {
        type: "string",
        description: "Sort order. Options: relevance (default), modified_desc (most recent), modified_asc (oldest), title_asc (alphabetical), title_desc (reverse alphabetical), issued_desc (most recently published), issued_asc (oldest published).",
        default: "relevance"
      },
      boost_quality: {
        type: "boolean",
        description: "Boost high-quality datasets in search results. Defaults to True for semantic search as quality is often important when users ask natural language questions.",
        default: true
      },
      limit: {
        type: "integer",
        description: "Maximum number of datasets to return per page (1-100)",
        default: 20
      },
      page: {
        type: "integer",
        description: "Page number for pagination (0-indexed)",
        default: 0
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    semantic_search_datasets(natural_query=..., )
    ```
  </Accordion>

</Accordions>


## Analysis Tools

<Accordions type="single" collapsible>

  <Accordion title="get_dataset_metrics" id="get-dataset-metrics">
    Get metadata quality metrics (DQV) for a dataset.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the dataset to retrieve quality metrics for"
      },
      include_history: {
        type: "boolean",
        description: "Whether to include historical quality metrics. Default: False",
        default: false
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_dataset_metrics(dataset_id=..., )
    ```
  </Accordion>

  <Accordion title="check_doi_eligibility" id="check-doi-eligibility">
    Check if a dataset is eligible for a DOI identifier.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the dataset to check DOI eligibility for"
      },
      identifier_type: {
        type: "string",
        description: "Type of identifier to check eligibility for (default: 'eu-ra-doi')",
        default: "eu-ra-doi"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    check_doi_eligibility(dataset_id=..., )
    ```
  </Accordion>

  <Accordion title="analyze_dataset_quality" id="analyze-dataset-quality">
    Perform comprehensive quality analysis of a dataset.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the dataset to perform quality analysis on"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    analyze_dataset_quality(dataset_id=...)
    ```
  </Accordion>

</Accordions>


## Preview Tools

<Accordions type="single" collapsible>

  <Accordion title="preview_schema" id="preview-schema">
    Get the schema (column names and types) of a CSV or JSON dataset. Fetches only the first portion of the file to infer structure. Supports CSV files and JSON arrays of objects.

    **Parameters:**

    <TypeTable type={
      url: {
        type: "string",
        description: "Direct download URL of the dataset file (CSV or JSON). Get this from get_dataset_distributions tool's downloadURL field."
      },
      format: {
        type: "string",
        description: "File format: 'csv' or 'json'. Auto-detected from URL if not specified."
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    preview_schema(url=..., )
    ```
  </Accordion>

  <Accordion title="preview_data" id="preview-data">
    Preview the first rows of a CSV or JSON dataset. Fetches only enough data to return the requested number of rows. Useful for understanding data content before downloading.

    **Parameters:**

    <TypeTable type={
      url: {
        type: "string",
        description: "Direct download URL of the dataset file. Get this from get_dataset_distributions tool's downloadURL field."
      },
      max_rows: {
        type: "integer",
        description: "Maximum number of rows to return (1-100, default 20).",
        default: 20
      },
      format: {
        type: "string",
        description: "File format: 'csv' or 'json'. Auto-detected if not specified."
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    preview_data(url=..., )
    ```
  </Accordion>

</Accordions>


## Management Tools

<Accordions type="single" collapsible>

  <Accordion title="list_dataset_drafts" id="list-dataset-drafts">
    List dataset drafts you have access to.

    **Parameters:**

    <TypeTable type={
      filter_by_provider: {
        type: "boolean",
        description: "Whether to filter drafts by the current provider. Default: False",
        default: false
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    list_dataset_drafts()
    ```
  </Accordion>

  <Accordion title="get_dataset_draft" id="get-dataset-draft">
    Get the metadata of a specific dataset draft.

    **Parameters:**

    <TypeTable type={
      draft_id: {
        type: "string",
        description: "Unique identifier of the draft to retrieve"
      },
      catalogue_id: {
        type: "string",
        description: "Unique identifier of the catalogue containing the draft"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_dataset_draft(draft_id=..., catalogue_id=...)
    ```
  </Accordion>

  <Accordion title="create_dataset_draft" id="create-dataset-draft">
    Create a new dataset draft. Requires API key.

    **Parameters:**

    <TypeTable type={
      catalogue_id: {
        type: "string",
        description: "Unique identifier of the catalogue to create the draft in"
      },
      title: {
        type: "string",
        description: "Title of the dataset (1-500 characters)"
      },
      description: {
        type: "string",
        description: "Description of the dataset (1-5000 characters)"
      },
      language: {
        type: "string",
        description: "Language code (ISO 639-1): 'de' for German, 'en' for English",
        default: "de"
      },
      keywords: {
        type: "string[]",
        description: "List of keyword tags for categorizing the dataset"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    create_dataset_draft(catalogue_id=..., title=..., description=..., )
    ```
  </Accordion>

  <Accordion title="update_dataset_draft" id="update-dataset-draft">
    Update an existing dataset draft. Requires API key.

    **Parameters:**

    <TypeTable type={
      draft_id: {
        type: "string",
        description: "Unique identifier of the draft to update"
      },
      catalogue_id: {
        type: "string",
        description: "Unique identifier of the catalogue containing the draft"
      },
      title: {
        type: "string",
        description: "New title for the dataset (1-500 characters). Optional"
      },
      description: {
        type: "string",
        description: "New description for the dataset (1-5000 characters). Optional"
      },
      language: {
        type: "string",
        description: "Language code (ISO 639-1): 'de' for German, 'en' for English",
        default: "de"
      },
      keywords: {
        type: "string[]",
        description: "New list of keyword tags. Optional"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    update_dataset_draft(draft_id=..., catalogue_id=..., )
    ```
  </Accordion>

  <Accordion title="delete_dataset_draft" id="delete-dataset-draft">
    Delete a dataset draft permanently. Requires API key.

    **Parameters:**

    <TypeTable type={
      draft_id: {
        type: "string",
        description: "Unique identifier of the draft to delete"
      },
      catalogue_id: {
        type: "string",
        description: "Unique identifier of the catalogue containing the draft"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    delete_dataset_draft(draft_id=..., catalogue_id=...)
    ```
  </Accordion>

  <Accordion title="publish_dataset" id="publish-dataset">
    Publish a dataset draft to make it live. Requires API key.

    **Parameters:**

    <TypeTable type={
      draft_id: {
        type: "string",
        description: "Unique identifier of the draft to publish"
      },
      catalogue_id: {
        type: "string",
        description: "Unique identifier of the catalogue containing the draft"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    publish_dataset(draft_id=..., catalogue_id=...)
    ```
  </Accordion>

  <Accordion title="hide_dataset" id="hide-dataset">
    Withdraw a published dataset back to draft. Requires API key.

    **Parameters:**

    <TypeTable type={
      dataset_id: {
        type: "string",
        description: "Unique identifier of the published dataset to withdraw"
      },
      catalogue_id: {
        type: "string",
        description: "Unique identifier of the catalogue containing the dataset"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    hide_dataset(dataset_id=..., catalogue_id=...)
    ```
  </Accordion>

</Accordions>


## Vocabulary Tools

<Accordions type="single" collapsible>

  <Accordion title="list_vocabularies" id="list-vocabularies">
    List available controlled vocabularies.

    **Parameters:**

    <TypeTable type={
      limit: {
        type: "integer",
        description: "Maximum number of vocabularies to return (1-5000)",
        default: 100
      },
      offset: {
        type: "integer",
        description: "Number of vocabularies to skip for pagination",
        default: 0
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    list_vocabularies()
    ```
  </Accordion>

  <Accordion title="get_vocabulary" id="get-vocabulary">
    Get the contents of a specific vocabulary.

    **Parameters:**

    <TypeTable type={
      vocabulary_id: {
        type: "string",
        description: "Unique identifier of the vocabulary to retrieve"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_vocabulary(vocabulary_id=...)
    ```
  </Accordion>

  <Accordion title="search_vocabulary_terms" id="search-vocabulary-terms">
    Search for terms within a vocabulary.

    **Parameters:**

    <TypeTable type={
      vocabulary_id: {
        type: "string",
        description: "Unique identifier of the vocabulary to search within"
      },
      query: {
        type: "string",
        description: "Search query to match against vocabulary term labels"
      },
      language: {
        type: "string",
        description: "Language code (ISO 639-1): 'de' for German, 'en' for English",
        default: "de"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    search_vocabulary_terms(vocabulary_id=..., query=..., )
    ```
  </Accordion>

  <Accordion title="get_autocomplete_suggestions" id="get-autocomplete-suggestions">
    Get autocomplete suggestions for search queries. Returns vocabulary terms and common patterns matching the query prefix. Useful for search-as-you-type functionality.

    **Parameters:**

    <TypeTable type={
      query: {
        type: "string",
        description: "Query prefix to get suggestions for (minimum 1 character)"
      },
      limit: {
        type: "integer",
        description: "Maximum number of suggestions to return (1-20)",
        default: 10
      },
      language: {
        type: "string",
        description: "Language code (ISO 639-1): 'de' for German, 'en' for English",
        default: "de"
      }
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_autocomplete_suggestions(query=..., )
    ```
  </Accordion>

  <Accordion title="get_resource_types" id="get-resource-types">
    List available resource types.

    **Parameters:**

    <TypeTable type={
    {"}"}} />

    **Returns:**

    ```json
    // Return type: object
    // See tool implementation for detailed schema
    ```

    **Example:**

    ```python
    # In Claude Desktop
    get_resource_types()
    ```
  </Accordion>

</Accordions>

