---
title: Your First Query
description: Search, explore, and preview Austrian open data
icon: Search
---

import { Callout } from 'fumadocs-ui/components/callout';

Once you've [installed the MCP server](/docs/installation), you're ready to start exploring Austria's 10,000+ open datasets.

## Search for Datasets

Ask your AI assistant in natural language:

```
Find datasets about Vienna population
```

![Claude Desktop showing a search for Vienna population datasets. The assistant uses the search_datasets tool and returns results including Bevölkerung Wien 2020-2024 with quality score 92, Einwohner nach Bezirken with score 87, and Demografische Entwicklung Wien with score 81. Each result shows title, publisher, formats available, and quality score.](/optimized/screenshots/placeholder.webp)

<Callout type="info" title="Screenshot placeholder">
Screenshot showing Claude Desktop with data.gv.at MCP Server search results for Vienna population datasets.
</Callout>

The MCP server searches data.gv.at and returns matching datasets with:

- **Title and description** in German (the source language)
- **Publisher** — which government agency provides the data
- **Formats** — CSV, JSON, XML, or other available downloads
- **Quality score** — 0-100 rating based on metadata completeness

Results are ranked by relevance and quality, so the best datasets appear first.

## Get Dataset Details

Once you find an interesting dataset, ask for more information:

```
Tell me more about Bevölkerung Wien 2020-2024
```

You'll get the full metadata: license terms, temporal coverage, spatial extent, update frequency, and all available download formats.

![Claude Desktop showing detailed metadata for a dataset including title, description, publisher contact, license CC BY 4.0, temporal coverage 2020-2024, spatial coverage Vienna, and available distributions in CSV and JSON formats.](/optimized/screenshots/placeholder.webp)

<Callout type="info" title="Screenshot placeholder">
Screenshot showing Claude Desktop displaying full dataset metadata including license, coverage, and download options.
</Callout>

## Preview the Data

Before downloading a large file, preview its contents:

```
Preview the CSV from Bevölkerung Wien 2020-2024
```

The MCP server fetches the first rows and shows you:

- Column names and inferred types
- Sample values from each column
- Total row count estimate

![Claude Desktop showing a data preview with column headers Bezirk, Jahr, Quartal, Bevölkerung and sample rows of data. The preview shows schema inference detecting string and integer types, with 2,400 total rows estimated.](/optimized/screenshots/placeholder.webp)

<Callout type="info" title="Screenshot placeholder">
Screenshot showing Claude Desktop with CSV data preview, schema inference, and row count estimate.
</Callout>

This helps you verify the data structure matches your needs without downloading the entire file.

## Tips for Better Results

**Be specific about location:**
```
Datasets about traffic in Graz
```

**Specify the data type:**
```
Time series data on unemployment rates
```

**Ask about quality:**
```
Show me high-quality datasets (score above 80) about education
```

**Filter by format:**
```
Find CSV datasets about air quality
```

## What You Can Ask

The MCP server supports these types of queries:

| Query Type | Example |
|------------|---------|
| Search | "Find datasets about X" |
| Details | "Tell me more about [dataset]" |
| Preview | "Preview the CSV/JSON from [dataset]" |
| Quality | "What's the quality score of [dataset]?" |
| Formats | "What formats are available for [dataset]?" |
| Related | "Find datasets similar to [dataset]" |
| Compare | "Compare [dataset A] with [dataset B]" |

## Next Steps

- [Searching Guide](/docs/guides/searching) — Advanced search techniques
- [Data Preview](/docs/guides/data-preview) — Preview options and limits
- [Quality Metrics](/docs/guides/quality-metrics) — Understanding quality scores
