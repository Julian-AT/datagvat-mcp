name: austria-mcp
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - pip
  - pip:
      - fastmcp>=2.3.0
      - httpx>=0.27.0
      - rdflib>=7.0.0
      - pydantic>=2.0.0
      - pydantic-settings>=2.0.0
      # dev dependencies (optional)
      # - pytest>=8.0.0
      # - pytest-asyncio>=0.23.0
      # - ruff>=0.4.0

